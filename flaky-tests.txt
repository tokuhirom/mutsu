# Flaky test quarantine ledger.
#
# Format:  <test-path>  <added:YYYY-MM-DD>  <review:YYYY-MM-DD>  <reason>
#
# A listed test is re-run (up to 3 attempts) by scripts/flaky-retry.sh, which
# both test runners go through, so a single unlucky roll no longer blocks a
# merge. It is NOT immunity: a deterministic failure fails all three attempts
# and still blocks. Every retry is reported in the CI job summary.
#
# The bar for adding an entry is deliberately high -- the non-determinism must
# be understood and must not be a defect in mutsu. Crashes, wrong answers, and
# anything whose root cause is unknown are never quarantined; they are bugs to
# fix. Read docs/flaky-test-policy.md before touching this file, and back every
# entry with numbers from scripts/ci-flake-survey.sh and scripts/flake-repro.sh.
#
# Entries expire: `make check-flaky-list` fails once the review date passes,
# which forces a re-measurement rather than a silent permanent quarantine.
#
# Keep sorted by path.

roast/integration/advent2014-day05.t 2026-08-13 2026-11-11 CORRECTED 2026-09-05: the "tests 4-7 are statistically flaky" half of the reason below was WRONG and is retained only as history. Tests 4-7 are fully determined -- each emitted n touches only index n -- so their failures were a real defect: `Supply.act` never enforced its "executed by only one thread at a time" guarantee, so concurrent `start` emitters ran the act callbacks at once and their writes to the shared captured `@seen` raced (measured 1 failure/64 native runs, 2/36 under ASan, with lost writes like `... 16 17 18 19 Buzz` where index 18 must be Fizz, and one Rust bounds-check panic proving concurrent `Vec::resize` on one aliased node). Fixed by serializing act tap dispatch; 0/80 after. That same unsynchronized aliased write is also the SIGABRT-in-`__libc_free` recorded below. See news/2026-09/supply-act-serialization-and-the-concurrency-crash-cluster.md. The entry is KEPT only for test 3's `$*SCHEDULER.cue` race, which the test itself asserts (`isnt $a, $times`) and which has not been re-measured; drop this entry at the next review if test 3 proves stable. HISTORY: Statistically flaky by design: test 3 (a `#?rakudo skip` comment mutsu does not act on without MUTSU_FUDGE) explicitly asserts a race outcome (`isnt $a, $times` after `$*SCHEDULER.cue: { $a++ } for ^100000; sleep 5`) and later tests build on Supplier.act taps racing against `start { sleep rand; ... }` emits. Job-spread evidence: CI run 31657310236 (PR #6350) failed `advent2014-day05.t` test 7 in jit-stress only; the `test` job on the identical commit passed it cleanly (`Roast tests (make roast)` log: `ok`). Confirmed non-deterministic and pre-existing (not a regression) by running the file repeatedly against both the pre-merge and post-merge release binaries under heavy machine load (multiple concurrent unrelated `cargo build --release` from other sessions): both binaries showed the same intermittent test-3/test-7 failures and stalls at that rate: neither binary is more or less reliable. `scripts/flake-repro.sh -n 10 -l 4 -e MUTSU_JIT=on -e MUTSU_JIT_THRESHOLD=2` passed 10/10 once the machine's extra load cleared, consistent with this being a genuine CPU-contention-timing flake rather than a code defect. SCOPE (re-measured 2026-08-20, closing todo/deep/procasync-stress-segv.md slice 4): this quarantine covers ASSERTION failures only. CI run 32116354874 showed this same file dying of SIGABRT inside glibc's allocator (heap corruption, tmp/crash/49110.txt) and being silently retried green -- that hole was closed by #6695, after which scripts/flaky-retry.sh refuses to retry any rc>=128 signal death and fails immediately. A crash here is therefore NOT quarantined and will fail CI loudly, per the "crashes are never quarantined" rule above; the underlying heap corruption is still un-root-caused and awaits a recurrence under the named-thread crash reports.
