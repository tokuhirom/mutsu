# doc-diff-harness report
# stats: match=31  mismatch=2  mutsu-crash=0  no-oracle=79  raku-drift=2  skipped-marker=5  skipped-nondet=9

==============================================================================
[1] output-mismatch  raku-doc/doc/Type/independent-routines.rakudoc:148
--- program ---
my $name = "Alice";

say "Hello, $name";

repl();

say "Goodbye, $name"
--- raku stdout ---
Hello, Alice
No line editor found.

You may want to exit first and `zef install Readline`, `zef install
Linenoise`, or `zef install Terminal::LineEditor` or install `rlwrap`
for a line editor before entering the REPL again.

Type 'exit' to leave
[0] > Goodbye, Alice
--- mutsu stdout (exit 0) ---
Hello, Alice
Goodbye, Alice

==============================================================================
[2] output-mismatch  raku-doc/doc/Type/independent-routines.rakudoc:473
--- program ---
my $fh = open :w, '/tmp/some-file.txt';
$fh.say: 'I ♥ writing Raku code';
$fh.close;

$fh = open '/tmp/some-file.txt';
print $fh.readchars: 4;
$fh.seek: 7, SeekFromCurrent;
say $fh.readchars: 4;
$fh.close;

# OUTPUT: «I ♥ Raku␤»
--- raku stdout ---
I ♥ Raku
--- mutsu stdout (exit 0) ---
I ♥  Rak

==============================================================================
[3] raku-drift-from-doc  raku-doc/doc/Type/independent-routines.rakudoc:312
--- program ---
say indir("/tmp", {
    gather { take ".".IO }
})».CWD; # OUTPUT: «(/home/camelia)␤»

say indir("/tmp", {
    eager gather { take ".".IO }
})».CWD; # OUTPUT: «(/tmp)␤»

say indir("/tmp", {
    my $cwd = $*CWD;
    gather { temp $*CWD = $cwd; take ".".IO }
})».CWD; # OUTPUT: «(/tmp)␤»
--- raku stdout ---
(/home/tokuhirom/work/mutsu-roast)
(/tmp)
(/tmp)
--- mutsu stdout (exit 0) ---
()
(/tmp)
()

==============================================================================
[4] raku-drift-from-doc  raku-doc/doc/Type/independent-routines.rakudoc:1429
--- program ---
my %h = i => 0;
append %h, i => (1, 42);
CATCH { default { put .message } };
# OUTPUT: «Unexpected named argument 'i' passed␤»
--- raku stdout ---
Cannot resolve caller append(Hash:D, :i(List)); none of these signatures matches:
    (\a, \b)
    (\a, **@b is raw)
--- mutsu stdout (exit 0) ---
Unknown call: append

