# doc-diff-harness report
# stats: match=6  matches-doc=1  mismatch=1  mutsu-crash=0  no-oracle=28  skipped-marker=0  skipped-nondet=4  skipped-oracle-nondet=0

==============================================================================
[1] output-mismatch  raku-doc/doc/Type/Promise.rakudoc:14
note: mutsu matches the doc's own `# OUTPUT:` here; raku does not.
--- program ---
my $p = Promise.start({ sleep 2; 42});
$p.then({ say .result });   # will print 42 once the block finished
say $p.status;              # OUTPUT: «Planned␤»
$p.result;                  # waits for the computation to finish
say $p.status;              # OUTPUT: «Kept␤»
--- raku stdout ---
Planned
42
Kept
--- mutsu stdout (exit 0) ---
Planned
Kept

