# doc-diff-harness report
# stats: error-match=0  error-mismatch=0  match=6  matches-doc=1  mismatch=1  mutsu-accepts=1  mutsu-crash=0  no-oracle=23  silent-crash=0  silent-match=4  silent-noise=0  skipped-marker=0  skipped-nondet=4  skipped-oracle-nondet=0

==============================================================================
[1] mutsu-accepts  raku-doc/doc/Type/Promise.rakudoc:81
--- program ---
use v6.d;
start { die }; sleep ⅓; say "hello";
# OUTPUT:
# Unhandled exception in code scheduled on thread 4
# Died
#     in block  at -e line 1
--- raku stderr (exit 1) ---
Unhandled exception in code scheduled on thread 4
Died
  in block  at /home/user/mutsu/tmp/ddh/prog-15036.raku line 2
--- raku stdout ---
(none)
--- mutsu stdout (exit 0) ---
hello

==============================================================================
[2] output-mismatch  raku-doc/doc/Type/Promise.rakudoc:14
note: mutsu matches the doc's own `# OUTPUT:` here; raku does not.
--- program ---
my $p = Promise.start({ sleep 2; 42});
$p.then({ say .result });   # will print 42 once the block finished
say $p.status;              # OUTPUT: «Planned␤»
$p.result;                  # waits for the computation to finish
say $p.status;              # OUTPUT: «Kept␤»
--- raku stdout ---
Planned
42
Kept
--- mutsu stdout (exit 0) ---
Planned
Kept

