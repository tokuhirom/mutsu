# doc-diff-harness report
# stats: error-match=0  error-mismatch=0  match=1  matches-doc=0  mismatch=1  mutsu-accepts=0  mutsu-crash=0  no-oracle=1  silent-crash=0  silent-match=1  silent-noise=0  skipped-marker=0  skipped-nondet=0  skipped-oracle-nondet=0

==============================================================================
[1] output-mismatch  raku-doc/doc/Type/X/Proc/Async/TapBeforeSpawn.rakudoc:13
--- program ---
my $proc = Proc::Async.new("echo", "foo");
$proc.start;
$proc.stdout.tap(&print);
CATCH { default { put .^name, ': ', .Str } };
# OUTPUT: «X::Proc::Async::TapBeforeSpawn: To avoid data races, you must tap stdout before running the process␤»
--- raku stdout ---
X::Proc::Async::TapBeforeSpawn: To avoid data races, you must tap stdout before running the process
foo
--- mutsu stdout (exit 0) ---
foo
X::Proc::Async::TapBeforeSpawn: To avoid data races, you must tap stdout before running the process

