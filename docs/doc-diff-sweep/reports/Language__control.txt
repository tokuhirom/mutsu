# doc-diff-harness report
# stats: match=72  matches-doc=0  mismatch=1  mutsu-crash=1  no-oracle=17  skipped-marker=9  skipped-nondet=10  skipped-oracle-nondet=1

==============================================================================
[1] mutsu-error  raku-doc/doc/Language/control.rakudoc:48
--- program ---
say "We get here";
{ say "then here." };
{ say "not here"; 0; } or die;
--- raku stdout ---
We get here
then here.
--- mutsu stdout (exit 1) ---
We get here
then here.
not here
--- mutsu stderr ---
Useless use of constant integer 0 in sink context
    at /home/user/mutsu/tmp/ddh/prog-9075.raku:3
Died
  in block <unit> at /home/user/mutsu/tmp/ddh/prog-9075.raku line 3

==============================================================================
[2] output-mismatch  raku-doc/doc/Language/control.rakudoc:1368
--- program ---
sub s(){ my $a = 41; return $a };
say ++s();
CATCH { default { say .^name, ': ', .Str } };
# OUTPUT: «X::Multi::NoMatch.new(dispatcher …
--- raku stdout ---
X::Multi::NoMatch: Cannot resolve caller prefix:<++>(Int:D); the following candidates
match the type but require mutable arguments:
    (Mu:D $a is rw)
    (Int:D $a is rw --> Int:D)
    (int $a is rw --> int)
    (uint $a is rw --> uint)
    (num $a is rw --> num)

The following do not match for other reasons:
    (Bool $a is rw)
    (Mu:U $a is rw)
    (Num:D $a is rw)
    (Num:U $a is rw)
--- mutsu stdout (exit 0) ---
X::Multi::NoMatch: Cannot resolve caller prefix:<++>(...); the parameter requires mutable arguments

