# doc-diff-harness report
# stats: match=72  mismatch=1  mutsu-crash=1  no-oracle=17  raku-drift=1  skipped-marker=9  skipped-nondet=10

==============================================================================
[1] mutsu-error  raku-doc/doc/Language/control.rakudoc:48
--- program ---
say "We get here";
{ say "then here." };
{ say "not here"; 0; } or die;
--- raku stdout ---
We get here
then here.
--- mutsu stdout (exit 1) ---
We get here
then here.
not here
--- mutsu stderr ---
Useless use of constant integer 0 in sink context
    at /home/tokuhirom/work/mutsu-roast/tmp/ddh/prog-1339511.raku:3
Died
  in block <unit> at tmp/ddh/prog-1339511.raku line 3

==============================================================================
[2] raku-drift-from-doc  raku-doc/doc/Language/control.rakudoc:570
--- program ---
my @foo = 1..3;
for @foo.kv -> $idx, $val { say "$idx: $val" }
my %hash = <a b c> Z=> 1,2,3;
for %hash.kv -> $key, $val { say "$key => $val" }
for 1, 1.1, 2, 2.1 { say "$^x < $^y" }  # OUTPUT: «1 < 1.1␤2 < 2.1␤»
--- raku stdout ---
0: 1
1: 2
2: 3
c => 3
b => 2
a => 1
1 < 1.1
2 < 2.1
--- mutsu stdout (exit 0) ---
0: 1
1: 2
2: 3
b => 2
a => 1
c => 3
1 < 1.1
2 < 2.1

==============================================================================
[3] output-mismatch  raku-doc/doc/Language/control.rakudoc:1368
--- program ---
sub s(){ my $a = 41; return $a };
say ++s();
CATCH { default { say .^name, ': ', .Str } };
# OUTPUT: «X::Multi::NoMatch.new(dispatcher …
--- raku stdout ---
X::Multi::NoMatch: Cannot resolve caller prefix:<++>(Int:D); the following candidates
match the type but require mutable arguments:
    (Mu:D $a is rw)
    (Int:D $a is rw --> Int:D)
    (int $a is rw --> int)
    (uint $a is rw --> uint)
    (num $a is rw --> num)

The following do not match for other reasons:
    (Bool $a is rw)
    (Mu:U $a is rw)
    (Num:D $a is rw)
    (Num:U $a is rw)
--- mutsu stdout (exit 0) ---
X::Multi::NoMatch: Cannot resolve caller prefix:<++>(...); the parameter requires mutable arguments

