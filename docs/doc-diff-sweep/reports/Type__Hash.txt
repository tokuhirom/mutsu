# doc-diff-harness report
# stats: match=29  mismatch=1  mutsu-crash=0  no-oracle=15  raku-drift=1  skipped-marker=0  skipped-nondet=1

==============================================================================
[1] output-mismatch  raku-doc/doc/Type/Hash.rakudoc:21
--- program ---
# initialization with pairs:
my %capitals = Spain => 'Madrid', 'United States' => 'Washington DC';

# adding another pair:
%capitals{'Zimbabwe'} = 'Harare';

# accessing a value by key:
my $country = 'Spain';
say "The capital of $country is %capitals{$country}";

# getting all keys:
say "I know the capitals of these countries: ", %capitals.keys.sort.join(', ');

# check if a key is in a hash:
if %capitals{'Europe'}:exists {
    # not executed
}

# iterating over keys and values (unordered):
for %capitals.kv -> $country, $capital {
    say "$capital is the capital of $country";
}
--- raku stdout ---
The capital of Spain is Madrid
I know the capitals of these countries: Spain, United States, Zimbabwe
Madrid is the capital of Spain
Harare is the capital of Zimbabwe
Washington DC is the capital of United States
--- mutsu stdout (exit 0) ---
The capital of Spain is Madrid
I know the capitals of these countries: Spain, United States, Zimbabwe
Harare is the capital of Zimbabwe
Washington DC is the capital of United States
Madrid is the capital of Spain

==============================================================================
[2] raku-drift-from-doc  raku-doc/doc/Type/Hash.rakudoc:342
--- program ---
push my %h, f => 7;
CATCH { default { put .message } };
# OUTPUT: «Unexpected named argument 'f' passed␤»
--- raku stdout ---
Cannot resolve caller push(Hash:D, :f(Int)); none of these signatures matches:
    (\a, \b)
    (\a, **@b is raw)
--- mutsu stdout (exit 0) ---
Unknown call: push

