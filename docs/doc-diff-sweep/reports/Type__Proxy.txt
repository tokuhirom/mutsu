# doc-diff-harness report
# stats: match=0  mismatch=0  mutsu-crash=1  no-oracle=2  raku-drift=0  skipped-marker=0  skipped-nondet=0

==============================================================================
[1] mutsu-error  raku-doc/doc/Type/Proxy.rakudoc:17
--- program ---
sub double() is rw {
    my $storage = 0;
    Proxy.new(
        FETCH => method ()     { $storage * 2    },
        STORE => method ($new) { $storage = $new },
    )
}
my $doubled := double();
$doubled = 4;
say $doubled;       # OUTPUT: «8␤»
--- raku stdout ---
8
--- mutsu stdout (exit 1) ---

--- mutsu stderr ---
Cannot assign to a readonly variable (doubled) or a value
  in block <unit> at tmp/ddh/prog-3426086.raku line 8

