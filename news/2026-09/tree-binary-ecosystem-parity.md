# Tree::Binary 0.0.7 passes under mutsu

Fixed package-qualified self-named core-role headers, defaulted role type
captures in role puns, and role multi-method dispatch. Tree::Binary 0.0.7 now
loads and passes all three upstream test files (51 assertions).
