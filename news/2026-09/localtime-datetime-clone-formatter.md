LocalTime 0.0.3 now passes its complete test suite under mutsu. DateTime cloning
preserves formatter callbacks, applies cloned date overrides through those
formatters, and honors formatter-backed `Str`/`gist` rendering.
