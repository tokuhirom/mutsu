`Blob`/`Buf` coercions, lexical type-object calls, parametric type queries, and
owner-relative method return types now work together in protocol codecs. State
hash initializers materialize deferred maps, and `bless` preserves typed hash
attributes and their ordinary string keys.
