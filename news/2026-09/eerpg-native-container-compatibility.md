EERPG compatibility now covers native-backed scalar subclass constructors, role-punned
BagHash attributes, object-keyed hashes, QuantHash coercions and associative subscripts.
Native Int subclass arithmetic also preserves the subclass payload, allowing EERPG's
amount and rule calculations to run unchanged.
