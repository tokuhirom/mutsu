`PDF::ISO_32000_2` now passes its ecosystem tests. Runtime role composition
enforces required methods, `is also` installs method aliases, and positional or
associative indexing on type objects dispatches to user-defined `AT-POS` or
`AT-KEY` methods.
