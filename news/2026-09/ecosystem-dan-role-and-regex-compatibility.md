Dan’s DataFrame suite now runs cleanly under mutsu. This fixes package-scoped lexical regex subrules in nested role methods, preserves role-punned positional multidimensional indexing when passed as a call argument, keeps mixed-in mutable data tied to its role-owned storage, preserves anonymous state in deferred maps, dispatches Whatever subscripts on typed arrays, and keeps infinite gather pipelines pullable through aggregate parameters.
