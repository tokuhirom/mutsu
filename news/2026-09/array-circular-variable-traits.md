# Array::Circular now passes under mutsu

mutsu now runs Array::Circular 0.0.7's complete test suite. The fix supports callable variable traits with live closure captures, role method bindings, and mutation of native arrays through mixed-in methods.
