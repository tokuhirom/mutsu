# Parenthesized test calls stop at their closing parenthesis

A comma after `is(...)` or another parenthesized test assertion is now treated
as a statement separator rather than an extra assertion argument.
