# ClassHOW exposes `attribute_table`

`ClassHOW.^attribute_table` now returns the class's attributes keyed by their
full names, including the original `Attribute` objects and their metadata.

Pinned by `t/oo/attribute/attribute-table.t`.
