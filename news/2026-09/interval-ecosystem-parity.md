`Interval` 0.0.6 now reaches parity under mutsu. DateTime arithmetic preserves
subclasses and role mixins, parameterized role methods retain their bound body
lexicals, and values assigned directly to tagged `EXPORT` stashes are imported.
