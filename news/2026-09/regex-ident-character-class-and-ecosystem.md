Regex character classes now support `<ident>` in the combined and subtraction
forms used by ECMA262Regex. mutsu also preserves `rx//` source adverbs and
applies `:ignorecase` when executing Perl5 regexes through PCRE2.
