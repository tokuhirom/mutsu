# mutsu — July 2026 implementation status

## Overview

July carried forward the momentum from late June, advancing the dispatch cluster (wrap/dispatching) and the remaining S17 concurrency-infrastructure work in one push. For detailed remaining items and in-progress analysis, see [TODO_roast/BLOCKERS.md](../TODO_roast/BLOCKERS.md).

## 2026-07-22: a big `FatRat` keeps its identity, distinct from a big `Rat` (#5238, closes PLAN §8.8)

A rational whose numerator or denominator overflowed i64 was stored in a single
`BigRat` value variant with no way to tell a (raku) `Rat` from a (raku) `FatRat`:
`(4.5.FatRat)**60` and `(4.5)**60` were indistinguishable, so mutsu reported `Rat`
for the FatRat (`.^name` / `.WHAT` / `~~ FatRat`), gave it the rounded `Rat.Str`
digit budget, and printed `n/d` for `.raku`. A routine typed `--> FatRat` returning
such a value failed its return type-check.

The fix adds a FatRat flag to `ValueRepr::BigRat` (threaded through the nanbox
`BigRatBox`, encode/decode/peek, and serde), read via the new `Value::is_bigfatrat()`.
The `ValueView::BigRat` borrow stays two fields, so the ~150 arithmetic/coercion sites
that must treat both flavours identically (value equality, `+`/`-`/`*`/`/`, numeric
coercion) are untouched; only the ~8 sites where the flavour is observable consult the
flag: the three type-name functions (`value_type_name`, `what_type_name`,
`dispatch_what`/`dispatch_caret_name` — `.^name` uses the last one), the isa table,
`.Str` (big FatRat keeps full precision; big Rat rounds to the `Rational.Str` budget,
keeping the astronomically-small-denominator `0` case via the Num fallback), `.raku`
(`FatRat.new(n, d)` vs the exact decimal), the three `.WHICH` builders, `eqv` (a big
Rat is never eqv to a big FatRat of equal value), and the `.Rat`/`.FatRat` coercions.
`make_big_fat_rat` now preserves the flavour even when the result reduces back into
i64 range. This also closes the `t/rat-complex-stringify-regressions.t` test-8 gap
#5063 left open (a big Rat `.Str` now rounds like raku). Pin: `t/bigfatrat-variant.t`
(24 assertions, all matching raku); `roast/S32-num/rat.t` (869) stays green.

Driven as a dedicated deep slice per the "autostart dedicated sessions" policy. It also
resolved several `numerics.rakudoc` doc-diff lines for free (big-FatRat division
contagion, `RatStr.FatRat`, `(42 + FatRat.new(1,2))/huge`).

## 2026-07-22: `Cool.Version`, the `Version.raku` ctor-form rule, and the §8.2 method-coverage harness

The first §8.2 deliverable landed: `scripts/method-coverage.raku` extracts every documented
`=head2 method/routine` from `Type/*.rakudoc` and probes both interpreters with dynamic
calls across several argument shapes (a hole = "No such method" on every shape where raku
dispatches; `^can` is unusable as the oracle on either side, and mutsu's arity-keyed
dispatch reports an arity miss as "No such method", which single-shape probing would
misread as ~everything missing). Crashing probes are isolated via a PROBE/RES line
protocol and double as §8.3 robustness findings (first catch: `Mu.return` / `Mu.emit`
abort the process).

Its first verified hole is fixed in the same PR: **`Cool.Version` was missing** — now
`"6.c".Version`, `42.Version` etc. build the Version from `self.Str`. That surfaced the
**`Version.raku` ctor-form rule**: Rakudo renders the `v` literal form only when the
canonical string starts with an ASCII digit (`v6.c`, `v6.*`, `v1.2+`) and otherwise the
constructor form (`Version.new('True')`, `Version.new('*.6')`, bare `Version.new` when
empty); mutsu unconditionally printed `v...`. Both the native repr and the container-leaf
`.raku` path now share `version_raku_repr`. Pin: `t/cool-version.t` (16 assertions,
verified green under the reference `raku`).

Also verified fixed & de-listed from PLAN §8.7 (landed earlier via the deferred-Seq
reification + double-slurpy fixes): the Hash::Agnostic dist now passes `t/01-basic.rakutest`
22/22 and `t/02-minimal.rakutest` 11/11.

## 2026-07-22: `.WHO` on builtin values is a Stash; `Stash.raku` is the symbols hash (PLAN §8.6)

`.WHO` on a *builtin value* (`42.WHO`, `@a.WHO`, `"x".WHO`) fell through `dispatch_who` to
a plain empty `Hash`, so `(@a>>.WHO).WHAT` was `(Hash)` where raku gives `(Stash)` (type
objects, user-class instances, and custom types were already correct). The fallback now
resolves the value's type name and returns the same `package_stash_value` stash. And
`Stash.raku` rendered the generic `Stash.new`; Rakudo's Stash is a Hash subclass whose
`.raku` is the symbols hash literal — mutsu now renders `{:Bar(Foo::Bar)}` / `{}` from the
stash's symbols. Verified against the reference `raku`; pin: `t/who-stash.t`. The §8.6
leftover (the Rakudo-internal *content* of builtin stashes like `Array::Element`, and
ClassHOW anon-mixin rendering) is recorded in PLAN.md as cosmetic/not-planned. Also
re-verified the §8.1 first-batch findings: only the autoviv-hole `.List` → `Nil` rendering
remains open (folded into the §8.5/hole-tracking territory).

## 2026-07-22: Iterable-type-object trailing comma and `[Z]` returns a Seq (PLAN §8.15 slice)

Two more §8.15 residues, both raku-verified. A one-element array whose sole element is
an *Iterable type object* now renders with the disambiguating trailing comma
(`[HyperSeq,]`, `[List,]`, `[Map,]`, `[Stash,]`) — Rakudo's List.raku rule is
`istype(elem, Iterable)`, which was implemented for instance elements but not Package
values; note Set/Bag/Mix are NOT Iterable in Rakudo, so `[Set]` stays comma-less. And a
`[Z]` reduction now yields a `Seq` like the plain `Z` infix (`([Z] ...).WHAT` is
`(Seq)`, `.raku` gets the `.Seq` suffix) — the n-ary zip fold in
`eval_reduction_operator_values` wrapped its result in a List. The `[:a].raku` item
recorded from the sweep memory was stale (current raku also renders `[:a]`). Pin:
`t/repr-residues-2.t`. The IO::Spec::Unix repr item stays deferred with a dispatch-trace
finding recorded in §8.15.

## 2026-07-22: `Proc.new.gist` is no longer `-1` (PLAN §8.15 slice)

Proc's registered native `Str`/`gist` rendered the exitcode number (`-1` for a fresh
Proc) — Rakudo's Proc has no stringifier of its own, so the default instance repr
applies. The native `Str`/`gist` arms are removed and Proc now gists/rakus as `Proc.new`
(the numeric `.Numeric`/`.Int`/`.Bool` coercions are untouched). Rendering the full
Rakudo attribute form (`in => IO::Pipe, ..., command => []`, with the run-proc cyclic
backref) stays recorded in §8.15 — it needs Proc's public attributes registered.
Pin: `t/proc-gist-not-exitcode.t` (verified green under the reference `raku`).

## 2026-07-22: `Supply.new` throws X::Supply::New (PLAN §8.15 slice)

Rakudo forbids constructing a Supply directly — live supplies come from a Supplier,
on-demand ones from `Supply.on-demand` / `supply { }` blocks. mutsu silently built a bare
instance. The `.new` dispatch arm now throws the typed `X::Supply::New` with Rakudo's
message; internal builders (`1.Supply`, `Supplier.Supply`, Proc streams, ...) assemble
their instances directly and are unaffected (the now-unreachable `make_supply_instance`
helper was removed). `t/concurrency-basic.t`'s live-supply section was itself non-raku
(`Supply.new` + `Supply.emit`) and is rewritten onto `Supplier.new` + `.Supply` +
`Supplier.emit`. Pin: `t/supply-new-dies.t` (verified green under the reference `raku`).

## 2026-07-22: Channel, the schedulers, and Supplier get their Rakudo reprs (PLAN §8.15 slice)

Three more §8.15 repr items closed: `Channel.new.raku`/`.gist` rendered the bare type name
`Channel` (the Channel value had no repr arm anywhere, so every path fell to its
type-object-looking string value) — now `Channel.new` across all four paths (bare/nested ×
raku/gist). `ThreadPoolScheduler.new` / `CurrentThreadScheduler.new` now show their one
raku-visible attribute (`uncaught_handler => Callable` when unset) via a shared
`scheduler_raku_repr` that the Promise repr composes, and `Supplier.new` shows
`taplist => Supplier::TapList.new` — both as native-instance special arms next to the
existing `X::AdHoc` arm, since these classes have no registered attribute metadata for the
generic walk. Pin: `t/object-type-reprs.t` (verified green under the reference `raku`).

## 2026-07-22: `[ident]` is an array literal unless `ident` is an infix operator

The reduction-metaop parser accepted ANY lowercase-initial bareword as a custom reduction
op, so `[red]` (an enum value, not an infix) parsed as `Reduction { op: "red" }` and
evaluated to `Nil`. Worse, the greedy `R`/`Z`/`X` meta-prefix strip ran before any validity
check, so `[RaceSeq]` became meta `R` + custom op `"aceSeq"` — defeating the existing
uppercase-initial guard that was supposed to keep `[Type]` an array literal.

A bareword now reduces only when it is a `REDUCTION_OPS` word infix (`min`/`max`/`lcm`/...)
or a user-declared infix, and declared infixes are matched as a whole BEFORE meta-prefix
stripping (`infix:<Xyz>` is not mangled into meta `X` + `yz`; `[Rfoo]` still resolves meta
`R` over a declared `foo` post-strip). Found by the §8.15 repr sweep.

Pin: `t/bareword-array-not-reduction.t` (verified green under the reference `raku`).

## 2026-07-22: repr oracle sweep — three nested-leaf residues fixed, seven object-type repr gaps recorded (PLAN §8.15)

A repr-focused run of the oracle diff sweep (`.raku`/`.gist` of ~50 built-in object
constructor expressions, bare and nested `[X]` shapes, mutsu vs the reference `raku`)
surfaced 35 divergent rows collapsing to ~10 root causes. Fixed immediately (the pure
container renderer lacked leaf arms, so these were right bare but wrong nested):

- **Enums lost their qualification inside containers**: `[red].raku` → `[Less]`-style bare
  keys; now `[Color::red, Color::green]` via a shared `enum_raku_repr` (also deduplicates
  the slow-path enum dispatch).
- **A nested Uni decayed to its text**: `["a".NFC].raku` → `[a]`; now
  `[Uni.new(0x0061).NFC]` via a shared `uni_raku_repr`.
- **A nested Version's gist dropped the `v`**: `[v1.2.3].gist` → `[1.2.3]`; now `[v1.2.3]`.

Recorded for follow-up in PLAN.md §8.15: `[ident]` parses as a reduction metaop for ANY
identifier (`say [red].raku` → `Nil`; raku only reduces infix ops), the missing
one-element-Iterable trailing comma (`[HyperSeq,]`), Channel/Supplier/Proc/
ThreadPoolScheduler/IO::Spec::Unix repr gaps (`Proc.new.gist` is `-1` — it delegates to
`.Numeric`), and `Supply.new` not dying.

Pin: `t/nested-leaf-repr-residues.t` (verified green under the reference `raku`).

## 2026-07-22: Promise gets its Rakudo repr; `Match.WHICH` (and every instance `.WHICH`) names its class

The two `.raku` residues the nested-instance sweep left behind (PLAN.md §8.11):

- **`Promise.new.raku` was `Promise(Planned)`** — the gist leaked through because Promise fell
  to the `to_string_value()` catch-alls in every repr path. It now renders the Rakudo attribute
  form `Promise.new(scheduler => ThreadPoolScheduler.new(uncaught_handler => Callable), status
  => PromiseStatus::Planned)` (the scheduler part is faithful: a promise runs on the global
  pool, and `ThreadPoolScheduler.raku` only ever shows its unset `uncaught_handler`). The form
  is a pure function of the status, so one helper (`raku_repr::promise_raku_repr`) feeds the
  pure renderer (nested `[$p].raku`), the native gist/raku fast path, the collection-gist
  helpers, and the slow-path dispatch — `.gist` matches raku too, since Promise has no custom
  gist. `.Str` stays `Promise(Status)` (raku's is a nondeterministic `Promise<addr>`).
- **`$/.WHICH` was `ValueObjAt.new("Str|b")`** — the Match method table's `_` fallback
  delegates unknown methods to the matched string, so `.WHICH` took the *string's value
  identity*. It now falls through to the generic instance arm and yields object identity,
  `ObjAt.new("Match|<id>")`. The same arm had a second bug: it labeled every instance `Any|<id>`
  (via the `'static`-str `value_type_name`), so `A.new.WHICH` said `Any|25` where raku says
  `A|…`; it now uses the instance's class name.

Pins: `t/promise-raku-repr.t`, `t/match-which.t` (both verified green under the reference
`raku`). Closes PLAN.md §8.11 (the residues item).

## 2026-07-22: a cyclic instance renders Rakudo's backref binding — and a mutual cycle no longer overflows the stack

`my $a = Bug.new; $a.myself[0] = $a; $a.raku` printed the terminating-but-wrong
`Bug.new(myself => [Bug()])`; raku prints `(my \Bug_2355566733120 =
Bug.new(myself => [Bug_2355566733120]))`. Worse, a *direct* instance→instance cycle
(`$x.next = $y; $y.next = $x`, a plain `$` attribute) crashed with a stack overflow for both
`.raku` and `.gist`: the `raku_leaf_active` guard the nested-instance fix added only lived in
the container-leaf walker, and `collect_public_raku_attrs` dispatches an instance-valued
attribute's `.raku` directly, bypassing it.

Both recursive paths now funnel through one cycle-guarded dispatch
(`dispatch_raku_leaf`, `src/runtime/methods_raku_dispatch.rs`): a re-entered instance renders
as its backreference name (`Bug_48` — class display name + the stable instance id, where
Rakudo uses the `.WHERE` address) and flags the id; the frame that first entered the instance
(the native renderer in `methods_instance_ops.rs`) consumes the flag and wraps its rendering
in `(my \Bug_48 = ...)`. A mutual cycle binds only the outer re-entered instance
(`(my \P_25 = P.new(next => P.new(next => P_25)))`), and the default gist matches, both as in
Rakudo. The user-facing `Mu.rakuseen` guard (for user-written `.raku` methods) is unchanged.

Pin: `t/cyclic-instance-raku-backref.t` (10 assertions, verified green under the reference
`raku`).

## 2026-07-22: a nested instance renders as `Foo.new(...)` for `.raku`, not `Foo()`

`.raku` of a container holding a user instance (or a built-in object type) rendered the
gist-ish fallback:

```
class Foo { has $.x }
[Foo.new(x => 1)].raku       # was: [Foo()]          now: [Foo.new(x => 1)]
(1.Supply, 2.Supply).raku    # was: (Supply(), ...)  now: (Supply.new, Supply.new)
[Date.new(2020,1,1)].raku    # was: [2020-01-01]     now: [Date.new(2020,1,1)]
```

The instance's *own* `.raku` was already right — only the nested case was wrong, because
container `.raku` recurses through the **pure** renderer
`builtins::methods_0arg::raku_repr::raku_value`, which has no interpreter and so cannot reach
the class registry; its `_ =>` arm fell back to `to_string_value()`.

`.gist` solves the same problem with an interpreter-side recursive walk, but duplicating that
walk for `.raku` would mean re-deriving every container rule the pure renderer owns (bracket
style, `$`-itemization of hash values, single-element trailing commas, adverbial pair keys,
cycle detection). So the fix keeps **one** renderer and rewrites only the *leaves*
(`src/runtime/methods_raku_dispatch.rs`): each dispatch-needing element gets its `.raku`
called through the interpreter, and the text is spliced back into a copy of the tree as a
`raku_raw` marker pair that `raku_value` emits verbatim. Containers are rebuilt preserving
kind/itemization/metadata; a subtree with no dispatch-needing element is shared as-is, and the
native fast path is only bypassed when `container_needs_raku_dispatch` says so
(`vm/vm_native_dispatch.rs`, mirroring the existing collection-gist bypass). Array, List, Seq,
Slip, Hash, Pair, Capture, Set/Bag/Mix (via their typed-key store), itemized containers and
typed `Array[Foo]` are all covered.

Two adjacent gaps fell out of the same verification sweep:

- `Version` had no `raku_value` arm, so a nested `v1.2` rendered as `1.2`.
- A `$`-sigil attribute is a `Scalar` container, so an aggregate it holds is itemized:
  `Foo.new(x => $[1, 2])`, not `Foo.new(x => [1, 2])`. The hash-value itemization rule is now
  shared as `raku_repr::itemize_scalar_repr` and applied to `$` attributes.

Pin: `t/nested-instance-raku.t` (22 assertions, all verified green under the reference `raku`
as well). Closes PLAN.md §8.11.

## 2026-07-22: bound-element immutability — a literal-bound hash/array element is read-only

A hash or array element bound to an immutable literal is now read-only, matching raku:

```
my %h; %h<i> := 137; %h<i> = 666;   # was: silently 666
                                     # now: throws X::AdHoc, %h<i> stays 137
```

The mechanism is a per-variable `__mutsu_ro_index::{var}` side set (parallel to
`__mutsu_bound_index`, in `src/vm/vm_var_index_tracking.rs`, gated by
`elem_index_meta_possible()` so it is zero-cost until some `:=` element bind of a literal
happens). A `:=` bind to an immutable scalar literal (Int/BigInt/Num/Str/Bool/Rat/Complex,
with no named container source — a bind to a mutable variable stays writable-through) marks
the element; the element-assign chokepoints consult it and throw before mutating. Both the
plain path (`exec_index_assign_expr_named_op_inner` and the `try_fast_hash_element_assign`
fast path) and the tied path (a user `Associative` that routes the write through `ASSIGN-KEY`,
where the literal-ness is otherwise lost across the `BIND-KEY` method delegation) are covered:
a tied container yields `X::Assignment::RO` "Cannot modify an immutable Int (137)". Whole-
container reassignment (`%h = (...)`) and `:delete` clear the markers so the element becomes
writable again. Also fixed a related tied-hash bug: a multi-element slice `:delete`
(`%h<d e f>:delete`) now deletes each key and returns the list of values, instead of passing
the whole slice array to `DELETE-KEY` as a single (nonexistent) key.

This clears subtests 4/5/6 of the `Hash::Agnostic` distribution's `t/01-basic.rakutest`
(T-051; dist now 21/22, only `.kv` remains). Pins: `t/bound-element-readonly.t`,
`t/tied-hash-bound-element.t`. See PLAN.md §8.7.

## 2026-07-22: `slip(3,)` — and with it PLAN.md §8.6 is closed

`.raku` on a one-element Slip keeps the trailing comma a one-element list gets: Rakudo emits
`slip(3,)`, mutsu emitted `slip(3)`. Both Slip rendering sites are fixed (the pure
`raku_repr::raku_value` used for nested elements, and the `.raku`/`.perl` 0-arg method
dispatch), and `raku_value` also picked up the `Empty` rendering for an empty Slip that the
method path already had. Pin: `t/slip-raku-repr.t` (8 tests, green under `raku` too).

### §8.6 is closed — what the four fixes were, and what the sweep says now

The section is removed from PLAN.md; the four fixes it tracked, in order, were:

| fix | before → after |
|---|---|
| nodality: coercers are not `is nodal` | `@a>>.Str` on `[(1,2),(3,)]`: `("1 2", "3")` → `[("1", "2"), ("3",)]` |
| the metaobject introspectors are not hypered | `(@a>>.WHAT).raku`: `[(Int, Int), (Int,)]` → `Array` |
| `Any.reverse` / list-only `.rotate` | `"abc".reverse`: `"cba"` → `("abc",).Seq`; `42.rotate`: `Nil` → `X::Method::NotFound` |
| one-element Slip rendering | `slip(3).raku`: `slip(3)` → `slip(3,)` |

plus `.tree` itemization, which the same sweep surfaced (its own entry below).

A full re-run of the sweep that started the section — every name in `is_nodal_list_method`,
through a flat and a nested shape, mutsu vs `raku`, printing only differing rows — now shows
**no remaining nodality divergence**. (The recipe is worth re-deriving rather than looking for:
enumerate the candidate names from the source, run each through two shapes under both
`./target/debug/mutsu -e` and `raku -e`, print only differing rows, then filter the noise. The
script lived in `tmp/nodal-sweep.sh` and is not committed.)

What it still prints is the inherently random `pick`/`roll`, QuantHash key *ordering*
(unordered by definition), rows where both sides error with different wording, and two findings
that are not about hypers at all and are now tracked on their own:

- **a user instance nested in a container renders as `Foo()`** — `[Foo.new].raku` is `[Foo()]`
  where raku gives `[Foo.new(x => 1)]`, and likewise `(1.Supply, 2.Supply).raku` →
  `(Supply(), Supply())`. The instance's *own* `.raku` is correct; only the recursion through
  the pure `raku_value` helper (which cannot reach the class registry) is wrong. New PLAN.md
  §8.11, with the root cause and fix shape written up.
- **`.WHO`/`.HOW` content** — mutsu renders a Stash as a plain `Hash` and its `ClassHOW`
  without the mixin roles. A metamodel-fidelity gap; it inherits the now-vacant PLAN.md §8.6.

## 2026-07-22: `.tree` itemizes every node it descends into

The last structural §8.6 leftover. `.tree` on an Iterable is `$(self.map(*.tree).Seq)` — each
node is an *itemized* Seq — and mutsu returned a plain, un-itemized Array:

```
(1, 2).tree.raku          # was (1, 2)          , now $((1, 2).Seq)
(1, (2, 3)).tree.raku     # was (1, (2, 3))     , now $((1, $((2, 3).Seq)).Seq)
(1..3).tree.raku          # was 1..3            , now $((1, 2, 3).Seq)
%(:a(1)).tree.raku        # was {:a(1)}         , now $((:a(1),).Seq)
"ab".tree                 # "ab" , unchanged: a non-Iterable is its own tree
```

The `.tree(n)` depth forms itemize exactly the `n` levels they tree, which is what makes
`.tree(*)` identical to `.tree` — `roast/S02-lists/tree.t` test 12 asserts precisely that and
was passing only because *both* sides were un-itemized; fixing the 0-arg form alone broke it.
Both paths now share one `tree_to_depth(&Value, depth)` helper in
`src/builtins/methods_0arg/dispatch_core_math.rs`, replacing the duplicated `tree_recursive` /
`Interpreter::tree_depth` pair (the latter is deleted), and it also covers Seq/Slip/Hash/Range
targets that the old Array-only version passed through untouched.

Pin: `t/tree-itemization.t` (15 tests, green under `raku` too). `make test` 2249 files /
21218 tests pass, as do the whitelisted `S02-lists/tree.t` and `S03-metaops/hyper.t`.

## 2026-07-22: `"abc".reverse` is `("abc",).Seq` — `Any.reverse` is `self.list.reverse`, and `.rotate` is list-only

Two more §8.6 leftovers from the `>>.` mutsu-vs-raku sweep.

`.reverse` on a non-Iterable is `Any.reverse` == `self.list.reverse`, so it yields a
one-element Seq. mutsu's *method* form flipped a Str's characters (that is `.flip`) and had
no `.reverse` at all for the other scalars — even though the `reverse(...)` *function* form
already did the right thing, comment and all:

```
"abc".reverse      # was "cba"      , now ("abc",).Seq , same as raku
42.reverse         # was an error   , now (42,).Seq
(a => 1).reverse   # was an error   , now (:a(1),).Seq
"abc".flip         # "cba" , unchanged; this is the character reversal
```

`.rotate` is the opposite case: Rakudo does **not** define it on `Any`, so a non-Iterable
invocant must throw `X::Method::NotFound` — mutsu returned a silent `Nil` (`42.rotate` →
`Nil`), which quietly swallowed a typo. (`.batch`, the third name PLAN.md listed here, turned
out to be correct already.)

`t/vm-basic.t` asserted the old `"abc".reverse eq "cba"`; it now pins raku's behaviour plus a
`.flip` companion. New pin: `t/any-reverse-rotate.t` (17 tests, green under `raku` too). The
25 whitelisted roast files that touch `.reverse`/`.rotate` (`S32-list/reverse.t`,
`S32-array/rotate.t`, `S02-types/array.t`, `S17-supply/*`, …) all still pass.

## 2026-07-22: `@a>>.WHAT` is the Array, not a list of `Int`s — the metaobject introspectors are not hypered

Closes the other half of PLAN.md §8.6, the last divergence the `>>.` sweep found.

Rakudo compiles `.WHAT`/`.WHO`/`.HOW`/`.DEFINITE`/`.WHERE` as special forms rather than
method calls, so a hyper never reaches the elements — the introspector applies to the
*target*:

```
my @a = (1, 2), (3,);
(@a>>.WHAT).raku       # was [(Int, Int), (Int,)]   , now Array , same as raku
(@a>>.DEFINITE).raku   # was (Bool::True, Bool::True) , now Bool::True
(@a>>.WHO)             # now the Array's own package stash
say (Int>>.DEFINITE)   # False
```

A `Seq` target is no longer consumed by `$s>>.WHAT` either. Two cases deliberately keep
hypering, because they are ordinary methods in Rakudo and were already correct:
`.WHICH`/`.WHY`/`.VAR`, and a *quoted* name (`@a>>."WHAT"()` → `[Int, Int]`), which is a
genuine dynamic method call. Implemented as an early bail in `exec_hyper_method_call_op`
(`src/vm/vm_hyper_method_ops.rs`); `WHO`/`HOW`/`DEFINITE` were dropped from
`is_nodal_list_method`, which no longer needs to approximate them at the node level.

Pin: `t/hyper-nodality.t` (now 24 tests, green under `raku` too). `make test` 2236 files /
21070 tests pass. Remaining §8.6 leftovers are unrelated and independent: `Int.reverse` /
`Int.rotate` / `Int.batch` are missing, `.tree` does not itemize, and `Supply`/`Slip`
`.raku` rendering differs cosmetically.

## 2026-07-22: `@a>>.Str` descends again — the hyper nodality set no longer includes the coercers

Follow-up to the playground sweep below, which found this and deferred it because it touches
every `>>.` call site.

Nodality decides *two* things about `>>.method` at once: whether the hyper descends into an
Iterable element, and whether the result is a `List` (node level) or keeps the target's
`Array` container. mutsu's `is_nodal_list_method` listed the **coercers and introspectors**
alongside the genuinely list-shaped routines, so both decisions came out wrong for them:

```
my @a = (1, 2), (3,);
@a>>.Str.raku     # was ("1 2", "3")   -- stringified each node
                  # now [("1", "2"), ("3",)] , same as raku
my @a = 1, 2;
@a>>.Int.WHAT     # was (List) , now (Array) , same as raku
```

`Str`, `gist`, `raku`, `perl`, `so`, `Bool`, `Numeric`, `Int`, `Rat`, `Real`, `defined`,
`item`, `sink`, `cache` and `lazy` were removed from the set; `elems`/`sort`/`reverse`/
`join`/… stay, and a `List` target still yields a `List`. Verified by re-running the full
mutsu-vs-raku sweep over every name in the set (`tmp/nodal-sweep.sh` in the PR): the 15
nodality divergences are gone, and what remains is unrelated (`pick` is random; `Supply`/
`Slip`/QuantHash `.raku` rendering; the `.WHAT`/`.WHO`/`.HOW`/`.DEFINITE` family, which
Rakudo does not hyper at all — now PLAN.md §8.6).

Pin: `t/hyper-nodality.t` (16 tests, green under `raku` too). `make test` 2229 files /
20998 tests and the nine whitelisted hyper roast files (`S03-metaops/hyper.t`,
`S07-hyperrace/*`, `S32-list/deepmap.t`, …) all pass.

## 2026-07-22: the GitHub Pages demo became a real playground — a stateful WASM REPL, and the three bugs its examples found

The Pages site was a one-shot evaluator: a textarea, a Run button, and eight canned
snippets. Every run built a fresh `Interpreter`, so nothing you typed survived — you could
not define a class and then poke at it. It is now a **playground**: an editor *and* a
REPL that share **one** interpreter session.

- **`Repl` is exposed to WASM** (`src/lib.rs`). It owns a long-lived `Interpreter` plus the
  accumulated-line buffer, and offers `evalLine` (REPL), `evalBlock` (the editor's Run),
  `reset`, and `isPending`. Both return `{"output", "incomplete"}` as JSON. The stateless
  `evaluate()` stays for compatibility.
- **The REPL core is no longer CLI-only.** `is_incomplete` / `process_line` moved out of the
  `native`-gated `src/repl.rs` into **`src/repl_core.rs`**, so the browser REPL and the
  rustyline CLI REPL run *the same* line-accumulation, bracket-continuation, and
  last-value-gist logic instead of a re-implementation. `src/repl.rs` is now just the
  rustyline loop.
- **The page**: syntax highlighting (a small Raku tokenizer over a transparent textarea),
  `#code=<base64url>` permalinks, 18 examples in four groups each with a one-line
  explanation, `↑`/`↓` history, `*` continuation prompt, and a session reset.
  `wasm-demo/e2e.test.mjs` grew to **31 assertions** covering session persistence,
  editor→REPL sharing, continuation, permalink round-trip (non-ASCII included), and that
  the highlighter escapes rather than injects HTML.

Every example was diffed against `raku` before shipping. Three of them failed, and each was
a real interpreter bug, not a demo problem:

- **`Bag`/`Set`/`Mix` `.sort(&by)` returned an unordered result** — and a *different* one on
  each run. The VM's native-sort gate (`src/vm/vm_native_sort.rs`) listed Array/Seq/Hash/Range
  but not the setty types, so they fell back to the tree-walking `InterpCaller`, which cannot
  invoke a compiled `WhateverCode` mapper: every sort key came back `Nil`, all elements
  compared equal, and the result was the container's hash-iteration order. `sort_value_generic`
  already decomposes setty values into `key => weight` pairs, so admitting them to the gate is
  the whole fix — and it removes a slow-path fallback rather than adding one.
  Pin: `t/sort-setty-whatever.t` (11 tests, green under `raku` too).
- **A `my regex NAME { ... }` swallowed the next line's `if`.** `stmt_ends_with_block`
  (`src/parser/stmt/modifier.rs`) did not know about `TokenDecl`/`RuleDecl`, so a block-form
  rule declaration was treated as still open for a postfix statement modifier. The
  declaration was re-parented into the conditional's then-branch — meaning the rule was not
  declared yet when the condition's own regex referenced `<NAME>`, and `<NAME>` fell back to
  a (nonexistent) `.NAME` method call on `Match`. It "worked" only when the condition was
  constantly true. Pin: `t/regex-decl-stmt-terminator.t`.
- **The REPL replayed the previous line's value after a declaration.** `run` reports a line's
  value as "top of stack, else the last topic set", and both survive between REPL lines, so
  `sub f { 1 }` echoed whatever the line before it produced. `process_line` now clears both
  first. This fixes the CLI REPL as well.

One divergence found by the same sweep was **not** fixed here because its blast radius is
every `>>.` call site: mutsu's nodal-method set is too broad, so `@a>>.Str` yields a `List`
where raku yields an `Array`. Recorded as PLAN.md §8.6 with the full list of affected names.

## 2026-07-19: ANALYSIS.md rev10 — resolved-item archive

ANALYSIS.md was re-verified against HEAD (#4811) after **259 commits since rev9**
(2026-07-15). The document keeps only *open* issues; the items that closed between rev9
and rev10 are archived here.

- **The `integration/` frontier is fully cleared (0 remaining as of 2026-07-17).** rev9
  called `integration/` (real Raku programs) "the remaining frontier"; it is now entirely
  whitelisted, and roast is no longer the productive axis (BLOCKERS.md: "No cluster
  remains"). The roast whitelist rose **1391/1463 → 1433/1464 (95.1% → 97.9%)**.
- **Deep-recursion process abort — fixed (was rev9 roadmap #1, impact "large").** The
  interpreter now runs on a dedicated **256 MB-stack thread** (`main.rs:99`), and the three
  pure-recursion integration tests (`99problems-41-to-50.t`, `99problems-51-to-60.t`,
  `man-or-boy.t`) pass. The lone residual abort,
  `deep-recursion-initing-native-array.t`, is a **memory-bound** shaped-native typed-array
  coercion issue (BLOCKERS S02 array-shapes T36-38), not a Rust-stack-recursion gap, so it
  was reclassified out of the recursion item.
- **RakuAST model layer added as a new section (§1.7).** The ~37-slice Phase 4–5 campaign
  (`src/rakuast/`, ~3200 lines) was absent from rev9; it is now documented as the largest
  active subsystem — `Q[…].AST` (read) + `EVAL($tree)` lowering back through the existing
  compiler (write), no second engine.

What was re-measured (open, carried into rev10, not resolved): hygiene metrics drifted
further and faster than rev9's slope — files >500 lines 210→239 (>1000: 57→62),
`runtime/mod.rs` 2309→2470, `unwrap/expect/panic!` 1759→1789, `#[allow(` 165→170,
`.clone()` 8947→9056, and `gc_contents_mut` sites ~53/21 files → ~59/25 (trending up).
These stay in the ANALYSIS.md body (§5/§6/§2.1) as open debt.

## 2026-07-19: RakuAST Phase 5 (EVAL) — 37 slices, a milestone

The RakuAST model layer (ADR-0011) reached a natural checkpoint: **37 EVAL slices landed** (PRs
#4736–#4804), building out `lower(RakuAstNode) -> Stmt/Expr` — the *write* direction that backs
`EVAL($rakuast)`. Together with the earlier read-direction work, the model layer now round-trips a
large fraction of the language: `Q[…].AST` produces a RakuAST tree, and `EVAL` of that tree runs it
back through the **existing** compiler (no second execution engine).

- **Both directions, dual-oracle.** 35 of the 37 slices cover `.AST` (read) *and* `EVAL` (write); the
  read-side gist is byte-identical to Rakudo's where applicable. Every slice is pinned by a test in
  `t/rakuast-eval-*.t` / `t/rakuast-*.t` that passes under **both** mutsu and raku. Two slices (hash
  literals `{a => 1}`, and the earlier read-only Phase-2 cluster) are read-only because raku's own
  EVAL of the produced node diverges from a naïve round-trip.
- **What runs under EVAL.** Real programs execute through `EVAL(Q[…].AST)`: recursion
  (`sub fact($n) { … fact($n-1) }`), higher-order list processing
  (`(1..5).map({ $_ * $_ }).grep({ $_ > 5 })`), exception trapping (`try { die "x" }` → `$!`),
  generators (`gather { … take … }`), and first-class closures (bare blocks, `-> $x { }`, `sub ($a,
  $b) { }`, `$f(…)`). Covered clusters: literals (incl. string interpolation, array literals, pairs,
  `*`, bareword types), all common operators + the ternary, the full control-flow set (`if`/`elsif`/
  `unless`/`while`/`until`/`repeat`/`for`/C-style `loop`/`given`-`when`/`do`/`try`/`gather`), sub
  declarations with typed/default/named/slurpy parameters, and control-flow calls
  (`return`/`last`/`next`/`die`/`fail`/`take`).
- **Two engine bugs fixed along the way.** (1) A latent `eval_block_value` soundness bug — a non-tail
  `given`/`when`/`default` left its block value on the stack and shadowed the block's real tail value
  (RakuAST-independent; the mainline discarded its stack, so it only bit value-blocks). (2) A
  `FatArrow` lowering regression from the named-infix `Ident` fallback (`x => 5` call arguments).
- **Where it stops, and why.** The remaining constructs are blocked by *representation mismatches*,
  not effort: desugar divergences (placeholder blocks `{ $^a }`, `with`/`without`, list assignment,
  `constant`), genuinely indistinguishable forms (`%h<a>` vs `%h{"a"}`), raku's own EVAL divergence
  (hash literals), and larger machinery (CATCH blocks, `WhateverCode`, code-block interpolation,
  regexes). The at-a-glance coverage/boundary summary lives in
  [docs/adr/0011-rakuast-model-layer-and-phasing.md](../docs/adr/0011-rakuast-model-layer-and-phasing.md)
  (Phase 5 "Status summary"); PLAN.md tracks the next candidates.

## 2026-07-19: `mzef` binary shim + in-repo zef vendoring (PLAN §1 B2)

The last B2 milestone before distribution: mutsu now ships its own `mzef` package-manager command,
and the real zef it drives lives **inside this repo** instead of an out-of-tree working copy.

- **Vendored zef** at `vendor/zef/` — upstream [Zef](https://github.com/ugexe/zef) 1.1.3
  (`ugexe/zef@0aa54f53`, 2026-05-24), **Artistic-2.0**. Only the runtime files are vendored
  (`bin/`, `lib/`, `resources/config.json`) plus `LICENSE`/`META6.json`/`README.md` for attribution;
  upstream's `t/`/`xt/`/`.github`/precomp are excluded (464K, 32 rakumod). Artistic-2.0 permits
  verbatim redistribution provided the license and attribution are preserved, which the vendored
  `LICENSE`/`META6.json` do. Provenance and the re-vendor recipe: `vendor/README.md`.
- **`mzef` shim** — a second cargo binary (`src/bin/mzef.rs`) that re-execs the interpreter:
  `mzef <args>` → `mutsu -I vendor/zef/lib vendor/zef/bin/zef <args>`. It resolves the vendored zef
  tree relative to its own executable (installed layouts `../share/mutsu/zef` / `../lib/mutsu/zef`
  first, then the dev `../../vendor/zef`, or the `$MZEF_ZEF_HOME` override) and the sibling `mutsu`
  binary (`$MZEF_MUTSU_BIN` override), then hands off via `exec` so signals/exit codes/stdio pass
  through transparently. zef config resolves from the vendored `resources/config.json` through
  `%?RESOURCES<config.json>`; installs land in mutsu's default `$HOME` site repo.
- **E2E verified** on a fresh `$HOME` (release): `mzef info JSON::OptIn` returns full fez metadata,
  and `mzef install JSON::OptIn` (tests ON) runs `Testing [OK] → Installing`, after which
  `use JSON::OptIn` loads from the fresh site repo. Pin: `tests/mzef_shim.rs` (offline `--version`
  resolution across the override paths).
- **B3 leftover**: the release artifact must place `vendor/zef` at `<prefix>/share/mutsu/zef`
  alongside `bin/mutsu` and `bin/mzef` — the shim already probes that path first.

### Release pipeline: mise-installable binaries with `mutsu` + `mzef` (B3)

Following the shim, wired the distribution so `mise use -g github:tokuhirom/mutsu` installs both
commands. While doing so, found that **every Release run since v0.3.1 had failed** — the macOS
ARM64 leg could not build vendored libffi (upstream Clang-17+ Mach-O CFI issue), and with the
default `fail-fast` that cancelled the Linux legs and skipped the publish job. So no GitHub Release
had ever carried a binary. Reworked `release.yml`:

- Builds **both** `mutsu` and `mzef` per target and packages a self-contained tarball with
  `bin/mutsu`, `bin/mzef`, and `share/mutsu/zef/...` at the **archive root** (no wrapping version
  dir), so mise's github-backend autodetection finds `bin/` with zero config and `mzef` resolves
  the bundled zef via `../share/mutsu/zef`.
- `fail-fast: false`; **Linux x64/arm64 required, macOS `continue-on-error`** (best-effort) so a
  broken macOS toolchain no longer blocks the Linux artifacts or the publish job.
- Added a `workflow_dispatch` trigger that runs the build+package matrix (incl. macOS) without
  cutting a tag; the publish job is gated on `github.ref_type == 'tag'`.
- README gained an "Install" section (`mise use -g github:tokuhirom/mutsu`).

Verified locally that the exact packaging tar reproduces a layout from which `mzef --version` and
`mutsu -e` both run with no env var. **Confirmed live on the v0.8.0 tag** (the first release ever to
publish a binary): the Release run published the `linux-x64`/`linux-arm64`/`macos-x64` tarballs +
SHA256SUMS (macos-arm64 failed on libffi, tolerated), and
`mise exec github:tokuhirom/mutsu@0.8.0` downloaded, checksum/SLSA-verified, and extracted it, then
ran **both** `mutsu` and `mzef` from `<install>/bin/` with the bundled zef resolved at
`<install>/share/mutsu/zef` — no env var. Remaining: the macOS **arm64** libffi build (macos-x64
builds fine; needs a libffi/libffi-sys bump or a system-libffi build — deferred, unverifiable from a
Linux dev box).

### Container image on GHCR (B3)

Added a `Dockerfile` and `docker.yml` so `docker run --rm -it ghcr.io/tokuhirom/mutsu` gives an
instant Raku REPL with the bundled `mzef` package manager. The Dockerfile is a clean two-stage
build — a `rust:1.93-bookworm` **builder** compiles `mutsu`+`mzef`, and the shipped
`debian:bookworm-slim` **runtime** stage carries only the two binaries, the bundled zef tree at
`/usr/local/share/mutsu/zef`, and zef's shell-out tools (curl/git/tar/unzip) + libpcre2 — no Rust
toolchain or source in the final image. `mzef` resolves the bundled zef via the same
`../share/mutsu/zef` exe-relative rule (with `MZEF_ZEF_HOME` pinned as a backstop). `.dockerignore`
uses ignore-all + re-include (src/Cargo/vendor) to keep the 45G `target/` out of the build context.
`docker.yml` publishes a multi-arch image (amd64+arm64 built on native per-arch runners, combined via
`buildx imagetools` manifest) to GHCR on `v*` tags (`:X.Y.Z`/`:latest`) and main (`:main`).

## 2026-07-18: vendored `roast` bumped `7f2d7508` → `b2cbe8a4` (2026-06-12, 34 commits ahead)

Re-vendored the roast suite to upstream HEAD via `scripts/update-vendor.sh roast b2cbe8a4`. The
update touched 33 files (1 new: `S02-types/quanthash.t`, 32 modified). Following plan A, the four
feature clusters that upstream's new subtests would otherwise regress were implemented *first*, so
the bump landed with **no whitelist edits** — all 28 whitelisted-and-changed files still pass:

- **① List-of-overlapping-needles `index`** (#4710, merged earlier) — the sub form
  `index($s, @needles)` returns the earliest match across all needles.
- **② Immutable QuantHash RO errors** — `:delete` / `DELETE-KEY` / `ASSIGN-KEY` on an immutable
  `Set`/`Bag`/`Mix` now throw `X::Assignment::RO` (was silently allowed; `Bag`/`Mix` `ASSIGN-KEY`
  also had a bogus hardcoded `"Int"`/`"1"` in the message). A *behavior change* vs the old vendored
  files, so it shipped **inside the bump PR**, not standalone. Pin `t/quanthash-immutable-ro.t`.
- **③ 6.e sprintf flag semantics** — 6.e (`use v6.e.PREVIEW`) changes several flag combinations:
  sign printed before the radix prefix on `#` negatives (o, x/X), `+`/space flags no longer applied
  to `%b`/`%B`, `#` forces a trailing decimal point on `%e`/`%f` at precision 0, `0` zero-pads
  strings even with a precision, and `0` beats `-` on float specs. Each change is `v6e_active()`-gated
  (reads `current_language_version()`), so 6.d output is byte-identical. Also a behavior change vs the
  old files → shipped inside the bump PR. Pin `t/sprintf-6e.t`.
- **④ `.assuming` priming signature gist** (#4722, merged earlier) — a primed sub's
  `.signature.gist` renders bound named params as defaults.

The new `S02-types/quanthash.t` remains non-whitelisted: it needs `.^parameterize` on Set/Bag/Mix
(parameterized QuantHash types) which mutsu lacks — recorded in `TODO_roast/BLOCKERS.md`.

## 2026-07-18: `S02-types/array-shapes.t` slice — Rat shape dims (#4730) and element-wise `Z=` (#4733); shaped-native `Z=` deferred

`array-shapes.t` is a full oracle under fudge (raku passes 43/43; its unfudged run only aborts at
the `#?rakudo skip` "partially dimensioned views NYI" block, which is skipped for rakudo). Two of
its three blockers landed:

- **T43 — Rat shape dimensions** (#4730): `my Int @a[2.5]` now yields a 2-element shaped array
  (`[(Int) (Int)]`) instead of throwing `X::TypeCheck::Assignment`. `shaped_dims_from_new_args`
  handled `Int`/`Num`/`BigInt`/enum shape dims but had no `Rat` arm, so a rational literal fell
  through and the `shape => 2.5` Pair leaked as element data. Added a `Rat` arm (truncate toward Int,
  `> 0` guard) to both the single-value extraction and the per-dimension loop; negative/zero-truncating
  rationals still raise `X::IllegalDimensionInShape`, matching raku. Pin `t/shaped-array-rat-shape.t`.
- **General `Z=` — element-wise zip-metaop assignment** (#4733): `@a Z= rhs` now assigns element-wise
  (`@a[i] = rhs[i]` for the zipped prefix, trailing `@a` elements kept), matching raku, instead of
  replacing `@a` with the truncated zip result. Compiled to `__mutsu_zip_assign`; the parser rewrites
  `@a Z= rhs` (assign_stmt) and `my @a Z= rhs` (my_decl_assign — checked *before* the custom `op=`
  operator, else the leading `Z` is taken as a user operator). Only `Z=` (op `=`) is rewritten;
  `Z=>` / `Z+=` keep their MetaOp path. Untyped shaped `my @a[2;3] Z= 0..5` works. Pin
  `t/zip-metaassign.t`.

**Deferred — native-typed shaped `Z=` and the coercion perf trap.** The roast file's tests 36-38 use
`my int @a[2;3] Z= 0..5` (a *native-typed* shaped array), which still throws
`X::TypeCheck::Assignment: expected int but got Array` on the result assignment: the typed-array
element coercion (`coerce_typed_array_elements`) recurses into shaped rows only when the top-level
`ArrayKind == Shaped`, but the value arrives with its top kind normalized to `Array` (each row keeps
the `Shaped` mark). **Two fixes were tried and both reverted** — (a) a per-item `is_shaped_array(item)`
in the recursion, and (b) forcing `kind == Shaped` via a top-level shape re-mark — because **both
caused a ~150× perf regression** on `roast/integration/deep-recursion-initing-native-array.t` (a
100M-element `my int @a[N;N]`): keeping the array on the `kind == Shaped` path slows the whole
native-array pipeline. `vm_var_assign_typed.rs` was left identical to main. The correct fix must
recurse into shaped rows *without* keeping the array on the slow `kind == Shaped` path; it is deferred
and recorded in `TODO_roast/BLOCKERS.md`. (Measurement note for the next attempt: element index
assignment `@a[$r;$c] = X` does **not** call `coerce_typed_container_assignment` — a `for` loop
benchmark misleadingly does, via its whole-array sync, so measure the real recursion pattern.)

The file's third blocker, **T31** (`for @a.pairs -> $p { $p.value = X }` on a multi-dim shaped array,
tuple pair key vs the 1-D `key.parse::<usize>()` writeback), is also still open. `array-shapes.t`
remains non-whitelisted (aborts at test 36); fixing the shaped-native coercion + T31 whitelists it.

## 2026-07-18: `APPENDICES/A02-some-day-maybe/multi-no-match.t` fully whitelisted — X::Multi::NoMatch for wrong-argument builtins

The last ★ roast file (raku full-pass, mutsu was 3/16) is now whitelisted. It asserts that a set
of builtins throw `X::Multi::NoMatch` (a *sane* error), rather than silently succeeding, hanging,
or throwing a vaguer type, when called with an argument shape that matches no candidate. There was
no single lever — each was an independent argument-validation gap fixed at its native handler:

- **`.splice`** — the start/elems positions take `Int` (plus `Whatever`/`Callable`); a `Num`,
  `Str`, or `Array` there now throws instead of being coerced. Fixed on both the lvalue path
  (`methods_mut_dispatch`) and the rvalue-array path (`methods_call_dispatch`, `array_mutate_copy`).
- **`Pair.new`** — only `($key, $value)` (two positionals) or `(:$key, :$value)` (both named) are
  valid; one/three positionals or a stray named arg (`:42a`) throw instead of filling in `Nil`.
- **`Junction.new`** — requires a type (positional `Str` or named `:type`) plus exactly one values
  positional; a lone positional, extra positionals, or no args throw instead of defaulting to `any()`.
- **`Int.new`** — accepts at most one positional; two or more now throw.
- **`Proc::Async.new`** — requires at least one positional command element; no command throws.
- **`Date.new`** — a type-object year/month/day (`Date.new(Int, 1, 1)`) dies instead of coercing to 0.
- **`.match`** — an *undefined* matcher (`Nil`, or a bare type object) throws instead of hanging or
  returning `Nil`; a *defined* non-Regex/non-Str matcher still yields `Nil`.
- **`.subst`** — no matcher at all throws (a bare pattern with no replacement is still valid).
- **`.printf`** — a bare `$handle.printf` with no format throws (native stdout/stderr and user-IO paths).
- **`Lock.protect` / `Lock::Async.protect`** — calling on the type object or with a non-Callable
  arg (`.protect: %()`) now throws `X::Multi::NoMatch` rather than `X::Method::NotFound`/`X::AdHoc`.

Pin: `t/multi-no-match-builtins.t`. `make_multi_no_match_error` was widened from `pub(super)` to
`pub(crate)` so the VM fast-path handlers can reuse it.

## 2026-07-18: `S02-types/generics.t` fully whitelisted — per-composition generic class naming + typed-attribute element checks

Following the coercion-type-terms landing (#4681, subtests 1-2), the remaining four subtests of
this deep 6.e generics test now pass, so the file is whitelisted. Three general-purpose fixes:

- **Parser: `is Type[params]` / `is Qualified::Name` container traits kept whole.** `has @.a is
  Array[Int]` parsed as `is_type: "Array"` with the trailing `[Int]` misparsed as a *separate*
  statement, so the parameterization was silently dropped (and `Foo.new(a => <bad>)` never
  type-checked). The `has`-attribute trait parser now consumes `::`-qualified segments and a
  balanced `[...]` parameterization into the type name. Pin: `t/generics-nominalizable-class.t`.
- **Per-composition parameterized class naming.** A class declared inside a *parametric* role body
  (`role R[::T] { my package G { class A is Array[T] {} } }`) becomes parametric over the role's
  type args: composing `R[Int]` renames the nested class to `R::G::A[Int]` (and `R[Str]` to
  `R::G::A[Str]`), so `.^name` and the coercion-term forms (`G::A:D`, `G::A()`) report the
  instantiation-pinned name. Each composition renames the freshly-declared class and records an
  alias so a bare `G::A` reference from a composed method still resolves.
- **Typed-attribute element checking through a generic Array subclass.** A role attribute's `is
  Type` container trait (`has @.a is G::A`) is now carried onto the consuming class, and
  `coerce_value_to_is_type` resolves a subclass of `Array[T]` (directly or via the composition
  alias) to its concrete element type — so a wrong-typed `.new(a => <bad>)` throws
  `X::TypeCheck::Assignment`, exactly like a literal `is Array[Int]`.

## 2026-07-17: mzef installs AND loads Test::META's whole dependency chain (#4658 + #4661–#4665)

Six merged PRs took mzef from "the ADR-0010 branch is red in CI" to "`zef install --/test
Test::META` installs all 13 dists and every one of them `use`s" — the install pipeline AND the
load chain both work end-to-end. The Test phase now runs too (16 concurrent suites), which is the
new frontier: 13/16 suites fail, dominated by an environmental `use-ok` failure in zef's staged
test subprocesses (details in [docs/mzef-install-pipeline.md](../docs/mzef-install-pipeline.md)).

- **#4658 — the ADR-0010 gates stay.** Lineage scoping isolates *siblings*; the #4650
  `thread_redeclared_vars` gates handle *parent-child* shadowing — orthogonal, both needed. The
  first draft removed the gates and a child's `my` re-declaration (`if INIFile.parse(...) ->
  $parsed`, desugared to `my $parsed = ...`) wrote through the chain and replaced the parent's
  Channel with a Match (`advent2013-day14.t`). Restoring the gates ALSO fixed the concurrent
  extract-matcher rejection — same defect class corrupting zef workers' staged paths — which is
  what completed the install pipeline. Eager store-side `declare` at `SetVarDynamic` was rejected:
  the own-lineage entry would outlive its block and the dirty sync would resurrect the dead shadow.
- **#4661 — two module-load blockers.** `-> Mu \type { ... }` (typed AND sigilless pointy param)
  failed to parse — JSON::Unmarshal writes every `subset ... where` lambda this way; and a role
  composing a sibling role by short name inside a module (`role Derived does Base` under
  `unit module M`) failed with "Unknown role".
- **#4662 — `use` dist selectors were parsed and DISCARDED**, and the inst# scan loaded whichever
  same-named dist `read_dir` yielded first (two JSON::Class dists exist). Resolution now filters
  by `:auth`/`:ver`/`:api` (Version-smartmatched, `0.0.14+` = at-least) and picks the highest
  version. Plus: `role Name:ver<...>:auth<...>[SIGNATURE]` — adverbs before the parametric
  signature — now parses.
- **#4663 — the JSON::Name trait shape, three defects.** A custom `trait_mod:<is>` that does
  `$a does NamedAttribute; $a.json-name = $v` lost all its work (ephemeral Attribute object);
  the short role name failed to resolve at the runtime `does` and silently REBOUND `$a` to a
  boolean; and two modules' exported trait multis collided on one import key (last `use` won).
- **#4664 — class-body trait multis reach nested classes** (META6 declares its `trait_mod:<is>`
  in its own body and uses it inside `class Support`); and a named `@` param no longer matches a
  scalar value (it out-dispatched the correct scalar candidate and died in the destructure).
- **#4665 — a regex-`where` subset accepts matching return values.** URI's `subset Host of Str
  where /.../ ` passed `~~` but failed every `--> Host` return: the return check's private
  predicate evaluator only understood callables. It now delegates to the `~~` matcher.

**Verification trap for the next session:** `use Test::META; say "LOADED"` proves nothing —
`use_module` silently succeeds for any missing `Test::*` module (compat branch). Verify with an
exported symbol (`::("&meta6-ok")`).
## 2026-07-17: `OUTER::` names ONE scope, not every outer scope — `my-6c.t` 112/112

`roast/6.c/S04-declarations/my-6c.t` sat at 111/112 on test 57:

```raku
{
    ok EVAL('not OUTER::<$x>.defined'), 'OUTER::<$x>';   # mutsu: False.  raku: True
    my $x;
}
```

BLOCKERS.md recorded this as needing **lexical hoisting** — the claim being that
`OUTER::<$x>` had to see the enclosing block's `my $x` declared *after* the EVAL as an
undefined container. That diagnosis was wrong, and the hoisting it asked for would not
have fixed the test. Measuring `OUTER::` against rakudo first (rather than implementing
the recorded root cause) showed three real defects instead — the same lesson as
`advent2013-day18.t` the same day.

`packages.rakudoc` defines the pair:

```
OUTER       Symbols in the next outer lexical scope
OUTERS      Symbols in any outer lexical scope
```

mutsu implemented `OUTER` as `OUTERS`. `exec_get_pseudo_stash_op` answered `OUTER::<$x>`
by building a hash of the **whole flat `env`**, which has no notion of scope levels at
all: one scope out happened to be right, and every scope beyond it was wrong in a way
that returned a *defined, unrelated* value. Test 57 fell out of exactly that — the
file-scope `my $x = 0` from two scopes away answered a lookup that should have found
nothing. The three defects:

1. **`OUTER::` cascaded.** `my $y = 7; { { OUTER::<$y> } }` gave 7; raku gives Nil. The
   fix is to resolve the name in the target scope *only*. This is a lexical, compile-time
   question, and the compiler already tracks it: `local_scopes` records which names each
   scope declares, so `emit_outer_var_access` now settles the lookup outright and emits a
   Nil constant when the target scope does not declare the name. Nothing was added to the
   VM — an earlier attempt to enforce this in `get_outer_var` had to be reverted, because
   `outer_scope_locals` is only reset by `run()`, not by the fast call paths, so inside a
   stored closure it still holds the *caller's* blocks: dynamic, not lexical.
2. **The two spellings had diverged.** `$OUTER::x` compiled to `GetOuterVar` (depth-aware);
   `OUTER::<$x>` never reached it, because the compiler special-cased only `CALLER::<$x>`
   and had no `parse_outer_prefix` sibling. Both spellings now share one path, so
   `OUTER::OUTER::<$y>` works (it returned Nil before) and `OUTER::<@a>` / `OUTER::<%h>`
   resolve (only `$` is stripped from a stash key; `@`/`%`/`&` stay in the env key).
3. **`OUTERS::` was unimplemented** (always Nil). It is now the innermost *enclosing*
   scope that declares the name — excluding the current scope, so
   `my $y = 7; { my $y = 8; $OUTERS::y }` is 7 — which is just `OUTER` at a depth
   `local_scopes` can find, so it reuses the same emit path.

Two supporting pieces were needed to make "does the target scope declare this name?"
answerable:

- **Parameters are declarations of their routine's scope** (`declare_param`):
  `sub f($p) { { $OUTER::p } }` sees 42. Plain `alloc_local` deliberately still does not
  record, since it is also how a *free* variable gets a slot, and a free variable merely
  mentioned in a body is not declared by it (raku:
  `my $y = 7; sub f { say $y; { say $OUTER::y } }` prints 7 then Nil).
- **EVAL'd units are compiled behind an empty wrapper scope** (`mark_as_eval_unit`).
  rakudo does not splice an EVAL'd unit into the invoking scope; it runs it behind
  wrapper scopes holding no user lexicals, so `EVAL 'OUTER::.keys'` is empty and
  `my $y = 7; { EVAL(q{OUTER::<$y>}) }` is Nil even with `$y` right there. This is the
  lexical-axis twin of `push_eval_caller_frames`, which already modelled the same layout
  on the caller axis for `CALLER::`. Plain lookups are unaffected — EVAL still shares the
  runtime env with its caller, so `my $z = 3; { EVAL(q{$z}) }` is still 3. **This, not
  hoisting, is what test 57 needed**: the enclosing `my $x` is irrelevant, because an
  EVAL'd unit cannot see the invoking scope through `OUTER::` at all.

The topic is exempt from the compile-time check: every block and routine scope declares
its own `$_` (a block's implicitly as `$_ is raw = OUTER::<$_>`, a routine's as a fresh
undefined one), so the question is only ever what that `$_` holds. Without the exemption
`$OUTER::_` compiled to Nil and regressed `t/outer-topic.t`.

Pin: `t/outer-pseudo-package.t` (23 assertions, every expectation checked against rakudo
first — the file passes under `raku` as well as mutsu). Whitelist 1430 → 1431.

## 2026-07-17: regex code assertions now run exactly once, on the real interpreter — `advent2013-day18.t` 10/10 (ADR-0009)

`roast/integration/advent2013-day18.t` was the last `integration/` ★ blocker, stuck at
9/10 on test 10 (`is-deeply @dups, ["a♥", "j♥"]`). The root cause recorded for it in
BLOCKERS.md — "code assertions over-run during backtracking" — turned out to be wrong
twice over. Measurement found **three** independent causes, none of them backtracking, and
all three are now fixed. Design and the corrected analysis:
[ADR-0009](../docs/adr/0009-regex-code-assertion-execution-model.md).

The tell was that a `<?{ … }>` ran **4 times on a match that cannot backtrack at all**:

```raku
grammar C { token TOP { (\w) <?{ %n<C>++; True }> } }
C.parse("a");   # mutsu: 4.  raku: 1
```

| # | Cause | Fix |
|---|---|---|
| A | LTM measured each candidate by running the real matcher over the whole pattern — **twice** (`regex_match_len_at_start` + `declarative_prefix_match_len`) | measure under `LTM_DECLARATIVE_MODE`, which never executes a code atom (#4651) |
| A2 | `longest_complete_prefix_end` re-matched the pattern against **every prefix** of the input to report how far a failed `.parse` got, executing the assertions each time | run the probe under `CODE_ATOMS_INERT` (#4656) |
| B | the assertion was decided in a **scratch interpreter** and replayed in the parent only on the **winning path** — so a failing parse had no side effects at all | run it inline on the real interpreter |

A2 is where the "backtracking" diagnosis died: day18's card assertion ran **15 times on
its 14-char input and 31 on its 31-char one** — input length + 1, one run per prefix. The
suggestive `15 = 2⁴−1` was a coincidence. A backtrace over `token TOP { <item>**3 }` split
its 13 runs into **3 from the real match — exactly raku's count — and 10 from the probe**.
mutsu's ratchet was never at fault.

`roast/S05-grammar/protoregex.t` then corrected A's premise: a code **assertion does not**
terminate the LTM declarative prefix (`a <?{1}> .+` has prefix `a .+`), only a plain
`{ }` block does (`a {} .+` has prefix `a`). mutsu's existing `has_code_block_in_prefix`,
counting only `is_assertion: false`, had that right all along.

B's `&self` → `&mut self` flip cost far less than the ADR feared: 47 methods, and the
borrow checker barely fought back (nothing in the matcher returns a reference borrowed
from `self`). Only two closures needed restructuring.

Results: assertion runs **4 → 1, matching raku exactly** for every shape measured; day18
**10/10**; whitelist 1429 → 1430. A also made grammar parsing faster for **every** grammar,
assertion-bearing or not, by removing one of two full trial matches per subrule
resolution — release, interleaved A/B, median of 9: **2.15 s → 1.61 s (×1.34)**. ADR-0007's
"still-unexplained runtime regex re-parse path (~4% in-profile)" is very likely largely
this.

Pins: `t/regex-ltm-declarative-prefix.t`, `t/grammar-parse-failure-probe.t`,
`t/grammar-assertion-side-effects.t` — all three pass under raku as well as mutsu, and
fail on the tree before their fix.

Not closed: a **file-scope** variable is still invisible to a code assertion (it lives in
the top VM frame's locals and never reaches env). Unchanged by any of this, and why day18
works — its `our @dups` sits in a block. That belongs to the `env_dirty` dual-store debt.

## 2026-07-17: mzef installs a real dist — the shared-store bare-name collision family (#4650/#4653/#4655/#4658)

Four fixes took mzef from "dependency resolution aborts" to "a real fez dist installs and is
`use`-able, and a dependency-ful one installs end-to-end": with the #4658 gate fix below,
`zef install --/test Test::META` resolves, fetches, extracts and installs **all 13 dists**.
All of it came out of running the **real zef** under mutsu — the north star working as intended.

- **#4650 — `Invalid dependency specification: True`.** `Zef::Distribution.depends-specs` binds
  `my $depends := system-collapse($.depends)`; when that returned `Nil` the bind produced **`True`**,
  the value of `Zef::Client`'s unrelated `$!depends` flag. Root cause: the cross-thread store is
  keyed by **bare name** and **global to the process**, and `clone_for_thread` migrates every env
  lexical into it on each spawn, so an early zef spawn left `depends => True` behind. `GetLocal`
  treated a `Nil` slot as "uninitialized — refresh from the store" and read the foreign entry.
  `set_shared_var_sym` **already** masked the write side on `thread_redeclared_vars`; the read path
  ignored it, and that asymmetry was the bug. Explains every symptom: `:=`-only (a `=` wraps in a
  `Scalar`, so the slot is a `ContainerRef` and returns before the Nil branch), Nil-only (the
  fallback is `is_nil()`-gated), name-sensitive (only migrated names collide).
  Pin: `t/shared-var-nil-redeclared-mask.t`.
- **#4653 — `.hash` on a type object.** `Any.hash` is `{}`; mutsu only special-cased the setty type
  objects and let the rest fall to the list path, throwing "Odd number of elements found where hash
  initializer expected ... (Any)". zef hits it in `Zef::Client`'s `bin-names`
  (`$dist.meta<files>.hash.keys`). `Mu` (no `.hash` at all → X::Method::NotFound) and `Hash.hash`
  (itself) keep raku parity. Pin: `t/type-object-hash.t`.
- **#4655 — CURI.install found sources via a `prefix` attribute.** Distribution classes disagree:
  rakudo's `Distribution::Path` has `$.prefix`, zef's `Zef::Distribution::Local` has `$.path`/`$.IO`
  and **no `prefix`**. The join produced a relative path that never existed and
  `std::fs::copy(..).ok()` swallowed it, so install wrote metadata whose `provides` named source
  files it had never copied — and returned True. `===> Installing:` + exit 0, nothing loadable.
  Now resolved through the distribution's own `.content($name-path)` (the API every Distribution
  implements, S22). Pin: `t/cur-install-content-api.t`.
- **#4658 — the store is scoped to a spawn lineage ([ADR-0010](../docs/adr/0010-cross-thread-lexical-sharing-scope.md)).**
  `Zef::Client.!fetch` hypers over candidates; each worker declares `my $uri` and runs a nested
  `start`, and that inner spawn migrates the worker's lexicals into the one global map a second time.
  N workers raced on the one key and every candidate downloaded whichever URL won. Per-name masking
  could never fix it — each batch thread has its own `Interpreter` and its own mask, but they shared
  the one map. Now each spawned thread gets a store **chained to its parent's**: siblings have no
  path to each other, while a child's write to the parent's `$counter` still reaches the parent.
  Lineage scoping does NOT subsume the #4650 `thread_redeclared_vars` gates, though — they handle
  the orthogonal **parent-child** case: a child's `my` re-declaration must not write through the
  chain to the parent's entry. The first draft removed them and deterministically regressed
  `advent2013-day14.t` (`if INIFile.parse(...) -> $parsed` inside a `start` block clobbered the
  parent's `my $parsed = Channel.new` with a Match). All three gates stay.
  Pin: `t/shared-store-lineage-scope.t`.

**What landing ADR-0010 taught us:** `__mutsu_*` keys are **not lexicals**. The atomic element stores
and the `state` cells are explicitly process-wide (all four threads of `my atomicint $x; …start {
$x⚛++ }` must hit one counter); lineage-scoping them silently loses updates — how three `state` tests
failed on the first attempt. They resolve at the root lineage; only user lexical names are scoped.

**Result:** `mutsu -e 'use JSON::OptIn'` fails on a clean HOME, `zef install --/depends --/test
JSON::OptIn` installs it, and `use JSON::OptIn` then loads — a real dist from the live fez ecosystem,
downloaded, extracted, installed into the site repo and resolved. (Verify with a **non-bundled** dist:
`use JSON::Fast` succeeds with no install at all because mutsu bundles it.) `zef install Test::META`
resolves 16 candidates and fetches them all; it now stops in **extract** — the frontier, tracked in
[docs/mzef-install-pipeline.md](../docs/mzef-install-pipeline.md).

## 2026-07-17: `|EXPR` argument interpolation unified onto MakeSlip — multi-slip calls fixed (mzef `zef install` blocker)

`zef install` died on `CLI.rakumod:508` with `Type check failed for an element of
@fetched; expected Candidate but got Slip`:

```raku
my @fetched = grep *.so, |@local, ($client.fetch(@remote).Slip if +@remote && $fetch);
```

`|EXPR` in an argument list had its own two opcodes (`CallFuncSlip` /
`ExecCallSlip`) carrying a **single** `slip_pos: Option<u32>`. Two defects fell
out of that shape:

- **Only the slipped argument was spread.** Every other argument was pushed raw,
  so a positional that merely *evaluated* to a Slip (the `.Slip if ...` above)
  stayed a one-element Slip. `grep *.so, |@e, @s.Slip` gave 1 element (raku: 2).
- **A second `|` corrupted the stack.** `slip_pos` recorded only the last slip
  and `regular_arity` counted only non-slip args, so `f(|@a, |@b)` drained fewer
  values than were pushed — `say "x", (grep *.so, |@n, |@s).elems` printed
  neither the label nor a correct count.

Both opcodes are gone. `|EXPR` now compiles to the existing `MakeSlip` plus an
ordinary call op, so argument spreading is driven by the argument's *value* and
any number of `|` args mix freely with the other arguments. This also removed
~180 lines of dispatch that were a hand-maintained mirror of the non-slip path
(its own comments read "Mirrors the non-slip proto branch") and had to be
re-synced by hand every time the real path gained a case; `MakeSlip` is
additionally *stronger* than the old `append_slip_value` (it materializes a
deferred `Seq` iterator and forces a `gather`, which the static helper could
not). Net -416 lines.

Routing slips through the ordinary call op exposed a latent bug in the call
caches: the light-call and OTF caches bind the **compiled** arity straight off
the stack, but a Slip argument spreads and so changes that arity. They only fire
once warm, so a slipping call worked on its first invocation and then broke on
the second (`lives-ok { f2(:a(1)) }; f2(:a(4))` → "Calling f1(Slip) will never
work with declared signature (%h)"). All four cache sites now skip on a Slip
argument via the shared `stack_args_have_slip` guard.

Pin: `t/slip-arg-flatten.t` (25 cases, all verified against raku first; includes
the warm-cache shapes, which are what the regression needed).

Known gap, unchanged and pre-existing: mutsu spreads a Slip-valued argument into
*fixed* positionals, where Rakudo keeps it intact until slurpy binding
(`sub g($a,$b){}; g(@s.Slip)` is an error in raku, works in mutsu). Slurpy and
listop callees — which is what zef needs — match raku.

## 2026-07-17: push-based supply event delivery — react busy-spin, lost done/emit, serial throttle all fixed (ADR-0008)

The oldest concurrency mechanism in the codebase — snapshot-polling supply
consumers — was replaced with push delivery (`ReactWaker` sinks; see
[ADR-0008](../docs/adr/0008-push-based-supply-event-delivery.md)). Root-caused
from the S17 CI load-flakiness investigation; the poll architecture had four
structural defects:

- **A react over supplier-backed sources busy-spun at 100% CPU** (no blocking
  point in the drive loop at all). `react whenever $s.Supply { }` burned a
  core for its lifetime and hammered the supplier-registry mutex.
- **`Supplier.done` lost events**: it fires callbacks then resets the registry
  entry, so a poller that hadn't seen the flag (or the values emitted just
  before) lost them forever — `start { $s.emit(1); $s.emit(2); $s.done }` +
  `react whenever $s.Supply` delivered only `1`, then hung. Now impossible by
  construction: emit/done/quit are pushed into consumer queues under the
  registry lock before any reset.
- **Quadratic clone churn**: `supplier_snapshot_seqs` cloned the entire emit
  history per delivered value (the `emit_seqs` merge machinery is deleted —
  push order *is* emit order).
- **1ms/10ms `thread::sleep` polls** (supply list wait, throttle control
  waits, `Promise.anyof`) amplified wall time badly under CPU contention —
  the mechanism behind mutsu running S17 1.5–2× slower than raku under load.
  All converted to condvar waits (`wait_activity`, `on_resolve`, channel
  wake hooks).

`Supply.throttle(0, &block, :control)` also ran its block **serially**; it now
runs `effective_limit`-way concurrent workers (emission = completion order,
delivery serialized), and the non-block path vents its first batch immediately
on receiving the control limit (Rakudo semantics) — throttle.t's fixed-sleep
margins roughly doubled. `Supplier.quit(Str)` wraps its reason in `X::AdHoc`
(instances pass through untouched, so `when MyException` still matches).

`Supply.interval` moved onto a **shared timer thread** (deadline min-heap +
condvar, `interval_timer.rs`) instead of one 1ms-sleep-loop OS thread per
interval instance — syntax.t (2000 short-lived intervals) went from ~195k
`clock_nanosleep` syscalls per run to zero on those sections, and its debug
CPU time dropped from ~206s to under 30s.

An idle `react whenever $supplier` now waits at ~0 CPU and wakes in the emit's
lock-handoff time. Pin: `t/react-supplier-done.t`.

**Follow-ups the same day** finished the two mechanisms the core PR had left on
the old model (PLAN §6):

- **Every timed event shares one driver thread** (#4638). `interval_timer.rs`
  generalized from an interval-only heap into a deadline heap of closure
  actions — an action returns `Some(period)` to reschedule (intervals) or
  `None` to die (one-shot), and due actions run with the heap lock *released*
  so an action may register further timers. `Promise.in`/`.at` and
  `Scheduler.cue(:in/:at)` moved onto it: a pending timer no longer holds a
  sleeping OS thread (**100 pending `Promise.in` = 102 → 3 threads**). Keeping
  a promise from the driver is safe because `SharedPromise::keep` dispatches
  waiters to their own thread rather than running user code on the resolver.
  Fixes on the way: `Promise.in(Inf)` panicked in `Duration::from_secs_f64`
  (now means never-resolve), and a cue's delay used to `sleep` un-quiesced on a
  GC-registered thread, stalling any stop-the-world for its whole duration.
  Pin: `t/timer-heap-threads.t` (passes on raku too).
- **The last polled sources now wake the loop** (#4639). Interval ticks,
  Proc::Async stdout/stderr, zip outputs, signal watchers, `IO::Path.watch` and
  the socket pumps delivered through raw mpsc channels, which the drive loop
  could only observe by re-polling every 10ms. `native_methods/supply_channel.rs`
  wraps the mpsc pair as `SupplySender`/`SupplyReceiver` sharing a waker set;
  every send — and the sender's *drop*, so hangups register too — pokes the
  consumer's `ReactWaker`. Making the registry and `ReactSubscription` hold the
  wrapper types let the compiler prove no raw supply channel was missed. The
  zip/zip-latest coordinators likewise dropped their per-source
  `recv_timeout(10ms)` round-robin for drain-all + one waker. With every source
  now pushing, `REACT_IDLE_WAIT` is a missed-wakeup safety net (10ms → 250ms)
  rather than a latency floor: an idle react (`interval(60)` + `Promise.in(3)`)
  went from **887 to 340 futex calls (−62%)**.

Still on the old model: a repeating `cue(:every)` owns a worker thread that
sleeps between iterations (50 idle `cue(:every(60))` = RSS +21 MB / VmSize
+16.4 GB, against raku's +3 MB / +25 MB). Measuring it showed the timer is not
the missing piece: each iteration runs user VM code, so a timer entry would
have to spawn a fresh 256 MiB-stack worker per tick. raku wins here because its
timer hands off to a thread pool, which mutsu does not have — tracked in PLAN
§6 as a worker-pool slice, not a timer one.

## 2026-07-17: integration/99problems-41-to-50.t whitelisted — regex LR seed loop no longer 2^depth (P47)

`integration/99problems-41-to-50.t` aborted at 2/9: P47's precedence-climbing
grammar (`multi rule expr($p) { <expr($p-1)> *%[ <?before <pred($p)>> <op> ] }`
plus `term:sym<not>`/`term:sym<paren>` recursing through `<term=.expr(3)>`) did
not finish. The file now passes 9/9 in ~1s. Four independent bugs, each
A/B-verified against raku:

1. **The LR seed-growing loop ran every subrule twice** (`regex/regex_match_atom.rs`).
   `new_max > best_inner_max` is `Some(x) > None` on the first successful
   iteration, so the loop always ran a second pass just to observe "no growth" —
   even for rules that are not left-recursive at that position. That 2× compounded
   through nesting to **2^depth**: an instrumented probe showed the per-level
   evaluation counts doubling exactly (`expr(3)`:96 → `expr(2)`:192 → `expr(1)`:384
   → `expr(0)`:768 → `term`:1536), i.e. 5841 evaluations over just **13** distinct
   (rule, position) pairs — a 449× redundancy. A rule whose seed is never
   *consulted* cannot change when the seed grows, so its first result is final:
   `LR_SEED_READ` now records genuine re-entries and the loop stops after one
   iteration otherwise. `not B` 13.7s → 0.28s (113×); `not not B` (previously
   non-terminating) 0.25s. This is the memoization the old BLOCKERS row asked for,
   obtained by not doing the redundant work rather than by caching it — no cache,
   no invalidation, no purity gate. Pin: `t/regex-precedence-climb-perf.t`.
2. **Hyper method calls bypassed `Nil.FALLBACK`** (`vm/vm_hyper_method_ops.rs`).
   `Nil.ast` correctly yielded `Nil`, but `(Nil,)».ast` raised "No such method
   'ast' for invocant of type 'Any'" because the hyper leaf went straight to
   dispatch. That silently aborted grammar actions doing `@<op>».ast` over a
   capture that never matched. The Nil-method categorisation is now shared via
   `nil_absorbs_method` instead of being duplicated. Pin: `t/hyper-method-on-nil.t`.
3. **A `%` separator's captures were not quantified captures**
   (`regex/regex_match_core.rs`). `collect_quantified_names_for_token` read the
   token's atom but never its separator, so `<a> *% <sep>` matching a single atom
   left `$<sep>` absent (`@<sep>` a bogus 1-element `(Nil,)`) instead of an empty
   list — an action then indexed a phantom op. Pin: `t/regex-sep-quantified-capture.t`.
4. **Block-scoped grammars lost their tokens** (`runtime/accessors_misc.rs`).
   A package declaration is `our`-scoped, not scoped to an enclosing bare block,
   but the block-scope registry restore preserved the *class* while restoring
   `token_defs` wholesale. `{ grammar B { token id {…} } }` followed by
   `grammar D is B { rule TOP { <id> } }` then failed with "No such method 'id'
   for invocant of type 'Match'" (the engine's method-subrule fallback) — exactly
   this file's shape, since its P46 block declares the base grammar its P47 block
   subclasses. The EVAL path already carried this fix; it now also applies to
   tokens owned by a declared package, so a block-local `my token` still drops.
   Pin: `t/grammar-decl-in-block-tokens.t`.

## 2026-07-16: integration/advent2012-day09 + advent2011-day11 whitelisted (frugal-ratchet + sigspace-capture + EVAL fixes)

Two ★-frontier `integration/` files reached the whitelist (both full raku pass),
via four independent, general-purpose fixes. Pins:
`t/grammar-frugal-ratchet-sigspace.t`, `t/eval-private-and-stubs.t`.

**advent2012-day09 (grammar longest-token-matching, 3/8 → 8/8)** — two regex
bugs, both real and general:

1. **Frugal quantifiers under ratchet matched greedily.** `\S+?` / `.+?` inside
   a `token`/`rule` committed to their *longest* run (`walk_ratchet_fast_paths`
   possessive scan + `walk_quant_chain`'s frugal branch gated on `!ratchet`)
   instead of growing shortest-first until the rest of the pattern matched. raku
   grows a frugal quantifier even under ratchet — ratchet only changes *greedy*
   behavior (commit-to-longest, no backtrack). Fixed by deferring frugal tokens
   out of the possessive fast paths and applying the shortest-first growth branch
   regardless of ratchet. A named-capture-wrapped frugal quantifier (`$<to>=\S+?`
   wraps in a `One` group) additionally had the outer wrapper's ratchet drain its
   frugal length candidates down to the shortest; the wrapper is now non-ratchet
   for the frugal case (the inner token already enforces ratchet semantics).
2. **A `rule`'s implicit sigspace dropped `<.ws>` before a `$<name>=` capture.**
   `should_insert`'s `(_, '$')` clause — meant for the end-of-string anchor —
   also suppressed the significant space before a `$<name>=` / `$0=` / `$var`
   capture alias, so `rule greet { … $<to>=\S+? ',' }` compiled to
   `… $<to>=\S+? ','` (no whitespace matcher) and failed on real input. A `$`
   that begins a term is now normalized so only the bare anchor `$` suppresses
   `<.ws>`.

**advent2011-day11 (privacy/OOP, 7/9 → 9/9)** — two EVAL-inside-a-method fixes:

3. **EVAL wrongly ran the whole-unit stub check.** `EVAL '$order.discount'` inside
   a file that forward-stubs `class Trusty {...}` (defined later) died with
   `X::Package::Stubbed` because EVAL re-ran the global unresolved-stub check
   against the outer unit's still-pending stubs. EVAL is its own compilation unit
   and now only reports stubs it introduced itself.
4. **`self!private()` typos are resolved at compile time.** raku resolves private
   method dispatch at compile time, so a typo'd `self!nope()` errors even when a
   preceding `return 1` short-circuits it at runtime. EVAL now validates
   `self!private` calls in the compiled string against the lexical `self`'s class
   (reusing the existing class-composition validator), throwing `X::Method::NotFound`.

## 2026-07-16: re-entrant `... *` sequence generators defer to lazy (integration/advent2012-day14.t)

`my @primes = 2, 3, 5, -> $p { … @primes … } … *` — a `...` sequence whose
generator consults the sequence being built — no longer aborts with
`X::Cannot::Empty`. Eager-prefix generation of an infinite closure sequence now
**catches a generator error and defers the sequence to lazy** instead of failing:
the generator re-runs on demand (after `@primes` is bound), matching Rakudo's
lazy `Seq`. This is deliberately surgical — only sequences that *error* during
eager generation defer; ordinary infinite closures still reify their eager
prefix, so no other sequence consumer (`^...`, `clone`, `.is-lazy`, slip
generators, …) changes behavior. Supporting slices:

- The deferred sequence is tagged with the `@`-assign-preserve marker so
  `my @primes = …` keeps it un-forced (forcing it during the assignment would
  re-run the generator before `@primes` is bound, re-triggering the error).
- **`LAZY-LIST ... endpoint`** where the left is a genuinely-infinite lazy
  sequence (`@primes ...^ * > sqrt $n` inside `is-prime`) now walks the lazy
  left's own elements against the endpoint (`eval_sequence_from_lazy_left`)
  rather than deducing a fresh arithmetic pattern from its realized prefix
  (which threw `X::Sequence::Deduction` on a non-deducible prefix like `2,3,5`).
  Narrowed to `closure_seq`/`sequence_spec`/`scan_spec` sources so a finite
  `lazy <a b c>` still seeds ordinary deduction.
- **`extend_closure_sequence`** flattens a `slip`ped generator result into the
  history so a multi-value generator (`{ slip $^a+1, $^b*2 }`) contributes each
  value as its own element past the eager prefix, matching the eager path.

Pin: t/seq-reentrant-generator.t. Whitelisted
roast/integration/advent2012-day14.t (0/6 abort → 6/6).

## 2026-07-16: grammar rule-embedded dynamic variables + sigspace fixes (integration/advent2013-day18.t 0/10 → 9/10)

A `rule`/`token` body may declare a *dynamic* variable inline
(`rule deal { :my %*PLAYED = (); … }`), scoped to the whole parse so action
methods and `<?{ … }>` code assertions run during the match share it (the card
grammar dedups played cards through `%*PLAYED`). Landing this fixed a cluster of
grammar bugs:

- **`:my %*var` no longer mangled.** A `%*PLAYED` inside a `:my … ;` declaration
  was being rewritten (`%[ <.ws>? * <.ws>? ]PLAYED`) — the `%` was mistaken for a
  regex hash-interpolation / `%`-separator by **four** passes a rule body goes
  through. All four now skip an embedded `:my`/`:our`/`:constant`/`:let`/`:temp`
  declaration verbatim: `interpolate_regex_scalars`, the LTM separator scan
  (`has_unquoted_ltm_separator` + `expand_ltm_pattern`), and both sigspace
  injectors (`inject_implicit_rule_ws` + `inject_separator_ws`).
- **Sigspace `[ <card> ]**5` matches space-separated input.** Whitespace *after*
  a term but *before* a closing `]`/`)` is a required `<.ws>` in Raku sigspace
  (`inject_implicit_rule_ws` no longer suppresses `(_, ']')` / `(_, ')')`), so a
  `rule` with `[ <w> ]**3` matches `"a b c"` but not `"abc"`, matching Rakudo.
- **Dynamic-var scope.** `Grammar.parse` scans the grammar's rules (MRO-walked)
  for `:my $*/%*/@*NAME = …;` declarations and establishes them in the parse's
  dynamic scope (`establish_grammar_dynamic_vars`), restoring the prior values
  afterward. Action methods and code assertions then share them.
- **`<?{ … }>` sees the matched-so-far `$/`.** A code assertion's `$/` is now a
  proper Match object whose `.Str`/`.lc`/`~$/` yield the text matched up to the
  assertion (previously a bare array of positional captures), so `%*PLAYED{$/.lc}`
  keys correctly. Making `$/` a Match also surfaced (and fixed) a latent bug: a
  `WhateverCode` index on a Match (`$/[*-1]`) computed its length only for
  Buf/Blob, so `*-1` resolved to `-1` → `Nil`; it now counts the Match's
  positional-capture `list` (roast S05-metasyntax/assertions.t).

Pin: t/grammar-dynamic-var-decl.t. The one remaining test-10 failure (an `our
@dups` array `.push` side effect inside a code assertion not persisting out of
the `&self` backtracking matcher) is recorded in BLOCKERS.md; the file is not yet
whitelisted.

## 2026-07-16: `is Array` subclass instances behave as Positional arrays (integration/advent2013-day08.t)

`class Vector is Array` with a custom `new` that redispatches its positional
args to the base Array construction via `nextwith(|@values)` now produces a
genuinely array-like instance. The elements live in the existing
`__mutsu_array_storage` backing attribute; this slice made that storage the
authority across every context the roast test exercises:

- **Construction** — `dispatch_bless` now seeds `__mutsu_array_storage` from
  the positional (non-Pair) args, so `nextwith(|@values)`/`callwith` inside a
  subclass `new` (which lands as `bless` with positional args) reaches the
  base `Array.new(1,2,3)` element semantics.
- **Indexing read** — `$v[0]` routing through `try_compiled_method_or_interpret`
  now delegates `AT-POS`/array methods to the storage (previously only the
  CallMethod-opcode path delegated, so `$v.AT-POS(0)` worked but `$v[0]` was Nil).
- **Indexing write / mutation** — `$v[1] = x`, `$v[1]--`, `$v[0]++` write into
  the storage element in place through the shared instance cell, instead of the
  associative path spuriously creating an attribute keyed by the index.
- **rw accessor lvalue** — `method z() is rw { self[2] }` is now an assignable
  lvalue that autovivifies `__mutsu_array_storage[2]`.
- **List context / iteration** — `for @$v { ... }` and `@$v` flatten to the
  storage elements (`value_to_list`), not the instance as one item.
- **Hyper ops** — `self »-« @vec` / `self »**» 2` deitemize the instance to its
  storage list so both sides line up by length.
- **gist / raku** — an Array-subclass instance gists as `[1 2 3]` (delegating to
  the storage), not the generic `Class.new`.

Pin: `t/array-subclass-vector.t` (19 cases). Whitelisted
`roast/integration/advent2013-day08.t` (0/11 abort → 11/11).

## 2026-07-16: grammar-parse — trail matcher landed (ADR-0007 Accepted, #4591)

The by-value `RegexCaptures` threading through the backtracking search is gone:
the engine now walks pattern tokens depth-first over **one mutable capture
store per pattern level with an undo-log** (`regex_trail.rs`: `CapStore`,
`mark()`/`merge_delta()`/`rewind()`). Atom candidate producers return *deltas*
relative to an empty baseline — the conversion was mechanically
`current_caps.clone()` → `RegexCaptures::default()` (~30 sites) because every
producer only ever appended to the cloned base — and the engine applies a
candidate, descends, and rewinds on backtrack, so per-step capture cost is
O(delta) instead of O(accumulated state). Quantifier chains grow/shrink on the
store with a mark per iteration; `quant_expand_greedy` (full caps clone per
tree node) became the trail-based `quant_alt_dfs`; the `$N=`/alias mid-vector
surgery and quantified-capture folds are trailed store operations with
value-level undo. Separated quantifiers moved to `regex_match_sep.rs`.

Measured (local release A/B vs main): `bench-grammar-parse-deep` ~×1.25 (perf
samples 11872 → 9449; memmove 15.4% → 7.5%; `RegexCaptures::clone` 2.1% → 1.0%),
shallow ~×1.2. Validation: full `make test`, the 93-file S05 whitelist (5524
subtests), and the exponential-backtracking pins all green; trail round-trips
unit-tested. Lesson recorded in the ADR: the "~60–70% allocator churn" was only
partly accumulated-state churn — the residual (~36%) is **per-subrule ceremony**
(candidate vecs, captured Strings, Arc subcap allocs, SipHash map traffic, a
still-unexplained runtime regex re-parse ~4%), so the ≥5× target needs the
follow-up slices listed in PLAN §5.

## 2026-07-15: grammar-parse — nested sub-captures shared behind Arc (#4586)

`RegexCaptures` threads by value through the entire backtracking DFS, and its
`named_subcaps` / `positional_subcaps` fields (and quantified-capture entries)
held the **whole nested sub-match tree** of every matched subrule as an owned
value. Cloning a parent `RegexCaptures` — which happens at every DFS stack push
and every fold in the separated / repetition quantifier paths
(`append_separated_captures`, the alternation/conjunction merges) — therefore
deep-copied that whole tree, so capture churn was O(tree-size) per step.

A completed sub-match is effectively immutable once stored, so those nested
sub-captures now hold `Arc<RegexCaptures>`: a parent clone is a refcount bump
instead of a recursive deep copy. The two rare post-store tweaks (setting
`action_name`, pinning an aliased group's span) go through `Arc::make_mut`,
which is free while the entry is still unshared (refcount 1). `Arc` (not `Rc`)
keeps `RegexCaptures` `Send`-compatible so there is no thread-crossing
regression risk; matching is single-threaded, so the atomic refcount cost is
negligible next to the malloc/memmove it removes.

Results (release, min of several runs vs the `main` baseline built in a
worktree): `RegexCaptures::clone`+`drop_in_place` drop from ~10% to ~4% of
profiled samples; `benchmarks/bench-grammar-parse.raku` ~9% faster
(1.51s → 1.37s); a deeper nested-JSON document ~4–10% faster. Pinned by
`t/regex-nested-subcaps-sharing.t`; the full S05 regex/grammar whitelist
(67 files, 2610 subtests) and the `t/` grammar+match suite stay green.

This is one slice of the grammar-parse allocation-churn item in PLAN §5. The
remaining, larger prize: grammar parsing is **linear** in document size but
~**30× slower than raku per matched character** (~18 ms/char on a deep
object-of-nested-arrays bench; profile: ~19% memmove + ~40% malloc/free). It is
a high constant-factor churn problem, **not** O(n²) — an earlier note to that
effect was a 3-iteration benchmark-loop artefact and has been corrected. The
sound fix is a single mutable capture store + undo-log (trail) matcher replacing
the by-value CPS threading; the design, structural blocker (all-ends enumeration
vs single-path rewind), full mutation/trail inventory, hazards, and a 4-phase
plan are recorded in
[docs/adr/0007-grammar-parse-trail-matcher.md](../docs/adr/0007-grammar-parse-trail-matcher.md)
(Proposed) — the next grammar-parse lever.

## 2026-07-15: roast-wide raku-vs-mutsu speed measurement + perf re-baseline

Measured wall-clock of raku vs mutsu (release) across all 1358 runnable
whitelisted roast tests (`scripts/roast-speed-diff.sh`, single-run; ratio =
mutsu/raku, so <1.0 = mutsu faster). **mutsu beats raku (Rakudo 2022.12) on
~1348/1358 tests, usually 2–30×** thanks to fast startup + JIT. The only files
where mutsu is meaningfully slower all converge on **one root cause — the
interpreter function-call path in hot loops**:

- `S04-declarations/state.t` **4.2×** (`for ^2000000 { $ = foo }`),
  `S06-signature/named-parameters.t` **2.6×** (`for ^1000000 { foo(:color($_)) }`),
  `S07-iterators/range-iterator.t` **1.7×**, `S12-methods/private.t` **1.6×**.
- Isolated micro-repro: a 1M-iteration loop calling a sub is **1.85× slower**
  than raku (positional) / **2.6× slower** (named arg).
- **The JIT bails at the call boundary** — the positional 1M loop runs at
  `MUTSU_JIT=on` 0.74s ≈ `off` 0.72s (JIT contributes nothing), so any loop that
  calls a sub runs the interpreter call path. Its profile is **~15% malloc/free
  churn per call** (frame + named-args structure + `Env::cow_mut` +
  `Arc::drop_slow`) plus per-call `current_package` / `Env::get_sym` /
  param-binding. Named args cost mutsu **+46%** vs raku **+6%** — a
  `String`-keyed named-args structure rebuilt every call.

This re-baselines the §5 perf roadmap: the call path is now the top perf target
(it hits real spec tests and the most common real-world shape), while the
`needs_env_sync` blanket and the `BlockScope` declone — both investigated the
same day — turned out to polish benchmarks mutsu already wins (see below). The
synthetic `bench-grammar-parse` 6.4× gap does not surface in the roast whitelist.

## 2026-07-15: `once` fires exactly once per clone (threads, repeated calls, methods)

`once { ... }` broke in three independent ways (PLAN §3). Fixed across two PRs:

- **Subs / closures / threads (#4567).** The site key was built from a global
  compile-time counter, so a worker thread — which re-OTF-compiles the routine
  into a fresh body — drew a different key and never matched a peer's fire; and
  a named sub's second call took a fast/light call path that skipped the
  clone-id setup, keying under the root scope and re-firing. The site key is now
  the `OnceExpr` op's **bytecode position** (deterministic across recompiles),
  the store is an `Arc`-shared `OnceStore` with a **claim/condvar** protocol
  (fires once even under a genuine race — a thread re-entering its own
  in-progress `once` is served `Nil` rather than deadlocking), and
  `once`-bearing routines are kept off the fast/light paths via
  `CompiledCode::has_once` so every call runs the same clone-id setup.
- **Methods (this PR).** A method's `__mutsu_callable_id` is a fresh *per-call*
  id, so a method's `once` had no stable clone key and re-fired every call.
  `once_scope_key` now derives a method's clone key from the innermost routine
  frame's `(owning class, method)` — stable per clone, and consulting the
  *innermost* frame means a `once` in a closure nested in the method still keys
  on that inner clone. This matches Rakudo: once total for a normal/inherited
  method (the defining class's single clone), once per composing class for a
  role method, and it leaves the per-call id recursive `return`-targeting relies
  on untouched. `once` now also fires once across threads for methods.

Pins: `t/once-clone-scope.t` (10 cases), `t/once-phaser.t` (12);
`roast/S04-statements/once.t` stays whitelisted.

## 2026-07-15: parameterized grammar rules (`multi rule expr($p)` / `<expr($p-1)>`)

The P47 precedence-climbing grammar shape now works (pin:
t/parameterized-rules.t; all reduced shapes verified against rakudo):

- **`multi rule`/`multi token` parse and accumulate**: the leading `multi`
  was being swallowed as a bare-word expression statement, so the second
  candidate silently replaced the first in `token_defs`.
- **Literal-arg dispatch is exclusive**: `<expr(0)>` picks `multi rule
  expr(0)` alone (rakudo narrowest-wins) — a generic `expr($p)` candidate is
  excluded when an exact literal candidate matches, and a candidate whose
  literal params MISMATCH the args is dropped (bind does not enforce
  literals).
- **Recursion toward a base case is not left recursion**: the LR-detection
  key now includes the argument values, so `expr($p)` calling
  `<expr($p-1)>` at the same position resolves normally instead of through
  the empty LR seed.
- **Lookahead arg instantiation**: `<?before <pred($p)>>` renders `$p` with
  the rule's bound params (the lookaround body otherwise evaluated args in a
  default env). Regex arg exprs also stop mis-scanning `$p-1` as the kebab
  identifier "p-1".
- **With-args candidate memoization** (`PARSED_TOKEN_ARG_CANDIDATES`), gated
  on bodies that reference nothing beyond their own params.

The full 99problems-41-to-50.t P47 grammar remains blocked on regex-engine
matching performance (exponential LR re-matching across nesting levels) —
recorded in BLOCKERS.

## 2026-07-15: while-gather pull resume + lazy readonly context — 99problems-31-to-40.t whitelisted (67/67)

Two fixes, root-caused from the file's abort at 44/67 (P37 Euler's totient):

- **Lazily-forced gather bodies run under their own readonly context**: the
  force paths swap out the whole readonly state (`take_readonly_state`), so
  a body writing its `is copy` param (`sub f($n is copy) { gather { $n div=
  2 } }`) no longer false-positives against a consumer frame's same-named
  readonly param (`sub phi($n) { for f($n) {...} }` threw "Cannot assign to
  a readonly variable (n)").
- **Condition-driven-loop gathers suspend at the iteration boundary**: the
  at-take suspension resumed a `while`/`loop` body by re-entering from the
  condition, replaying the statements from the iteration start to the take —
  `while $n > 1 { take $n; $n div= 2 }` pulled 6,6,6... forever. A take-limit
  hit inside `while`/`until`/C-style/`repeat` now defers the suspension
  (`gather_suspend_pending`, armed per loop kind via
  `lazy_take_boundary_defer`) to the loop's next iteration boundary, where
  the condition re-entry is exact. `for`-shaped bodies keep the immediate
  at-take suspension: their positional resume (`next_index`) is exact for
  values, and roast pins the side-effect timing (S04-statements/gather.t
  "gather is lazy"). An overshoot backstop (limit+64) still bounds loop-less
  recursive-take shapes.

Pin: t/gather-while-pull-resume.t.

## 2026-07-15: map routine callbacks, recursive index-rw temp corruption, (<=) Range — 99problems-21-to-30.t 6/15 → 14/15

Three general fixes root-caused from the file's abort:

- **`return` in a `sub` callback invoked via map ends that call** (routine
  semantics): `map $f, @xs` / `@xs.map($f)` with a compiled routine `$f` now
  routes through `call_sub_value` instead of the inline bare-block fast path
  (whose escaping return signal surfaced as X::ControlFlow::Return). Bare/
  pointy blocks, WhateverCode, and placeholder blocks (`{ $^x }` — Blocks,
  not Routines, and Pair elements must not bind as named args) stay on the
  fast path.
- **Recursive slice-arg writeback corruption**: the call-site Index-argument
  `is rw` writeback temps (`__mutsu_index_rw_*`) are compile-time-fixed
  global names, so a recursive execution of the same call site (or the
  callee's env merge) clobbered the caller's pending compare, firing a bogus
  `@xs[1..*] = ...` that exploded on an immutable List (P26 `combination`).
  Fixed on both ends: the cross-frame env merges (compiled-call, method,
  interpreter-sub) never copy `__mutsu_index_rw_*`/`__mutsu_call_result_*`
  back to the caller, and the writeback adds an `eqv`-against-current-slot
  guard so a no-op difference does not fire.
- **`(<=)` enumerates a Range operand**: `coerce_value_to_quanthash` lacked
  the Range arm its `coerce_to_set` sibling had, so `@n (<=) (1..49)` built a
  one-element Set of the string "1..49".

Remaining 1 subtest (P27 nested-map Seq structure) is recorded in BLOCKERS.
Pin: t/map-sub-return-index-rw.t.

## 2026-07-15: statement-form loop-phaser scope + sub-body INIT lifting — advent2012-day15.t whitelisted (11/11)

Three fixes, one per layer:

- **Statement-form loop phasers share the enclosing block's scope** (rakudo
  semantics): `NEXT (state $best) max= $score;` declares `$best` where the
  loop body can see it, so `LAST say "BEST: $best"` works. The parser marks
  the statement form as `[SyntheticBlock([stmt])]` (block form keeps its own
  `Stmt::Block` scope in `expand_loop_phasers`); the phaser-reorder pass
  preserves the marker instead of flattening it.
- **INIT/CHECK inside a named sub body are program-init-time**: the phaser
  lift now recurses into `Stmt::SubDecl` bodies, so `my $fh = INIT Open(...)`
  calls Open before the mainline runs (and memoizes the value for call time).
- **The I/O ops no longer seed not-yet-declared locals into env**: Say/Put/
  Print/Note's pre-sync (`sync_env_from_locals_declared`) skips slots whose
  name is absent from env — a premature seed made a later `state` declaration
  in the same loop body record itself as a "shadowed outer binding", and the
  loop-local restore wiped the variable at loop exit (the same guard the
  regex-interpolation sync already had).

Pin: t/phaser-stmt-form-scope.t.

## 2026-07-15: private-attr read on a wrong-class invocant throws — weird-errors.t whitelisted (36/36)

Reading `$!x` inside a method whose concrete invocant's class neither carries
the attribute in its instance cell nor declares it anywhere in its MRO now
throws the rakudo-parity `P6opaque: no such attribute '$!x' on type ... in a
... when trying to get a value` error instead of yielding Nil (weird-errors.t
test 29: `our method foo(A114672:) { $!x }` called as `&B114672::foo(A114672.new)`).
The check sits in the GetLocal Nil-fallback and after the GetGlobal cell-read
miss (`missing_private_attr_read_error`), and is declaration-based — an
attribute that is declared but merely unseeded still reads as Any, so no
construction-order path can throw spuriously. Side effect: the class-C
"untrusted access dies" tests of the no-oracle `S12-attributes/trusts.t` now
pass (its failing set shifted; see BLOCKERS). Pin: t/private-attr-wrong-class.t.

## 2026-07-15: weird-errors.t 31/36 → 35/36 — four parse-form gaps closed

Continuing the ★ list top-down. Four general parser/semantics fixes (each
verified against rakudo); test 29 (private-attr read on a wrong-class
invocant must throw) is deferred as its own slice — see BLOCKERS:

- **`:[...]` itemized-array colonpairs**: `:[]` is `$[]`, `:[1,2]` is
  `$[1, 2]` (colonpair_expr).
- **Leading-`;` LoL argument slices**: `f(;x)` is the two-slice list
  `((), (x,))`; `parse_call_arg_list` already grouped mid-list `;`s but
  required a first argument. `say(;:[])` now prints `()([])`, byte-identical
  to rakudo. (The stmt-level paren-args parser also gained a semicolon-sliced
  form.)
- **`::lowercase` is a runtime symbol lookup**: `::a` throws X::NoSuchSymbol
  (symbol => "a") like `::('a')` instead of losing the `::` and reporting
  X::Undeclared::Symbols; an existing lowercase type still resolves.
- **Indirect object notation**: `new Foo:` / `method Type: args` desugars to
  `Type.method(args)` (narrow parse: uppercase invocant, colon attached,
  terminator after — labels/adverbs/smileys can't match).
- **`hash` listop**: `sub f1 { hash a=>1 }` parses (added to
  `is_expr_listop` beside set/bag/mix).

Pin: t/weird-errors-parse-forms.t.

## 2026-07-15: module-file backtrace frames + Failure dual backtraces — error-reporting.t whitelisted

The last two ④ items land and `integration/error-reporting.t` passes 33/33
(whitelist 1411/1463):

- **Backtrace frames report the defining file** (test 15): `FunctionDef` (and
  `CompiledFunction`) now carry `source_file`, recorded at registration time
  from `?FILE` — which module loading scopes to the module path while the
  module mainline runs. `RoutineFrame` gained `def_file`, and both backtrace
  builders display a frame at its defining file, so a `use`d module's sub
  reports `Foo.rakumod` while mainline frames keep the script path.
- **Unhandled thrown Failures print rakudo's dual-backtrace form** (test 20):
  the Failure's exception already carried the fail-site backtrace; the
  unhandled-Failure error now threads it through
  (`failure_original_backtrace`), and the throw-site attach renders
  `<fail-site bt>\n\nActually thrown at:\n<throw-site bt>` — byte-identical
  to rakudo on the roast program.

Pin: t/backtrace-module-file.t (+ fixture t/lib/BacktraceFixture.rakumod).

## 2026-07-15: error-reporting quality slice — 3 of the 5 remaining error-reporting.t items (31/33)

Working the ④ error-message-quality cluster top-down (integration/error-reporting.t
28/33 → 31/33; the file is not yet whitelisted — tests 15/20 remain, see BLOCKERS):

- **`say` no longer swallows return signals from lazy Seqs** (test 21):
  `render_gist_value` used to fall back to the native gist on ANY `.gist`
  dispatch error, which ate the `return` control signal raised while the gist
  forced a lazy map whose block did `return` (the enclosing routine had
  already exited). It now returns `Result`: a return signal (or typed
  X::ControlFlow::Return) propagates as "Attempt to return outside of any
  Routine"; other dispatch failures keep the fallback. TODO in code:
  `render_str_value` (put/print) still swallows these.
- **`%::{''}` → X::Undeclared** (test 25): bare `%` followed by `::` +
  non-identifier is rakudo's undeclared bare variable `%`; the parser now
  compiles it to a typed X::Undeclared die (symbol/what/line attrs). Bonus:
  `%::name` now resolves as the root-namespace-qualified `%name` (it used to
  mis-parse as an anonymous hash + orphan stash index).
- **Inheriting from a type-capture parameter is a compile-time error**
  (test 30): `-> ::T { class :: is T {} }` raises X::Inheritance::Unsupported
  at CHECK time without running the block (`check_type_capture_inheritance`,
  a best-effort AST walk in the style of the other eval_check passes).
- **Bonus general fix**: `like`/`unlike` matched only plain `Regex` values —
  any adverbed regex (`rx:i/.../`) silently never matched. They now route
  adverbed regexes through the general smartmatch.

Pin: t/error-reporting-quality.t.

## 2026-07-15: wrap-chain candidate scan gated off the slow dispatch path — zef ctor another ~3x

Third slice of the zef-constructor hot path (PLAN §1 B2). After the definite-return fix, the
ctor profile still showed ~6% AST `Debug` formatting: `run_instance_method` (the slow path
every `bless`/TWEAK takes) checks for a method-level `.wrap` chain via
`find_method_candidate_index`, which structurally fingerprints the target **and every
candidate** — a Debug traversal of whole method bodies — on **every call**, even in the
overwhelmingly common program with zero `.wrap`s.

- Added the `has_any_wrap_chains()` prefilter to the slow path (`class_dispatch.rs`) and the
  `nextsame` fall-through (`builtins_dispatch_next.rs`) — the compiled path already had it
  (`vm_call_method_compiled.rs`).
- `find_method_candidate_index` itself now tries `Arc::ptr_eq` on the body first (a resolved
  MethodDef is a clone of a registry entry, so pointer identity finds the candidate without
  touching the AST); the fingerprint scan survives only as a fallback for defs rebuilt with a
  fresh body Arc.

ctor microbench (release, x5000): 0.70s → **0.35s** (raku 0.12s — 35x → 2.9x over the whole
session); full fez populate (7657 dists, together with #4559's subrule memoization landed in
parallel) 11.9s → **6.5s**. Session total: populate went from the "3-5 minutes" frontier
description to 6.5s.

## 2026-07-15: `--> Nil` returns no longer pay a full string EVAL — zef ctor 6x, fez populate 19.5s → 11.9s

Follow-up to the accessor-MRO fix below, attacking the next populate hot spot (PLAN §1 B2
frontier). Profiling an 18-attribute constructor microbench (`Zef::Distribution.new` shape,
`tmp/ctor-bench.raku`) showed **the whole `HashMap<String, ClassDef>` registry being cloned
twice per constructor** plus `eval_eval_string` in the flat profile. The chain:
`finalize_return_with_spec` on a routine declared `--> Nil` (a *definite* return spec, e.g.
zef's `submethod TWEAK(... --> Nil)`) evaluated the spec by **string-EVALing `"Nil"` on
every return**, and `eval_eval_string`'s setup snapshots the entire class registry (x2).

Fix: `evaluate_definite_return_value` now constructs constant specs directly — `Nil` /
`True` / `False` / `Empty` / `pi` / integer literals — and only falls back to EVAL for
genuinely expression-shaped specs (verified byte-identical values vs raku; note raku
rejects `--> -3` at compile time, a pre-existing mutsu laxness left untouched).

Numbers (release): ctor microbench x5000 = 4.23s → **0.7s** (6x; 35x → 5.8x vs raku's
0.12s); full fez populate (7657 dists) 19.5s → **11.9s**; short-name index parity holds
(9260/9260 = raku). Remaining ctor cost: `bless` on the interpreter slow path, TWEAK x2
via the resolver path, allocation churn.

## 2026-07-15: child-class attribute accessors now shadow parent methods (MRO-level accessor dispatch) — zef populate 3-5min → `zef info Zef` ~50s

An mzef-campaign session (PLAN §1 B2) that turned out to be a general **method-dispatch
correctness bug**: mutsu treated "user-defined method beats auto-generated accessor" as a
global rule, so a **parent class's explicit method shadowed a child class's `has $.x`
accessor** of the same name. In Raku the accessor is an ordinary method of its declaring
class and participates in the MRO like any other method (child accessor wins).

- **Impact on zef**: `Zef::Distribution has $.name`, but its parent
  `Zef::Distribution::DependencySpecification` declares `method name` = spec-parts =
  `Zef::Identity.new` = a full REQUIRE identity-grammar parse (~4ms/call, plus `.Str`/`.trans`
  churn). Every `.name` read during Ecosystems populate paid a grammar parse. This — not the
  suspected "plain Named subrule per-call cost" — was the bulk of the populate slowdown.
- **Fix**: new `resolve_user_method_or_accessor` (class_introspection.rs) decides the winner
  **per MRO level**: explicit class-local method > public attribute accessor > role-composed
  method (class prioritization, 6.c S14-roles/attributes.t), walking the MRO once so a
  more-derived accessor beats a less-derived method. Applied at 4 dispatch sites: the
  interpreter instance-dispatch gate (methods_instance_ops), the fast accessor read
  (vm_call_method_ops — the shadowing case now takes the *fast* attribute read), the
  compiled-path defer check (vm_call_method_compiled_interpret, prefiltered to
  role-origin/ancestor-owned resolutions so the hot path stays cheap), and the lvalue
  assignment path (methods_mut_method_lvalue — a shadowing `is rw` accessor is writable).
  Replaced the old `has_class_local_method` MRO-blind predicate.
- **Numbers** (release, real fez index 7648 dists): `.name` micro-stage 1.31s → 0.02s (300
  dists, 65x); full fez populate ~60s → **19.5s** (raku 2.8s); warm `zef info Zef`
  end-to-end **~50s** (raku warm ~3.8s). Bonus: the short-name index now has **9259/9259
  keys, exactly matching raku** — the former 3-key diff was the same bug (dists whose
  identity fails to parse lost their `.name`).
- pin = `t/accessor-mro-shadowing.t` (9 cases: shadowing both directions, same-level
  method-suppresses-accessor, role-vs-class prioritization both levels, rw write path,
  deep hierarchy).

Remaining populate hot spots (per dist): `bless` on the `run_instance_method` slow path,
TWEAK x2 via the resolver path, allocation churn — see PLAN §1 B2.
## 2026-07-15: CompUnit `need` protocol + multi-param `is copy` fix — integration/precompiled.t whitelisted

The "precomp infrastructure" ledger item turned out to be four small protocol
gaps plus one general compiler bug (whitelist 1410/1463; with this and
advent2011-day07.t below, both remaining cluster-⑤ items are done):

- `$*REPO.precomp-repository` returns a `CompUnit::PrecompilationRepository::
  Default` object (mutsu loads from source; the object exists so precomp-aware
  callers like roast's `Test::Compile` can thread it through `need`).
- `nqp::bindattr($obj, Type, '$!attr', $v)` joins the minimal nqp shim —
  writes the attribute cell directly via `insert_through`/`commit_attrs`.
- `PROCESS::<$REPO> := $repo` (the *bind* form) no longer stores the raw
  `__mutsu_bind_index_value` marker Pair as the dynamic — the pseudo-stash
  assignment path unwraps it.
- **Compiler bug**: per-param `is copy` / `is rw` traits on MULTI-param pointy
  for-loops were read only from the single-param def, so `-> $x is copy, $fn`
  marked BOTH params readonly and the first write died with
  X::Assignment::RO (this was what actually aborted precompiled.t's chained
  compunit loads). Traits are now honored per param: `is copy` params are
  assignable without writeback, `is rw` params write back (matches raku),
  plain siblings stay readonly.

`CompUnit::Repository::FileSystem.need` (from the zef campaign) already did
the real loading. Pins: `t/compunit-need-protocol.t`,
`t/for-multiparam-copy-rw.t`.

## 2026-07-15: custom grammar metaclasses (Metamodel::GrammarHOW + EXPORTHOW) — advent2011-day07.t whitelisted

The last "major metamodel work" item of the integration cluster:
`Advent::GrammarProfiler` (a grammar profiler that swaps in a custom metaclass
via `EXPORTHOW.WHO.<grammar> = ProfiledGrammarHOW`) now works end to end, and
`integration/advent2011-day07.t` passes (whitelist 1409/1463). Four general
mechanisms, no test-specific hacks:

1. **`Metamodel::GrammarHOW` is inheritable** (added to the builtin parent
   list next to `Metamodel::ClassHOW`), and `callsame` from a user HOW method
   now reaches the **native** metamodel implementation once the user MRO is
   exhausted: `run_instance_method`/`push_method_dispatch_frame` record a
   metamodel dispatch context (gated by a registry flag so ordinary programs
   pay one bool read), and `dispatch_next_candidate` serves the default
   `find_method` (via `classhow_find_method`) / `publish_method_cache` as the
   final candidate.
2. **EXPORTHOW installation**: a `grammar` declared while an
   `EXPORTHOW::<grammar>` stash entry is visible gets an instance of that HOW
   (`install_custom_grammar_how`) — recorded as its `.HOW` and, when the HOW
   class defines a user `find_method`, in `Registry::grammar_custom_how`.
3. **Token methods are callable**: `Routine { is_regex: true }` stubs (what
   `^find_method` returns for a token) called as `$meth($cursor)` run the
   token anchored at the cursor position and return a Match
   (`try_call_token_method_value`, hooked into both `call_sub_value` and the
   VM's `vm_call_on_value`).
4. **Subrule dispatch routes through the custom HOW**: the regex engine's
   named-subrule path consults `grammar_custom_how` and calls
   `find_method(TypeObj, name)` in a scratch interpreter; a returned wrapper
   closure is invoked with a cursor and its Match converted back into an
   engine candidate (inner captures recovered via a thread-local side
   channel). Module-level `our %timing` mutations inside the wrapper persist
   because env values are shared cells.

Pin: `t/exporthow-grammar-how.t`. Known simplifications (TODO comments in
code): EXPORTHOW is approximated as globally visible rather than lexically
scoped, and left-recursive rules bypass the LR seed-growing loop on the
custom-HOW path.

## 2026-07-15: TODO_roast consolidated — S*.md retired, BLOCKERS.md is the single ledger

The 17 per-synopsis `TODO_roast/S*.md` checklists were retired and merged into
`TODO_roast/BLOCKERS.md`. They had drifted badly (983 unchecked entries vs the actual 55
non-whitelisted files) and duplicated `roast-whitelist.txt`. BLOCKERS.md now carries a complete
per-file inventory of every non-whitelisted test (integration/6.c/APPENDICES/MISC + the S\*
table), with the still-open S\*.md investigation notes carried over. The completed cluster
diagnostics below are archived here from BLOCKERS.md.

### Archived: breakdown of integration cluster ① (stack-overflow aborts; all 4 root-caused 2026-07-14)

The initial diagnosis "deep recursion overflows the stack" was **wrong**. The aborts look
identical but the causes are distinct:

| File | Actual cause | Status |
|---|---|---|
| `99problems-41-to-50.t` | **Infinite recursion.** A closure's frame env was a scoped child of the *caller's* env, and the captured env was merged with "don't insert if already present", so a caller lexical of the same name shadowed the callee's own capture (i.e. lexical scoping degenerated into dynamic scoping). The P46 actions build closures that each hold their own `my @args` per clause, so an inner clause read the outer `@args` (which contained itself) and self-recursed | **Fixed in #4510.** P41 and P46 pass. Next blocker is P47's parameterized `multi rule expr($p)` |
| `99problems-51-to-60.t` | **Infinite recursion** (P57). `CallMethodMut` unconditionally executed `locals[slot] = env[name]` on every method call on a named receiver, but the callee's env is "flattened caller + its own writes" and **parameters exist only in slots**. So when the receiver had not been rebound, the caller's same-named variable overwrote the callee's parameter, and in the self-recursive `add-to-tree($tree[1], …)` the `$tree` rolled back to the caller's node and never reached a leaf | **Fixed in #4516** (write back only when actually rebound). All 37 tests run to completion |
| `man-or-boy.t` | **Infinite recursion.** The remainder of the same bug as #4510: "a same-named lexical in the calling frame shadows the closure's own capture". This time the shadowed variables are **`&x1..&x5` (A's `&`-sigil parameters)**, which are not in `authoritative_free_vars` and therefore never overwrite-installed. Details below | **Fixed and whitelisted** (10/10). (a) Added `GetCodeVar`/`CallOnCodeVar` to the free-var scan (the constants are sigil-less `"x1"` while the lexical is `&x1`, so record them with the `&` prefixed), (b) exempted `&` names from the rw-arg sink exclusion (`own_call_arg_sources`) (raku rejects `is rw` on `&` parameters, so no rebind path exists) |
| `deep-recursion-initing-native-array.t` | **Not a bug.** It really does need ~20,000 levels of nesting, but **it passes in release from the start** (7 seconds, 3/3 stable). It only looked like an overflow because this table's initial measurement used a **debug build** (debug uses several times the native stack per frame; release's limit is 20k–40k levels, and the required 20k just barely fits) | **Added to the whitelist.** Neither stack growth nor a depth guard was needed |

Lesson: **the shape of the abort (`stack overflow`) says nothing about the cause.** Count the
depth and compare against raku first, to separate infinite recursion from finite deep recursion
(you can tell instantly when `raku` finishes in 8 calls while mutsu emits a 1900-line trace).
3 of the 4 were infinite recursion, and the remaining one was a **measurement error**.

A second lesson: **the ① measurements were taken with a debug build.** Debug consumes several
times the native stack per Raku frame compared with release, and
`deep-recursion-initing-native-array.t` (~20,000 levels) overflowed only in debug. CI uses
release, so **any failure where stack depth is suspect must be confirmed with a release build**
(the same point as the "Delegate the full roast run to CI" section of CLAUDE.md).

The decisive probe when suspecting a dual-store (locals ↔ env) bug: **read the same variable
twice within a single expression**.

```raku
sub f($tree, $d) {
    say $tree.WHAT.^name ~ ' | ' ~ $tree.WHAT.^name;
    return if $d >= 1;
    f($tree[1], $d + 1);
}
f([3, Any, Any], 0);
# raku:  Array | Array  /  Any | Any
# mutsu before the fix:    Any | Array   ← first read correct; second rolls back to the caller's value
```

### Archived: man-or-boy diagnosis (2026-07-14, measured after #4516)

Tracking the identity of each `&x` with `.WHICH` (`Sub|ID`) pinpoints the divergence point.
**The probe must not *call* `&x`** (`x4('TAG')` would fire the B closure and change the execution
itself). Even if you give it a name like `sub bee`, mutsu returns an empty `&x.name` (a separate
small gap), so use `.WHICH` for identification.

```
A#1 k=4 d=0 B=Sub|33  x=[27 28 29 30 31]    <- the original 5 literals
A#2 k=3 d=1 B=Sub|41  x=[33 27 28 29 30]    <- A#1's B fired from inside A#1 = correct
...
A#5 k=0 d=4 B=Sub|65  x=[57 49 41 33 27]    <- k<=0, so it calls x4 (=Sub|33 = A#1's B)
A#6 k=2 d=1 B=Sub|73  x=[33 57 49 41 33]    <- expected [33 27 28 29 30]
```

A#6 is "A#1's B fired **from inside A#5**". B's body is
`A(--$k, $B, &x1, &x2, &x3, &x4, ...)`, so `&x1..&x4` must be read from **A#1's captured env**
(= 27,28,29,30), but mutsu reads `&x1..&x4` from **the calling frame A#5** (= 57,49,41,33).
Invocations fired from inside A#1 (A#2) happen to have a coinciding chain and thus look correct;
**it breaks the moment B fires from a different frame**.

This is the same bug as the shadowing fixed in #4510. #4510 overwrite-installs only "plain
lexicals the creator does not modify after capture" (`CompiledCode::authoritative_free_vars`);
`&x1..&x5` are not in that set, fall into the don't-overwrite path, and lose to the caller's
same-named parameters.

**Resolution (2026-07-15)**: the cause was two layers deep, and both had to be fixed (which is
why experiments fixing only one of them "did not work"):

1. **Free-var scan omission**: reads of `&x1` go through `GetCodeVar`/`CallOnCodeVar`, whose
   constants are the sigil-less `"x1"`. They are not included in `op_name_const_idx` (the
   GetGlobal family), so they never entered `free_var_syms` at all, and the capture filter
   dropped them (`&x1` is a plain user lexical, so the system-name rules would not keep it
   either) — **they were not even captured** = resolved purely via dynamic scope.
   → The scan now records them under the `&`-prefixed key `&x1`.
2. **rw-arg sink exclusion**: A forwards `&x1..&x5` as arguments in `x4($k, &x1, …)`, so
   `own_call_arg_sources` rejected the vouch. But **raku rejects `is rw` on `&`-sigil
   parameters** ("Can only use 'is rw' on a scalar ('$' sigil) parameter"), so per spec there
   is no path for a `&` argument to be rebound by the callee → exempt `&` names.
   (Rebinding via `is raw` theoretically remains, accepted as a known gap. A direct `&f = …`
   rebind is a name-write and is captured as before.)

**Remaining gaps also resolved (follow-up commits in the same PR)**:

- **Bare calls `x()`**: the `CallFunc`/`ExecCall` family record the read only as a callee name,
  so it was outside the free-var scan. Registering every callee name would bloat
  `free_var_syms` with `&say` etc. (paying a `free_at_entry` lookup per closure call), so only
  callee names that match **the set of enclosing `&`-sigil lexicals visible at the definition
  point** (`CompiledCode::outer_code_var_names`, propagated transitively from the parent's
  `local_map` by `compile_closure_body`/`compile_sub_body`) are recorded as `&name` free vars.
  Zero bloat, lexically precise.
- **Authority loss across multiple nesting levels** (a pre-existing bug that hit scalars too):
  the vouch was burned only onto "direct children of the creator", so the inner closure in
  `sub mk($x) { my $mid = sub { sub { $x } }; $mid.() }` could not be vouched by the
  intermediate frame (`$x` is not the intermediate's local) and was shadowed again by the
  caller's same-named lexical. Since the vouch already guarantees "the entire subtree performs
  no writes" (nested free-var writes fold up to the creator), this was resolved by
  **propagating authority down to descendants as long as no intermediate closure redeclares the
  same name** (`propagate_authoritative_down`).

### Archived: breakdown of integration cluster ② (unparseable files; cluster complete — #4511 / #4514 / #4515 / #4517 / #4518 / #4522)

**Reached the whitelist (7 files)**: `advent2010-day11.t`, `advent2013-day04.t`,
`advent2014-day16.t` (#4511: `qto`/`qqto` fused adverbs, whitespace/newlines between the quote
word and the delimiter, the starting line of the heredoc body, selective adverbs such as
`q:to:c`, multi-argument `for` over a lazy gather Seq) / `advent2009-day16.t`,
`advent2009-day23.t` (#4514: `when` as a statement modifier, smartmatch with a Match on the RHS,
`(with …)`/`(given …)` as expressions, `my@i` declarations without whitespace) /
`advent2012-day04.t` (#4515's feed continuation lines and lazy `for` over an infinite closure
sequence, #4517's looser-than-comma operator precedence, #4518's destructuring signature + grep
binder) / `advent2012-day19.t` (#4522) / `6.c/APPENDICES/A04-experimental/01-misc.t` (#4523:
made `^find_method`/`^lookup` resolvable on concrete values, coercion types' implicit source now
`Any`, `Date`/`DateTime` `.IO` throws on type objects).

The last one, `advent2012-day19.t`, passed all 10 subtests in #4522. The problem was that
**user-defined operators with precedence traits** (`sub postfix:<k> is tighter(&infix:<*>)`)
were never applied to terms: the postfix loop in `postfix/loop_.rs` skipped postfixes whose
registered precedence is below `PREC_PREFIX` as "picked up at the prefix layer", but that
pick-up loop existed **only inside the branch for user-defined prefix operators**, so on a bare
term like `4.7` nothing consumed them. The file also required `4kΩ` (a chain of postfixes at
different levels) and `4.7kΩ ± 5%` (an infix looser than both) = precedence climbing that
applies user-defined operators according to the precedence table. #4522 implemented that, plus
chains of alphabetic postfixes (`4kVW`), the `registry.rs` bug where
`resolve_op_precedence` resolved `is looser(&postfix:<k>)` as "`PREC_PREFIX` - 5" instead of
"registered level - 5", `Mu.ACCEPTS`, and `Range.new`.

## 2026-07-15: J4d rounds 4–6 complete — gate re-judged, ADR-0004 closed (#4534/#4537/#4540)

Executed the remainder of J4d as 3 profile-driven slices and **completed and closed the ADR-0004 phase plan**.

- **Round 4 (#4534): FxHash-ify the dispatch tables + fuse the argument scan**. ~11% of the
  fib(28) after-profile was SipHash (the Symbol-keyed light-call cache + the String-keyed
  compiled_fns table — probed on every call). Introduced the `CompiledFns` type alias
  (FxHashMap, 84 sites) + converted the Interpreter's 5 dispatch caches to FxHashMap +
  single-passed the junction/callsite-marker scan on the pos-light hit path (1 nanbox decode
  per arg). fib(28) 0.19-0.20 → 0.17s (-12%).
- **Round 5 (#4537): caller-env frame reuse for light calls (CoW write-latch scheme)**.
  The post-round-4 profile showed ~26% of `positional_light` self was the env ceremony
  (clone → `scoped_child` → swap → drop) as `lock inc/dec` pairs of Arc refcounting. If the
  caller's overlay is still the process-shared empty singleton (`overlay_is_shared_empty`),
  installing a fresh overlay is observationally an identity, so the env is used as-is.
  Soundness comes not from static analysis but from a **dynamic latch**: the singleton doubles
  as a write detector (the first by-name write un-shares via `cow_mut`'s CoW), and unwind sees
  the latch fire and replays the scoped-overlay return merge in place (`retain_overlay`).
  Unlike the static overlay-skip rejected in round 3, this cannot go flaky.
  Plus: turned the GC call-safepoint's `PENDING.swap` (a locked RMW per call) into
  test-and-test-and-set. fib(28) 0.16-0.17 → 0.13-0.14s (-19%).
- **Round 6 (#4540): readonly-set Arc snapshot → mutation journal**. Unified the **dual
  mechanism** — the per-frame `readonly_vars` snapshot (Arc clone+drop = ~15% of
  `positional_light` self) and the hand-written delta lists for light method frames
  (`readonly_added`/`readonly_removed`) — **into a single journal**. mark/unmark record only
  real changes, and `exit_readonly_frame` replays them in reverse. A Scope sentinel confines
  the cancellation peephole to within a single frame (eating an outer frame's entry would let
  an `is copy` param destroy the caller's readonly — caught during development by
  t/copy-param-shadows-caller-readonly.t). At top level journaling is off = no lifetime growth.
  fib(28) cycles ~526M → ~494M (-6%).

**bench CI measurements** (ratio = vs raku, the source of truth): fib+jit **0.34 → 0.28**
(`d6d405cc` → `c397b90b`), bench-fib+jit **0.66 → 0.56**. Local cumulative = fib(28) pre-J4d
~0.4s → **0.13s**.

**Re-judging the J4 "int loop 5-10x" gate** (ADR-0004 addendum, 2026-07-15): the int-arith
on/off ratio is 1.24x, still short of the letter of the gate, but that expectation used the
pre-J4c/J4d interpreter as the denominator. Reducing shared costs (dispatch, call ceremony)
speeds up both series at once, so the ratio can never get there in principle; in absolute
performance, int-arith+jit = **0.18x of raku (≈5.5x faster)**. Judged that "native-class
execution of tight numeric loops" is achieved in absolute terms, and closed the ratio gate
as obsolete.

**Design-evaluation record**: former PLAN item (1), "per-call-site inline cache on the CallFunc
opcode", was rejected (after FxHash-ification the double probe is ~10-15ns/call with ~3%
remaining gain, against which the Arc-ification + map-id machinery is oversized). Former
item (2), "direct JIT→JIT calls", also got no dedicated path; the equivalent gain was
recovered by cutting the fixed costs of the light-call path shared by interpreter and JIT
(rounds 5 and 6) — on the profile, call ceremony was more than 3x the dispatch probe. The
root fix for the remaining on/off-shared fixed costs (SetLocal env-mirror etc.) belongs to
the §6 lexical-slot campaign.
## 2026-07-15: ctor dispatch/identity campaign — constructed-object cell threading (#4571 / #4573 / #4575 / #4576)

Continuation of the mzef-populate constructor work (#4557/#4558/#4561/#4566/#4569).
bench-ctor 0.341s → 0.299s (**-12.5%** local A/B, pinned core) plus a raku-compatibility
fix family:

- **#4571** — single-visible-candidate fast return in `resolve_method_with_owner_impl`:
  with exactly one visible non-multi candidate the outcome is invariant to the
  speculative argument match (a failed match falls through to the same def), so the
  match (env snapshot + speculative binding) is skipped entirely; excluded when a param
  carries `where`/a type constraint/a sub-signature. Also gated the merge-path
  `attr_alias_local` scan (O(env keys x attrs) per full-path method exit) on the
  precomputed `has_attr_aliases`.
- **#4573** — **BUILD/TWEAK run against the constructed object itself**: the phases now
  thread the instance's shared attribute cell instead of building a phantom intermediate
  instance per step (2 AttrMap clones + an exit `to_map()` + a spurious DESTROY queue
  entry each). raku semantics restored: `self` inside BUILD/TWEAK IS the returned object
  (identity, and two-way visibility of mutations through a stored `self`). Supporting
  refactor: `call_compiled_method` takes `&AttrMap`; `run_instance_method` /
  `run_resolved_method_compiled_or_treewalk` split into cell-authoritative `_celled`
  cores returning `(value, Option<AttrMap>)` (`None` = the live cell is authoritative)
  plus map-in/map-out compat wrappers. Pin: t/ctor-self-identity.t.
- **#4575** — attribute writes reach the instance after `$outer := self`: the bind
  rewrites the frame's `self` into the bind's ContainerRef cell, and the write paths
  (the `$!x =` type-object guard, `self_instance_attrs`) matched only Instance/Mixin —
  a scalar assignment died with "Cannot look up attributes in a ... type object" and an
  array `.push` was silently lost. Both now read through the ContainerRef. Pin:
  t/bind-self-attr-write.t.
- **#4576** — the legacy full-`.new` path (non-native-eligible classes) delegates its
  ~200-line inline BUILD/TWEAK MRO walks to the shared celled phases ("1 operation = 1
  implementation", -195/+48 lines), extending the identity fix to those classes; and
  `self_instance_attrs` unwraps ContainerRef/Mixin levels **iteratively without holding
  the cell lock** (capped at 8) — #4575's recursion inside `with_deref` held the Mutex
  across levels, the suspected trigger of its jit-stress SEGV (S17-supply/syntax.t,
  Signal 11; 28 hardened-build runs under the CI's exact jit-stress conditions show no
  SEGV, only the documented S17 load-flaky shape once).

## 2026-07-15: BLOCKERS cluster 5 (individual feature gaps) — 8 files whitelisted, 2 remain

Whitelisted 8 of the 10 individual-feature-gap files under `integration/` in one day (#4533 /
#4535 / #4536 / #4538 / #4541 / #4542). Each fix is an improvement to a general mechanism, not
a one-off:

- **`advent2010-day22.t` / `advent2009-day20.t` / `advent2009-day18.t` (#4533)** —
  `.^attributes` on builtin types (a model table for `Rat`'s `$!numerator`/`$!denominator`
  etc.) and non-empty `.^methods(:local)` (the list of declared own methods); `signature.raku`
  literal default rendering (`$blah = 5`), sigil-derived `Positional`/`Associative`/`Callable`
  param types (`Int @x` is `Positional[Int]`), deferring stub `{...}` bodies' map callbacks
  until forced; `my Cup of EggNog $mug` keeping its type argument in `.WHAT`/`.raku`
  (`SetVarType` instantiates the ParametricRole, `nominal_type_object_name_for_constraint`
  does not cut at `[`, and ParametricRole gained a raku/gist repr).
- **`advent2010-day14.t` (#4535)** — the real issue was not `nextsame`: an I/O-only method
  like `method m { say "y" }` was misclassified as `can_skip_merge=true` because the
  `Say`/`Put`/`Print`/`Note` opcodes were missing from `has_calls`, so method exit restored a
  stale env snapshot and discarded the captured-outer write of a user `$*OUT` override. Added
  the 4 ops to `has_calls`.
- **`advent2013-day10.t` (#4536)** — `attach_trailing_adverbs`, which attaches an operator
  adverb to the "operator at the expression root" (`1+2-3 :adv` gets the outermost `-`;
  right-associative `1**2**3 :adv` gets the leftmost `**`); postfix space-form colonpair grabs
  are held back inside a user prefix's operand and attached to the prefix call (parentheses
  reset the context); user infix overrides of `- * / **` now dispatch through the VM just like
  `+` (previously they silently ran the native op); Empty operands of operator calls are
  preserved; `m:nth[5]` (bracketed adverb argument) is rejected with X::Comp::Group.
- **`advent2011-day04.t` (#4538)** — **recursive calls of a wrapped sub now re-enter the wrap
  chain** (the `is Cached` memoization idiom caches every recursion level). Replaced the broad
  `is_wrap_dispatching` guard with a callsame/callwith-only one-shot `wrap_skip_once`, and the
  wrappee returned by `nextcallee` always executes directly via the `__mutsu_wrap_direct`
  marker (fixed forward the infinite recursion in defer-call.t that CI uncovered).
- **`advent2011-day10.t` (#4541)** — `--doc` renders pod blocks before declarator blocks in
  source order and renders `=item` as `  * `; `use Pod::To::Text` is recognized as a builtin
  module (JSON::Fast style), with a native `pod2text` that actually renders the Pod object tree.
- **`advent2009-day24.t` (#4542)** — when a derived grammar added proto candidates, **all
  inherited candidates were wiped out** (the MRO walk stopped at the first scope with a hit).
  Unified the 3 walks of token def/pattern resolution into "merge candidates from all classes,
  dedupe by candidate identity (`name:sym<X>`) preferring the most derived".

The remaining 2 are `advent2011-day07.t` (`Metamodel::GrammarHOW` inheritance = major
metamodel work) and `precompiled.t` (precomp infrastructure).

## 2026-07-15: J4d round 3 — making the light-call ceremony alloc-free (#4529)

Even after round 2, fib was ~50% per-call ceremony. Three cuts that leave soundness untouched:

- **`Env::scoped_child` made steady-state alloc-free**: it used to pay 2 `Arc`s per call
  (fresh allocation of an empty overlay map + cloning the walked-to parent Env and re-wrapping
  it in an Arc); now the empty-tier walk reuses the existing parent `Arc` directly, and the
  empty overlay is a process-shared singleton (on first insert, `cow_mut`'s `Arc::make_mut`
  duplicates it via CoW = behavior unchanged). `ptr_eq` consumers are unaffected too (shared
  singleton = both empty = the "no merge needed" verdict is correct either way).
- **Skipping the param-slot seed**: `positional_light` seeded every local from the env, but
  param slots are unconditionally overwritten by the bind immediately after (arity shortfall
  already early-returns before the seed) = pure waste. In an all-params body like fib, the
  seed (a per-call overlay-chain hash walk) disappears entirely.
- **Pool reuse of the args Vec**: the pos-light call site's
  `stack.drain(..).collect::<Vec>()` (a malloc/free per call on the hottest path) now borrows
  from and recycles into the locals pool.

Cumulative measurements (local release, vs pre-J4d main): fib(28) JIT on ~0.4s → **0.23s**,
3M-iter Num loop 1.09s → **0.39s (-64%)**. fib profile: `positional_light` 19.3→12.8%,
`scoped_child`/seed walk gone. Remainder = the body of `exec_call_func_op` (cache probe,
junction scan, `peek_callsite_line`) = candidates for follow-up slices.

## 2026-07-15: J4d round 2 — removing the per-store intern chain from the SetLocal mirror (#4528)

Profiling after round 1 (#4527) showed the remaining cost of the hot Num loop was not dispatch
but **the SetLocal env-mirror chain re-interning the name on every store** (`Symbol::intern`'s
TLS alone was ~29%). Three fixes, reusing existing machinery only:

- **SetLocal mirror for plain lexicals**: the unconditional env mirror in
  `exec_set_local_op_inner` now goes through `set_env_plain_lexical` (the same
  1-Symbol-insert writer as `flush_local_to_env`) with the pre-interned `locals_sym` when the
  name is in `plain_locals` (no sigil/twigil/qualifier = the overwhelming majority of scalar
  SetLocals). The full mirror's alias branches (`Main::` probe + `format!`, twigil pairs,
  `&infix` aliases) are unreachable for this name class (applying the #4507 analysis to the
  statement-assignment mirror as well).
- **`CheckReadOnly` on a pre-interned probe**: every whole-variable assignment used to intern
  the name via `check_readonly_for_modify(name)`; now it is
  `is_readonly_sym(code.const_sym(idx))`. Error construction is kept on the cold side.
- **Hoisting the for-loop topic/param intern**: `exec_for_loop_int_range` was doing
  `"_".to_string()` + `Env::insert` (which interns internally) **every iteration**; the
  topic/param/caret-alias Symbols are now interned once before the loop and the body does only
  `insert_sym`.

Measured (local release, 3M-iter Num loop, this slice alone vs pre-J4d main): JIT on
1.09s → **0.53s**, **JIT off (interpreter baseline) 0.92s → 0.70s** — the mirror chain runs on
every SetLocal regardless of JIT, so unlike round 1 the interpreter side also gets -24% faster.
Composes with #4527.

## 2026-07-15: layer-4 JIT J4d round 1 — Tier B GetLocal inlining + lock removal from hot-loop entry (#4527)

The first slice of J4d (ADR-0004, Tier B variable-op inlining). Started only after profiling
JIT-on fib / Num loops with perf (lesson = following the measurement protocol of ADR-0006
"opcode count ≠ time"):

- **Per-iteration lock removal from hot-loop entry**: `try_enter_range` was paying a mutex
  lock + linear scan + `Arc` clone on **every iteration** of compound loops (11.9% of the hot
  Num loop). Settled ranges (compiled fn ptr / bailout) are now published to a write-once
  lock-free `(key, entry)` cache on `JitCodeState` (4 slots), making the steady state 2 atomic
  loads. Overflowing ranges stay on the legacy mutex path (correctness unchanged, just slower).
  11.9% → 3.1%.
- **Tier B inline GetLocal**: GetLocals in JITted bodies always went through the Tier A shim,
  re-running the guard chain of `exec_get_local_op` — including **the env `get_sym`
  cell-adoption probe (a hash lookup) on every read**. When there is no dynamic spoiler and
  the slot word is a refcount-free scalar (small Int / Num / Bool / Package), it now expands
  directly to 2 loads + 1 store. Soundness is guaranteed not by static analysis of user code
  but by a **monotonic latch** (the USER_INFIX_DECLS pattern): `CONTAINER_CELLS` (bumped at
  the single `Kind::ContainerRef` chokepoint of NaN-box encode — 0 ⟺ no cell exists in the
  process, so the adoption probe is a trivially-true no-op) / `CALLER_VAR_BINDS`
  (`$CALLER::x := ...`) / per-interp `atomic_var_seen`, `sigilless_attrs_active`.
  Attribute slots and `!`/`.`/`@`/`%` names are excluded statically. The latch values appear
  on the jit: line of `MUTSU_VM_STATS`.
- **Measured (local release A/B, same methodology)**: 3M-iter Num loop JIT on
  **1.09s → 0.62s (-43%)**. This shape previously had **JIT on losing to the interpreter**
  (1.09 vs 0.92 — the per-iteration entry overhead ate the nativization gain), now flipped to
  a +33% win. fib(25) 0.136 → ~0.07s. The authoritative ratio comes from the post-merge bench
  CI (bench-data).
- pin = `t/jit-getlocal-fastpath.t` (4 fast-set cases + every spoiler kind: `is rw` cell,
  `:=` alias, boxed Str, Nil declaration check. threshold=2 / JIT off / identical output
  under raku).
- **Remaining (next slices)**: (1) the Num loop's residual cost = the SetLocal env-mirror
  chain (`Symbol::intern`/TLS ~35% — interpreter side); (2) JIT→JIT per-call bind inlining
  (fib profile: `call_compiled_function_positional_light` 19% + `Env::scoped_child`/drop
  ~14%); (3) args Vec alloc removal.

## 2026-07-14: `my $x = ...` no longer pays intern, SipHash, and COW — time-parts vs raku 1.17 → 0.62 (#4506 / #4507 / #4508)

time-parts remained **the only benchmark slower than raku even with JIT on** (ratio 1.17).
Profiling showed the slowness was not in arithmetic but in **the declarations themselves**.
The 5 `my`s in the loop body × 100k iterations — 500k declarations — each executed a fixed
amount of metadata bookkeeping:

| Symptom | Culprit |
|---|---|
| `Symbol::intern` 9.6% | The env is Symbol-keyed, yet the mirror write re-interned the name every time |
| `format!` 4.3% | Every mirror store built `format!("^{name}")` and asked the env whether a placeholder alias existed |
| SipHash 6.7% | Empty `HashMap<String, _>`s (type constraints, `is default`, readonly) probed on every declaration |
| `Env::insert`/`cow_mut` 5.2% | **Removing a nonexistent key** triggered `Arc::make_mut` (= a clone of the whole map if the env was shared) |
| `__memcmp_avx2` 5.9% | The declaration-tracking sets were `String`-keyed (hash + memcmp on match) |

Fixes (all following existing latch machinery — no new mechanisms):

- **#4506** — added a latch for the `^name` placeholder key (`PLACEHOLDER_KEY_SEEN`),
  eliminating the probe itself. Passed the pre-interned Symbol that `flush_local_to_env`
  already had (but discarded as `_name_sym`) all the way through to `set_shared_var_sym`.
  Removed `SetVarDynamic`'s re-intern, the per-iteration `String` allocation, and the
  O(locals) scan that is pointless from the second pass on. `Env::remove_sym` returns before
  `cow_mut()` when the key is absent. Abolished all SipHash probes of empty maps.
- **#4507** — **deleted the ~310-line unreachable `simple_locals` fast path**. The compiler
  set it based on `name.starts_with('$')`, but **scalar locals are stored without the sigil**
  (`my $x` → `"x"`), so it was always false. As `--dump-bytecode` shows with
  `locals: ["*d", "x", "@a", "%h"]`, no local name starts with `$`. Introduced the honest
  `plain_locals` instead (names with no sigil, twigil, `::`, or attribute marker), reducing
  mirror writes whose alias maintenance is unnecessary in principle to a single Symbol insert.
- **#4508** — made the declaration-tracking sets (`block_declared_vars` / `loop_local_vars`)
  Symbol-keyed. Consumers already had Symbols and were round-tripping via
  `sym.with_str(|s| set.contains(s))`, so every consumer got simpler at the same time. Also
  fixed `BlockLocalScope`'s pre-branch env snapshot, which had been **String-ifying every key
  in the env**.

Results (median of 7, same quiet window, raku measured in the same window):

| Configuration | before | after | vs raku |
|---|---|---|---|
| JIT on (default) | 0.255s | **0.136s** | 1.17 → **0.62** |
| interpreter only | 0.238s | 0.205s | 1.46 → **0.93** |

**No benchmark exceeds raku ratio 1.0 anymore** (the interpreter alone got under it too).
No regressions on the other benchmarks.

**Next target** (recorded in PLAN §5): the top of the remaining profile is still the env
mirror, but its root is the `needs_env_sync` blanket (a single `ForLoop` in a frame makes
every local a mirror target). Lifting it requires removing the 3 env name dependencies at
once — `exec_do_block_op` restoration, loop shadow save/restore, and closure capture — a
**campaign unified with §1.3/§1.5**.

## 2026-07-14: PLAN / BLOCKERS inventory — the roast frontier turned out to be `integration/` (41 real programs)

Measuring the items listed in PLAN.md §4 "roast backlog" one by one showed **most were already
implemented** (when BLOCKERS.md was consolidated section-by-section on 2026-07-12, only the
derived list on the PLAN side had been left stale). Verified with `target/debug/mutsu`:

- **Implemented = removed from PLAN**: generalized negation meta (`!before` → `False`) / hyper
  assignment (`@a >>+=>> 1` → `2,3,4`) / `augment class` (MONKEY-TYPING) / namespaced
  construction (`A::B.new`) / file tests (`.IO.e`) / PRE and POST phasers / signature type-check
  enforcement (passing `"str"` to `sub f(Int $x)` throws).
- **lazy-seq item (4) is also resolved**: `is-lazy` preserved after partial reify, `push` to a
  lazy list raising `X::Cannot::Lazy`, slurpy `is-lazy`, laziness preservation through
  `.List`/`.Array` — all 7 probes match raku.
- **What genuinely remains** is only multi-line feed (the form spanning a leading `==>`).

Instead, **leftover work that had fallen entirely out of the BLOCKERS.md table** was found.
79 files are un-whitelisted (1384 / 1463), and the breakdown is not the 24 S* files (= the
BLOCKERS table — all non-goals / un-oracle-able / waiting on infrastructure) but chiefly
**41 `integration/` files + 7 `6.c/` files + 4 APPENDICES files**. Cross-checking against
`TODO_roast/raku-baseline.tsv`, **nearly every un-whitelisted `integration/` file has
`raku_status=PASS` (perfect score under raku) = ★achievable in BLOCKERS' definition** (e.g.
`advent2013-day08.t` mutsu 0/11 vs raku 11/11, `99problems-41-to-50.t` mutsu 1/9 vs raku 9/9,
`error-reporting.t` mutsu 4/33 vs raku 33/33). integration/ consists of Advent Calendar and
99-problems — **real Raku programs** — making it also the compatibility metric closest to the
goal of "real-world code just works".
→ Added a new section "integration — real-program compatibility" to BLOCKERS.md and moved the
main battleground of PLAN §4 there.

## 2026-07-14: 4 slices cutting allocation / hash churn on the call path (from scouting §2, #4492–#4495)

4 slices driven by the profile in
[docs/perf-callpath-scouting.md](../docs/perf-callpath-scouting.md) (bench-fib: malloc/free
11.8%, SipHash 4.1%, `RawTable::clone` 3.7%; bench-class: ~20% in the allocator alone). All
verified with the ADR-0006 measurement protocol (`perf stat -e instructions:u`, release, JIT
on, cores pinned with `taskset`):

- **#4492 allocation-free per-call readonly snapshot and return merge**:
  `save_readonly_vars()` deep-cloned a `HashSet<String>` on every call. Made it
  `Arc<FxHashSet<Symbol>>` (copy-on-write), so save is an `Arc` bump. The exit-time
  construction of the callee-local-names `HashSet<&str>` also became a direct query of
  `declared_locals: FxHashSet<Symbol>`.
  → **bench-fib -32.3% / bench-tak -23.9%** (method-call/poly-call ±0.2% = no added cost).
- **#4493 pre-intern the sigilless-alias / readonly env keys**: every scalar store ran two
  `format!("__mutsu_sigilless_{alias,readonly}::{name}")`s (even in programs using neither
  sigilless nor `:=`). Pre-interned per slot + skipped entirely via the existing monotonic
  flags. → **num-arith -21.6% / bench-mandelbrot -14.9%**.
- **#4494 Symbol-keyed instance attributes (ADR-0006 §2.4)**: `HashMap<String, Value>` →
  `AttrMap` (`FxHashMap<Symbol, Value>`). Via the `AttrKey` trait, the hot path uses `Symbol`
  while the hundreds of cold construction sites keep `&str` literals.
  → **method-call -11.6% / bench-class -2.7%** (removes the 5.2% `__memcmp_avx2` in the profile).
- **#4495 stop rebuilding metadata keys per declaration/store**: `my $t = ...` ran 6
  `format!`s and 4 env probes (speculative clearing of deleted-index / sigilless-readonly /
  alias / bound-slice). Pre-interned per slot and skipped when no marker was ever created.
  `reset_atomic_var_key` also returns immediately when there is not a single atomic variable.
  → **time-parts -37.2% / bench-mandelbrot -33.7% / num-arith -32.1%** (cumulative with #4493).
  ★Side finding: `cas $x` runs in the worker-side interpreter, so a per-interpreter flag misses
  it (added a process-wide companion to `atomic_var_seen`).

Remaining (untouched parts of scouting §2): (1) removing SipHash from `compiled_fns` (still
`HashMap<String, CompiledFunction>`); (2) removing the callsite-line marker
(`peek_callsite_line`); (3) the lexical-scope slot campaign (removing the per-call
materialization of the env itself = the main prize).

## 2026-07-13: baseline optimizations round 5 — retiring `SetSourceLine` for a static ip→line table (ADR-0006 §2.3 complete) + ★the "opcode count ≠ time" lesson

An instruction's line number is **static data fixed at compile time**, and no dispatched
instruction is needed to carry it. Retired `SetSourceLine`, which ran once per statement
(**1.9M of bench-fib's 8.9M executed opcodes = 21%**, 11% on time-parts), moving it to
`CompiledCode::op_lines` (a `Vec<u32>` parallel to `ops`) (#4489).

- **Compiler**: `Stmt::SetLine` emits no opcode; it only advances the emit cursor
  (`set_emit_line`). `emit()` bakes the current line into each instruction. The two sites that
  `ops.insert` — the `SetLocalDecl` fusion (#4488) and the accessor ref marker — update
  `op_lines` in lockstep, with index consistency pinned by `debug_assert`.
- **VM**: `cur_source_line` is pulled via `sync_source_line(code, ip)` **only by instructions
  that can observe the line** (each arm for calls / re-entry into user code / frame push /
  declaration registration / raise sites, plus the JIT call shim). Pure hot ops — arithmetic,
  locals, branches — cost 0.
- **★The most important lesson (measured)**: **the reduction in opcode count does not match the
  reduction in time.** The first naive version refreshed the line for **every instruction** in
  the `exec_one` prologue, which cost more than the dispatch it removed — **+7.8% instructions
  on fib**. Only after restricting to "observation points only" did it become **-3.4%** (fib,
  interpreter path). `SetSourceLine` is the cheapest possible op (one store), and mutsu's
  average op is orders of magnitude heavier, so a 21% opcode reduction is not a 21% time
  reduction. **±0% on the JIT path (the default configuration)**; bytecode size down ~15%.
  → Before tackling the next administrative ops (`SetVarDynamic` 500k, `CheckReadOnly`),
  **first confirm with perf instruction counts that the op actually consumes time**
  (measurement procedure = [ADR-0006 §4](../docs/adr/0006-baseline-interpreter-optimizations.md)).
  A profile retaken the same day (release, JIT on, P-core pinned, `cycles:u`) showed the real
  targets: on bench-fib, **the JIT native code body itself runs only 5.7%**, dominated by
  allocation (`_int_malloc` + `_int_free` 11.8%), env table duplication and hashing
  (`Env::scoped_child` 5.4% / `get_sym` 4.4% / **SipHash 4.1%** /
  `hashbrown RawTable::clone` 3.7%), and the call path (10.9%).
  bench-class is **~20% in the allocator alone** + 5.2% `memcmp` on String-keyed attribute
  names. → Next targets: (1) the env's per-call clone and SipHash (= the lexical slot
  campaign); (2) Symbol-ifying the attribute `HashMap<String,Value>` (recorded with a table in
  PLAN §5).
- **By-product: line numbers actually became more accurate.** The old scheme left the line
  stale after executing a block/loop body, requiring a defensive re-emit shim of
  `SetSourceLine` before method calls (`emit_source_line_if_known`). Deleted that too.
- pin = `t/source-line-table.t` (top level / inside a loop body / after executing an inner
  body / inside an expression containing a `do` block / sub→sub caller line / backtrace line
  of a sub that died — all 7 cases match raku).

## 2026-07-13: baseline optimizations round 4 — fusing declaration sequences (ADR-0006 §2.3 peephole, first half)

`my $x = <expr>` dispatched 3 instructions every time: `MarkExplicitInitializerContext` +
`MarkVarDeclContext` + `SetLocal`. The two markers' only effect is "set a flag that the
`SetLocal` body reads and clears", so they were fused into the single instruction
`SetLocalDecl { slot, explicit_init }` (#4488).

- **Safety**: the fusion rewrites, inside `CompiledCode::emit()`, **only the marker sequence it
  itself just pushed**. Jump targets are recorded only at statement boundaries, and the 3
  instructions are emitted contiguously, so a path that "jumps into the middle of the markers"
  structurally cannot exist (even if a path jumped to the first marker, the fused instruction
  does the same thing as that marker sequence + `SetLocal`, hence equivalent). A post-pass that
  deletes ops would require reindexing jumps/compound ops; this avoids that entirely.
- `compute_free_vars`' "declaration vs reassignment" distinction (keyed on the presence of
  `MarkVarDeclContext`) and the JIT (added the dedicated helper `set_local_decl`) were also
  adapted to the new instruction. The JIT still compiles the hot loop (compiles=1 / bailouts=0).
- **Measured**: time-parts executed opcodes **6.40M→5.40M (−15.6%)**. Together with §2.1,
  7.60M→5.40M (−29%). Wall-clock in release: time-parts 0.63→0.58s (interp), 0.58→0.55s (JIT on).
- pin = `t/decl-marker-fusion.t` (declaration vs reassignment, with/without explicit
  initializer, closure capture. Same results under raku).
- **Next target**: `SetSourceLine` is **1.9M of bench-fib's 8.9M executed opcodes (21%)** and
  600k (11%) on time-parts. The right fix is to delete the opcode and move to a static ip→line
  table (PLAN §5 Lever 7 §2.3-b).

## 2026-07-13: baseline optimizations round 3 — `constant` inlining + constant-condition DCE (ADR-0006 §2.2)

Guarded logging like `constant DEBUG = False; if DEBUG { note ... }` is a frequent pattern in
real code, but mutsu **executed** it every time as `GetBareWord` + `JumpIfFalse` (#4487).

- **Implementation**: track the values of `constant`s whose initializer is a compile-time
  constant scalar and replace reads with `LoadConst` (this propagates to child compilers =
  sub/closure bodies, so a file-level `constant` read from inside a sub is inlined too).
  `if`/`unless` whose condition thereby becomes constant resolve the branch at compile time.
- **Safety design**:
  - **Never delete a dead branch containing a declaration.** Raku installs declarations at
    compile time even if the branch is never taken, so only branches consisting of "plain
    expression statements only" (= the logging-guard shape) may be removed.
  - **No inlining when a non-constant local of the same name exists.** mutsu strips sigils in
    the AST, so `my $DEBUG` and a sigilless `constant DEBUG` land on the same `local_map` key.
    A name in `local_map` but not in `constant_vars_in_scope` = treated as a shadow and falls
    back to normal resolution.
  - Scope lifetime is identical to `constant_vars_in_scope` (a constant that leaves its block
    reverts to an `our`-scoped package symbol and is not an inlining target).
  - Container-valued constants are excluded since their identity is observable (per ADR §2.2).
- **Measured** (release, median of 5): debug-guard executed opcodes **3.00M→2.60M (−13.3%)**,
  with 200k each of `GetBareWord` and `JumpIfFalse` completely gone. Wall-clock:
  **JIT on 0.34s→0.25s (−26%)**, interp 0.32s→0.26s. Ratio vs raku **2.2x → 1.3x**.
- pin = `t/const-inline-dce.t` (16 cases; verified raku produces the same results).

## 2026-07-13: baseline optimizations round 2 — constant pool dedup (ADR-0006 §2.4)

`CompiledCode::add_constant` unconditionally did `constants.push`, so the same name string or
the same literal was duplicated into a separate slot per use site (#4486).

- **Implementation**: added a reverse index (`FxHashMap<ConstKey, u32>`) to `add_constant`.
  Only scalars that are *indistinguishable when equal in value* (Int/Num(bit)/Str/Bool/Rat)
  get keys; values with **observable identity** like containers or Instances get no key and
  always take a new slot. `0.0` and `-0.0` are distinguished by bit pattern (`1/0.0` and
  `1/-0.0` differ, so they are not merged). The index is compile-time scaffolding, discarded
  at `compute_needs_env_sync()` (the chunk-finalization point), so executed code carries no
  extra memory.
- **Measured** (the new `const-pool:` line of `MUTSU_VM_STATS=1`): **29–45% of `add_constant`
  calls share an existing slot** (time-parts 44.8%, roast S06 workout.t 28.9%, t/wrap.t
  37.1%). The constant pool's real size shrinks accordingly, benefiting `CompiledCode` memory
  and cache locality (same direction for mzef's tens-of-thousands-of-dists compile).
- Direct effect on execution speed is small (as anticipated in ADR §2.4).

## 2026-07-13: baseline optimizations round 1 — constant folding (ADR-0006 §2.1)

Implemented **§2.1 constant folding**, the first of the measures adopted in
[ADR-0006](../docs/adr/0006-baseline-interpreter-optimizations.md) (#4485). Pure expressions
consisting only of literals (`60 * 60 * 24`, `'a' ~ 'b'`, `-1`) are evaluated at emit time and
folded into a single `LoadConst`.

- **Implementation**: `src/compiler/const_fold.rs`. Evaluation calls **the same native
  operations as the VM** (`builtins::arith_add/sub/mul/div/mod/pow`,
  `Interpreter::concat_values`) directly, so `Int/Int → Rat`, the BigInt promotion of
  `2 ** 100`, and Num formatting automatically match runtime (no separate "compile-time
  arithmetic" implementation = ADR §2.1-2). Covers arithmetic (numeric operands only), `~`
  concatenation, and numeric comparison (only Int/Int and Num/Num, where the VM has a
  fast path). String operands like `'3' + 4` are not folded, preserving the VM's numeric
  coercion path (which can throw X::Str::Numeric).
- **Operator-override safety condition (ADR §2.1-1)**: `sub infix:<+>` overrides even native
  `Int + Int`, and mutsu registers the declaration in the global `user_declared_infix_ops`,
  so if even one declaration exists anywhere in the unit (nested block, sub body, class body),
  folding is forbidden. A static AST walk was rejected ("miss one spot = mis-fold"); instead,
  **the compile sites a declaration must pass through (`SubDecl`/`ProtoDecl`/`VarDecl`) mark a
  shared `FoldCtx`, and if a declaration shows up after folding has already happened, the
  whole unit is recompiled with folding off**. Structurally sound (no walk can fall behind),
  and the double work is paid only by files that declare operators. Additionally, folding is
  disabled when the JIT's `USER_INFIX_DECLS` (a process-wide counter) is nonzero (covers
  EVAL/blocks compiled after module loading).
  pin = `t/const-fold-infix-override.t` (even a `1 + 2` written before the declaration is not
  folded).
- **Measured** (release, median of 7, local): time-parts executed opcodes
  **7.60M → 6.40M (−15.8%)**, `Mul` 1.0M→0.4M, `LoadConst` 1.7M→1.1M.
  Wall-clock: interp **0.65s → 0.61s (−6.2%)**, JIT on 0.60s → 0.59s (the JIT already folds
  these constant `Mul`s natively, hence the small delta). Time drops less than the opcode
  reduction rate because time-parts' remaining time is dominated by administrative ops
  (`SetSourceLine`, `MarkVarDeclContext`, `SetVarDynamic`, `CheckReadOnly`) — that is ADR §2.3
  peephole territory, the target of subsequent slices.
- debug-guard is unchanged by this slice (reads of `constant DEBUG` = §2.2's job).
- Side effect: folding also shortens the JIT's input opcode sequence (ADR §4).
- `MUTSU_CONST_FOLD=0` disables folding (for A/B and isolation).

## 2026-07-13: layer-4 JIT J5 complete — `MUTSU_JIT` default on (ADR-0004 phase completed)

Achieved **J5 (flip to default on)**, the final phase of ADR-0004 §2.5. mutsu now executes hot
code natively via the Cranelift JIT by default (`MUTSU_JIT=off` is the explicit opt-out).

- **Gate measurements** (bench CI main `f19946ad`, 2026-07-13):
  (1) gc-stress × jit-stress matrix green (standing since J2; now that both are defaults, the
  regular jobs double as the matrix — as anticipated in ADR-0004 §2.6);
  (2) **startup budget unchanged**: bench-startup 0.0087s → +jit 0.0086s (compilation happens
  only for hot chunks past the threshold and cold code is never compiled, so the 0.04x startup
  weapon is not blunted); (3) **every `+jit` series at least matches the interpreter**:
  fib 0.77x→0.59x, bench-fib 1.64x→1.26x, int-arith 0.41x→0.38x, no regressing series.
- **Implementation**: flipped the default of `jit_enabled()` (same shape as `gc_enabled()`:
  `off`/`0` means off, unset = on, unknown values warn and turn on). Unlike GC, no `cfg!(test)`
  exception — JIT state is per-`CompiledCode` (+ a Mutex-protected engine) so cross-talk
  between tests is structurally impossible. pin = `tests/jit_diff.rs::jit_is_on_by_default`.
- **Preserved the meaning of the bench CI series** (`scripts/bench-ci.sh`): the plain series
  now explicitly pins `MUTSU_JIT=off` and remains the interpreter baseline, while the `+jit`
  series is "the default configuration users actually run".
- The jit-stress CI job continues (threshold=2 force-compiles every Tier A chunk = exhaustive
  stress that the default threshold of 100 never reaches).
- The J4 "int loop 5-10x" wording remains unmet at the time of this early flip: J4c made the
  interpreter itself -34% faster, shrinking the denominator, while absolute performance
  improved across all series. The remaining **J4d (Tier B variable-op inlining) continues on
  top of default-on** (noted in ADR-0004).

## 2026-07-13: layer-4 JIT J4 core complete — Tier B inline arithmetic, hot-loop entry, variable-op hot paths (#4478 / #4479 / #4480)

Achieved the core of **J4 (Tier B + hot-loop entry)** from ADR-0004 §2.5 in 3 slices.

- **J4a (#4478) Tier B inline arithmetic**: Add/Sub/Mul and NumLt/Le/Gt/Ge/Eq/Ne are
  **expanded directly into CLIF with NaN-box tag branching** instead of helper calls
  (small-Int⊕small-Int / Num⊕Num fast paths; other word shapes fall back to the existing
  shims). Also inlined at the same time: LoadConst (immediate push of refcount-free
  constants), SetSourceLine (1 store), ContainerizePair (non-Pair skip), and
  JumpIfFalse/JumpIfTrue (direct Bool-word branch + backedge safepoint). Direct stack access
  uses `offset_of!` + a runtime probe of the `Vec<Value>` internal layout
  (`vm_jit_layout.rs`; on failure it automatically degrades to the helper-call form). User
  `infix:<+>` overrides are conservatively guarded by the process-wide monotonic counter
  `USER_INFIX_DECLS`. The inlined portion has **zero** refcount / GC traffic (one wing of the
  J4 gate). pin = `t/jit-tier-b-arith.t` (48-bit boundary, Int mul overflow→BigInt,
  NaN/-0e0/Inf, mixed types) and `t/jit-tier-b-infix-override.t`.
- **J4b (#4479) hot-loop entry**: top-level / compound loops (for/while/loop/repeat) never
  reached the function-level try_enter, so their bodies were always interpreted (int-arith was
  0% JIT). Added **a single hook in `run_range`**, which every compound loop passes through
  per iteration, nativizing hot `[start,end)` sub-ranges via `compile_range` (per-range
  counter + entry cache in `JitCodeState.ranges`). Of the native body's errors, goto/warn
  **rejoin the interpreter mid-stream** from the recorded resume ip (`run_range_from`);
  bodies containing Last/Next/Redo compile-bail and stay interpreted. pin = `t/jit-hot-loop.t`.
- **J4c (#4480) variable-op hot paths (speeds up the interpreter side too)**: the post-J4b
  profile showed the loop body's remaining cost was not arithmetic but variable-op helpers —
  (1) gated `CheckReadOnly`'s `__mutsu_bound::`/`__mutsu_sigilless_readonly::` probes
  (format!×2 + env lookup×2 per assignment) behind process-global "ever created" flags
  (an extension of the existing `CLOSURE_META_KEY_SEEN` chokepoint pattern);
  (2) a fast path for `GetGlobal` via memoized `const_sym` — fixed the fact that **`$_`'s
  name `_` contains an underscore, so the kebab-alias attempt (`_`→`-`) walked the entire
  fallback chain twice every time**; (3) GetLocal/SetLocal's ContainerRef adoption probe now
  goes through `locals_sym` (removing the per-op intern); (4) empty gates for
  `distribute_bound_multidim_slice` / `resolve_binding`.
- **Verification**: for every slice, make test + all of t/ (JIT on, threshold=2) green; roast
  via CI jit-stress (all whitelisted files) green; all bench output byte-identical on/off;
  binding/sigilless/multidim pins green.
- **Gates**: fib loop `gc_candidate_pushes == 0` (both on/off show only the 13 at startup,
  zero loop contribution across 242k JIT entries) ✅ / the "int loop 5-10x" wording remains
  unmet — J4c made **the interpreter itself -34% on int-arith**, shrinking the relative gap
  (the remainder is helper calls like GetLocal/SetLocal = the province of J4d Tier B
  variable-op inlining).
- **Measured (bench CI = bench-data branch, main commit `c8955d2e`, median×7, same-runner
  raku ratio)**: fib **0.82x → JIT on 0.65x** (-21%), bench-fib **1.78x → 1.39x** (-22%),
  int-arith **0.47x → 0.43x** (-27% on the interpreter side alone from the pre-J4c J4b row of
  0.62x), method-call 1.19x → 1.16x, bench-class 1.02x → 1.00x. **bench-startup is 0.06x with
  on/off exactly identical** (compilation only touches hot chunks = effectively passing J5's
  startup-budget gate); benches with no JIT-eligible hot code (array/hash/string families) are
  on/off identical with zero regressions.
- **Added the `+jit` series to bench CI** (#4480, `scripts/bench-ci.sh`): every main push
  records all benches with `MUTSU_JIT=on` under the name `<bench>+jit`. From now on, numbers
  in documents use bench-data as the source of truth (codified in CLAUDE.md).
- Remaining = **J4d**: Tier B variable-op inlining (GetLocal/SetLocal fast paths), JIT→JIT
  per-call bind inlining, args Vec alloc removal → **J5 default on** (the gc-stress×jit-stress
  matrix is already green; only the startup budget and bench non-regression remain to verify).

## 2026-07-13: layer-4 JIT J3 complete — calling-convention optimization, dedicated CallMethod shim, type-check fast path (#4476)

Achieved **J3 (calling convention / IC)** of ADR-0004 §2.5. Killed both findings from the J2
measurements: "method-heavy chunks actually get slower with JIT on (per-op dispatch of the
generic step)" and "per-call bind/env cost is the real body". In the perf profile,
malloc/free-family functions accounted for ~27% of the method-call profile, chiefly caused by
the full `attributes.to_map()` clone on every dispatch.

- **Dedicated JIT shims for CallMethod / CallMethodMut** (same shape as J2's call_func): read
  the payload in-place from code.ops and reproduce resume-point recording, `is rw` writeback,
  pending-local drain, attr-cell snapshot/mirror (Mut), and `current_code` restoration
  verbatim-identically to the dispatch arm. Removes the per-op interpreter dispatch through
  the generic step.
- **`to_map()` removal (the main per-call alloc of the calling convention)**:
  `call_compiled_method_fast` no longer takes an attribute map; it reads the invocant's live
  attr cell under a single read guard. `reconcile_attrs` now returns `Option<HashMap>`
  (Some = adjusted by `:=` recovery), and `call_compiled_method(_fast)` returns
  `(Value, Option<HashMap>)` (the existing semantics — commit only when adjusted — unchanged).
  Adapted the 7 call sites.
- **tag/class fast accept in `type_matches_value`**: exact matches like `Int $n` /
  `Point $other` are immediately true (the case where a user `subset` shadows the same name
  goes to the full checker via a registry guard).
- **Memoized CallFunc's name Symbol in a per-chunk `const_sym` table** (removing 4 global
  interns per call) + conditionalized the current_package save/restore.
- **Measured** (release, perf stat -r5, taskset 0-3, A/B in the same quiet window as the main
  binary, 10–20x scaled benches): method-call's JIT on/off flipped from J2's **-6% → +3%**
  (+3.9% vs main off), bench-fib interpreter **+5.4%** / JIT on **+12.5%**, bench-class ±2%
  (layout-noise territory).
- **Verification**: all of t/ green under both JIT stress (threshold=2) and JIT off; all 1384
  roast whitelist files green with release + JIT on; bench output byte-identical on/off.
- Remaining = **J4 Tier B inlining**: CLIF expansion of NaN-box tag-branch arithmetic,
  hot-loop entry (for-loop bodies have no try_enter hook and remain outside JIT — the
  method-call bench is compiles=1), JIT→JIT per-call bind inlining, args Vec alloc removal.

## 2026-07-12: layer-4 JIT J2 complete — Tier A coverage, hooks on every call path, jit-stress CI (#4474)

Achieved the **J2 gate** of ADR-0004 §2.5: all 1677 t/ files + all 1384 roast whitelist files
green under `MUTSU_JIT=on MUTSU_JIT_THRESHOLD=2` JIT stress, on/off byte-identical output on
all benches, and a **jit-stress job** in CI (same shape as gc-stress; threshold=2, full t/ +
roast run, passing in 10m44s).

- **Entry hooks expanded to all 6 paths**: besides J1's vm_call_light, now vm_call_fast /
  vm_call_light_typed / vm_call_named_inner / call_compiled_method /
  call_compiled_method_fast. Method bodies became JIT-eligible; bench-class (3 chunks, 19k
  entries) / method-call now compile.
- **Expanded dedicated shims (macro-generated)**: Mul/Div/Mod/Pow/Negate,
  NumLe/NumGt/NumGe/NumEq/NumNe, Concat/StrEq/StrNe as direct helpers on par with
  Add/Sub/NumLt.
- **The generic `step` shim (J2's main mechanism)**: ~60 opcodes with complex payloads but
  straight-line behavior (verified per dispatch arm to unconditionally end at ip+1) —
  CallMethod/CallMethodMut/CallOnCodeVar, the variable read/write family, inc/dec, closure
  construction, MakeArray/MakeHash, SinkPop, etc. — are included in chunks via a shared shim
  that runs `exec_one` for exactly 1 step. The arm behaviors (resume_ip recording, rw
  writeback, attr-cell mirror) are reproduced verbatim, and `current_code` is restored after
  re-entrant arms. Whitelist violations trip a debug_assert.
- **Nativized JumpIfTrue / JumpIfNotNil branching** (peek-style cond helpers that do not pop —
  for `||`/`//` short-circuiting. Only JumpIfFalse is pop-style).
- **Measured** (release, perf stat -r5, taskset 0-3): fib 0.138→0.128s (**+7.3%**),
  bench-fib 0.385→0.368s (**+4.2%**) = the dispatch removal. method-call / bench-class are
  flat to -1.7% — their hot opcodes go through the generic step (per-op interpreter dispatch
  remains). Removing that is the territory of the next slices, **J3** (calling convention /
  inline cache) and J4 (Tier B inlining). The ADR's "bench-fib 3.2x→2.5x" gate was the old
  baseline predating the July perf slices (at the current 1.92x, the dispatch share is ~4%).
  JIT remains default off.
- **Opcodes still out of scope** = compound control (WhileLoop/TryCatch/Given/BlockScope
  etc.). J4 hot-loop entry territory.

## 2026-07-12: layer-4 JIT J1 complete — hot Tier A chunks run native on a Cranelift skeleton (#4471)

Achieved the **J1 gate** of ADR-0004 §2.5: with `MUTSU_JIT=on`, fib runs under the JIT
(`jit: compiles=1 entries=242686 bailouts=0`), output is byte-identical on/off, make test
green (on). t/ is also green under the aggressive `MUTSU_JIT_THRESHOLD=1` stress (the only
-j4 failure, `t/io-socket-recv-limit.t`, reproduces identically with JIT off — a pre-existing
parallel-load flake).

- **Approach (ADR-0004 §2.3 Tier A = subroutine threading)**: supported opcodes are
  transliterated into a sequence of `extern "C"` helper-shim calls (`vm_jit_helpers.rs`, each
  faithfully reproducing one arm of `exec_one`). `Jump`/`JumpIfFalse` are native branches;
  backedges get a GC safepoint poll (§2.4). No guard/deopt/OSR — a chunk containing an
  unsupported opcode is a permanent bailout (with a per-reason histogram).
- **Error transport**: shims park a `RuntimeError` (including return signals / fail) in
  `Interpreter::jit_error` and return nonzero status; the entry wrapper feeds it into the same
  match arms as the interp loop — semantics match by construction.
- **Entry splicing**: an `ip==0` hook at the head of the body loop of
  `call_compiled_function_positional_light` (fib's actual path). Via `CompiledCode.jit`
  (AtomicU32 counter + AtomicU64 entry cache, reset on Clone), the per-call cost after
  compilation is a single atomic load. The hot threshold is `MUTSU_JIT_THRESHOLD`
  (default 100 calls).
- **J1-supported opcodes (the fib shape, 12 kinds)**: LoadConst GetLocal SetLocal Add Sub
  NumLt JumpIfFalse Jump ContainerizePair SetSourceLine Return CallFunc.
- **feature `jit`** (default on; inert at runtime until `MUTSU_JIT=on`). The wasm build is
  unaffected via `--no-default-features`. **MSRV 1.92 → 1.93** (required by cranelift 0.132.
  CI main is already on 1.96; raised the release/pages/tagpr workflows to 1.93).
- **Extras**: a `--dump-bytecode` CLI (static disassembly of the mainline + each function),
  vm-stats gained `jit: compiles/entries/bailouts` + a bailout opcode histogram, and
  `tests/jit_diff.rs` (on/off byte-identity differential, reusing the gc-stress methodology).
- **Measured**: release fib(25) 0.14s → 0.13s (raku 0.16s). J1 is a skeleton, so the small
  delta is as expected — the real payoff of dispatch removal comes after J2's coverage
  expansion (gate: bench-fib → a measured improvement equivalent to ~2.5x).
- **Design note**: int-arith is a top-level `ForLoop` compound opcode (the loop spins inside
  the opcode), which function-entry JIT can never catch in principle — as anticipated in
  ADR-0004 §2.2, this is J4 hot-loop entry territory. Next slice = **J2** (Tier A opcode
  coverage + the remaining 3 call paths + a jit-stress CI job).

## 2026-07-12: layer-3b NaN-boxing complete — `Value` is now an 8-byte packed word (#4467/#4469)

Landed the final 2 slices of ADR-0001 layer 3b (ADR-0005 / docs/nanbox-3b1-step-b-design.md),
achieving **`size_of::<Value>()` 48 → 8 bytes** (`Option<Value>` is the same size via the
NonZeroU64 niche). The precondition for starting the JIT (layer 4, ADR-0004) is now in place.

- **#4467 (B-guards)**: switched `ValueView`'s smart-pointer payloads from
  `&'a Arc<T>`/`&'a Gc<T>` to the by-value guard types `ArcRef`/`GcRef`/`WeakGcRef`
  (`RefGuard<'a,P>` = `ManuallyDrop` reconstruction + `Deref`, refcount unchanged). Only the
  19 kinds whose word directly carries a pointer became guards; multi-field box families
  living in stable pointees after the flip (Pair/GenericRange/Mixin/…) stay `&'a`.
  `Junction.kind` becomes tag-derived, so it went by-value (`JunctionKind: Copy`). The 708
  fallout sites were mechanically fixed with `cargo fix` (558) + 4 parallel agents (150).
- **#4469 (B-flip)**: `Value(ValueRepr)` → `Value(NanBox)`. The seams are `into_repr` = tag
  decode / `from_repr` = tag pack / `with_repr_mut` = decode-edit-repack; `same_variant` is a
  variant-tag comparison that folds the companion-field kinds (6 Array kinds, Hash itemized,
  Set/Bag/Mix mut, 4 Junction kinds, IntBoxed) onto the `ValueRepr` discriminant. The new
  `nanbox/peek.rs` handles borrowed decode (guard construction for view(), word-level probes
  like `as_int`/`as_str`/`as_list_slice`). Thanks to the prior wall work, the flip itself had
  only 4 compile-fallout errors.
- **gc-stress caught a real bug**: compound payloads formerly owned by `Box` became shared
  `Arc<...Box>`; with each holder tracing the shared box, inner `Gc` edges were double-counted
  and trial-deletion underflowed the strong count. Added a
  `value_bearing_arc_payload_uniquely_owned()` gate for outermost Arc payloads to `gc_trace`
  (shared wrappers are treated as external roots = the same safe-side under-collect as the
  existing `uniquely_owned`).
- **Gate measurements**: GC counters (candidate_pushes/roots_scanned) match main exactly
  (fib 13/13, class family 4031/2028). Same-window A/B (perf stat -r5, taskset) shows
  **all benches 5–9% faster** (fib -6.7%, int-arith -7.7%, bench-class -6.7%, f64 loop -9.2%)
  = no regression from the Num offset encoding (the NaN-favored fallback stays closed). Ratios
  vs raku: fib 0.90x, bench-class 0.89x, bench-fib 1.88x, method-call 1.13x — all targets
  held. It did not spike to the ADR's "int-arith 2x" level because July's perf slices had
  already mowed down the clone/drop bottlenecks (landing as a uniform lift instead).

## 2026-07-12: mzef end-to-end campaign — zef info within working range on the real fez index (#4457/#4460/#4462)

A re-investigation session for PLAN §1 B2 (the mzef north-star). Confirmed **both previously
recorded blockers (%-sigil Associative bind / Zef::CLI parser error) are already resolved**,
updated PLAN, and landed 3 PRs of general bugs found by actually running upstream zef 1.1.3:

- **#4457**: (1) `Hash/Set/Bag/Mix.classify/categorize` passed the whole invocant to the
  classifier instead of iterating the Pair sequence (+ fixed the bug where bare fat-arrow Pair
  list elements were swallowed as named arguments and never reached the block's topic, by
  decaying them to positional `ValuePair`); (2) hash initialization died with "Odd number of
  elements" because it did not recognize itemized (`$(:a(1))`) / cell-held Pairs as Pairs
  (itemized *hashes* still die, as in raku); (3) `IO::Path.child/.add` dropped the base for
  child paths with a leading `/` (Rust `Path::join`'s absolute-path replacement leaking
  through: `("$*HOME/.config").IO.child('/zef/config.json')` became `/zef/config.json`,
  breaking zef's config search).
- **#4460 (static folding of grammar tokens)**: the true cause of `Zef::Identity`'s REQUIRE
  grammar taking ~300ms per parse (debug) was that grammar tokens referenced by an enumerated
  char class (`<-restricted +name-sep>`) were resolved **per input character** with a full
  token-registry walk + construction of a fresh scratch Interpreter (~6.7ms/char). Closed
  tokens are now folded at parse time (single-char candidates → native CharClass; multi-char
  literals → inline alternation), with soundness guaranteed by keying `REGEX_PARSE_CACHE` on
  (pkg, pattern) + `TOKEN_DEFS_GEN` generation invalidation (also fixing the latent
  unsoundness of keying on the pattern string alone). REQUIRE 20 parses 5.02s→0.19s (27x,
  debug); in release, **1.1x vs raku** (from ~70x).
  pin = `t/regex-charclass-token-fold.t` (includes same-named tokens in different grammars and
  inheritance overrides).
- **#4462 (`Version.parts/.plus/.whatever`)**: zef's DependencySpecification.spec-matcher pads
  and compares version part arrays, but mutsu had no `Version.parts` — "No such method" was
  swallowed by the surrounding try/grep and surfaced as "Found no candidates matching
  identity: Zef". Implemented the 3 methods natively (all cases match raku).
  pin = `t/version-parts-plus.t`.

**Where we got to**: with an instrumented copy that changes `Zef::Repository.candidates`'
`.hyper(:batch(1)).map` to a plain `.map`, `zef info Zef` **works end-to-end through the
Identity output** on the real fez index (7648 dists) (release, ~3 min). The biggest remaining
blocker is **a real bug where instance %-hash attribute writes on hyper workers are uniformly
~80% lost** (`@!attr` array pushes are intact; with GC on, separate `$!name` corruption also
co-occurs) = the next slice (details in PLAN §1 B2).

## 2026-07-12: `S32-hash/perl.t` fully conquered (typed-hash `.raku` idempotence + `Associative[T]` type-capture binding) — #4452

Whitelisted `S32-hash/perl.t` (47/55 → 55/55, perfect under raku) — the **last ★achievable
item** remaining in the BLOCKERS.md table. The test round-trips typed hashes / object hashes
through `.raku.EVAL`. The 8 failures decomposed into 4 independent bugs:

- **`Associative[T]`/`Positional[T]` type-capture binding did not resolve the `T` inside
  `[...]`** (a real role-matching bug actually more important than the headline):
  `resolved_type_capture_name` resolved a leading `T` or `T:D` but let `T` inside a
  parameterization's type arguments pass through untouched. As a result, a correctly typed
  `Hash[Int]` was rejected against an unbound `Associative[T]`. Fixed to resolve type
  arguments inside `[...]` as well.
- **Anonymous typed containers in expression position (`my Int %`, `my Int %{Int}`) were not
  tagged**: this had been a workaround for the role bug above. With #1 fixed, enabled it.
  Anonymous object hashes also keep their key type (unlike named object hashes they are not
  subject to raw-binding mutation, so the `%j<b>:k` adverb path is not broken).
- **`raku_value` rendered typed hashes as bare `{...}`**: the itemized wrapper (`$(...)`) and
  the native shortcut dropped the `(my T %{K} = ...)` form. Made `raku_value` type-aware,
  matching the slow-path `dispatch_constrained_hash_raku`.
- **Object hashes stringified keys instead of recording the original typed key**:
  `Hash[Int,Int].new(1 => 2)` / `my Int %{Int} = 1 => 2` reported `"1"` instead of `1`.
  Populated `original_keys` on the 3 paths: the `.new` constructor, assignment coercion, and
  expression-position SetVarType.

No regressions (`assuming-type-capture.t`, `S32-hash/adverbs.t`, `S09-hashes/objecthash.t`,
`S06-signature/type-capture.t`, etc.). **This brings the ★achievable backlog in BLOCKERS.md
to zero.**

## 2026-07-12: `6.c/S05-grammar/methods.t` fully conquered (grammar method subrules / embedded-block lexicals / dup-token error)

Whitelisted `6.c/S05-grammar/methods.t` (4/8 → 8/8, perfect under raku). Resolved the "deep
grammar-engine composite" among BLOCKERS.md's ★achievable items with 3 independent fixes:

- **subrule-as-method calls + exception propagation** (tests 1/2): when a subrule name that
  does not resolve to a token/regex/rule refers to a **plain method** of the grammar, as in
  `rule TOP { <.panic> }`, call that method (`try_regex_subrule_as_method`,
  `regex_match_atom.rs`). A `die` inside the method propagates out of the parse via
  `PENDING_REGEX_ERROR` instead of being swallowed as a silent non-match. The invocant is the
  grammar type object; `copy_full_registry_into` hands the method to the scratch interpreter.
- **outer-lexical capture in embedded blocks** (test 3): `regex TOP { x { $x = 42 } }`
  mutates the outer `my $x`. Because the compiler auto-qualifies free variables into the
  current grammar package (`$x` → `SetGlobal("G::x")`), added an **outer-lexical write
  fallback** to `SetGlobal` (symmetric to GetGlobal's read fallback): for a qualified name
  whose qualifier == current_package and which is not an `our`/package variable, if the bare
  name exists in the env as a lexical, write back to the bare name. Restricted via the
  `in_regex_code_block` flag to **only during embedded regex block execution**; normal `our`
  writes are unchanged. Enabled eager code-block collection during grammar parse so side
  effects survive even when the full-length match fails (`.parse('xxx')` matching a
  single-char rule). After method calls, `pending_local_updates` are drained to the caller's
  slots.
- **dup-token error message** (test 6): unified the message for duplicate token/regex
  declarations within a package to the raku form
  `Package 'A' already has a regex 'a' (did you mean to declare a multi method?)`
  (`system_eval_redecl.rs`).

## 2026-07-12: module-sub OTF gate-relaxation campaign complete (#4427→#4429→#4431→#4437, PLAN §3)

Progressively lifted the interpreter-fallback exclusions in
`def_is_otf_compilable_module_single` (`vm/vm_call_func_ops.rs`) experiment-driven (module +
script in tmp/ → compare output with raku + count fallbacks with `MUTSU_VM_STATS` → relax +
pin test when identical), exhausting the frontier of "just remove the gate".

**Now allowed (all experimentally confirmed to match raku)**:

- **#4427 (07-11)**: nested sub/proto/token declarations / `subtest` / `CATCH`, `CONTROL` /
  phasers / `once`. Side fix: the tree-walk bug "a `CATCH` directly under the body makes the
  non-die path's return value Nil" is resolved by OTF-ification. `once` is thread-local
  OTF-safe because the fingerprint-keyed `otf_compile_cache` reuses a single compiled body,
  keeping the site key stable.
- **#4429 (07-12)**: nested class/role/grammar declarations. Non-pollution across subs for
  same-named `my` classes is guaranteed by the parse-time `decl_id`. Captured
  lexicals/inheritance + callsame/parameterized roles/grammar parse all match.
- **#4431 (07-12)**: return-type coercion (`--> Foo:D()`). Experimentally confirmed the old
  mismatch (coercion-return.t) was already resolved by compiled-path improvements. Custom
  COERCE multis, Nil/Failure as-is, the X::TypeCheck::Return / X::Coerce::Impossible failure
  family, and coexisting multiple coercion subs all match.
  pin = `t/module-sub-otf-coercion-return.t`.
- **#4437 (07-12)**: `EVAL`/`EVALFILE`. Preconditions = #4433 + #4435 (next entry). All 12
  probes match (param/lexical read/write, sub declaration inside EVAL, CALLER:: d1/d3, topic,
  module-private sibling calls, nested my-sub capture, die + CATCH, EVALFILE); the fallback
  counter shows only the intentionally excluded start/await.
- Standard param traits (`is copy`/`is rw`/`is raw`/`is readonly`/`is required`): lifted the
  blanket `pd.traits.is_empty()` exclusion (compiled binding already handled them). The
  `is rw` caller writeback that the tree-walk fallback silently dropped is also fixed by
  OTF-ification (#4091's compile-time caller slot).
  pin = `t/module-sub-otf-trait-params.t`.
- Module subs with `state` were resolved by compiled_fns expansion slice 1 (#4312): capture
  the compiled body in `imported_compiled_fns` under the fingerprint key → Arc-share into
  thread clones → reset `state_scope_id` to None and dispatch via the shared body. Concurrent
  state under `await (^N).map:{start f()}` fixed from 0→N.
  pin = `t/module-state-sub-shared-cell.t`.
- Stale items confirmed complete by measurement: bare multi OTF / `state` candidates,
  caching-proto bodies (#4047) / `code_signature` params (#3883) / capture params / `@_`
  slurpy recursive subs (0 fallbacks including recursion for both local and module; removed
  from PLAN in #4432).

Overall pin = `t/module-sub-otf-interpreter-constructs.t` (40 assertions, verified 40/40
under rakudo).

**Exclusions kept (proven impossible or zero practical impact = the gate-experiment frontier
is exhausted; remaining rationale)**:

- `start`: when a recursive sub's start closure captures a param, the recursive call's param
  re-bind clobbers the captured value on the thread env, and after `await` returns, the value
  of the deepest call is visible (`t/start-block-return-value.t` test 3 regresses fib(5)=8→3.
  The tree-walk protects via env save/restore around each call). Non-recursive start captures
  are correct under OTF too, but the AST gate cannot detect recursion, hence a blanket
  exclusion. Root fix = per-call capture cells (left in PLAN §3).
- Sigilless scalars (`\x`): flat calls write back correctly under OTF, but via `EVAL 'f($v)'`
  the raw alias's write-through does not reach the caller lexical (there is no equivalent of
  the #4091 machinery that saved `is rw` for raw aliases). Guard =
  `t/sigilless-params.t` "sigilless aliases are writable through EVAL calls" FAILs under
  OTF-ification (reproduction confirmed in the 2026-07-06 experiment).
- `is encoded(...)`: NativeCall marshalling; almost never appears on module subs, zero
  practical impact.
- Single candidates shadowing builtins with default params: kept intentionally due to
  name-cache pollution risk (user policy, PR #3546).
- `state` sharing across signature alternates: intentionally left as an interpreter boundary.

**Known bug on another axis (left in PLAN §3)**: cross-thread double-firing of `once` — the
`once_values` store is value-copied at thread clone, so under `await (^N).map:{start f()}`
each thread re-fires the once (raku fires once across all threads). Fix = shared-cell-ify
`once_values` (same shape as #4312's state cells).

## 2026-07-12: fixed EVAL's CALLER:: frame semantics to match raku (#4433/#4435)

The 2 bugs that were preconditions for EVAL OTF-ification (#4437 above) — in both, the
tree-walk itself disagreed with raku:

- **#4433**: suppressed the spurious "Useless use ... in sink context" warning on EVAL's
  final expression (return-value position). A one-shot thread-local `EVAL_VALUE_TAIL`
  excludes the final real statement from sink warnings. pin = `t/sink-warning.t` +3.
- **#4435**: CALLER:: frame resolution. Semantics pinned down by raku measurement (try-free
  probes):
  - EVAL inserts **2 intermediate frames** (multi candidate + proto) between the EVAL'd
    mainline and the caller. The caller scope is reached at `CALLER::CALLER::CALLER::`
    (depth 3); user lexicals are invisible at d1/d2, and d4 passes beyond the caller. Same
    for EVAL from the mainline.
  - A CALLER:: lookup **miss** (no such name, stack exceeded) is **quietly Nil** (sigil forms
    too).
  - **Exists but non-dynamic** is an **X::Caller::NotDynamic throw** regardless of
    mainline/sub/EVAL'd unit.
  Implementation = `push_eval_caller_frames` (push the current env + 2 empty frames onto
  caller_env_stack/callframe_stack) + making `get_caller_var`'s not-found/over-depth cases
  quiet-Nil. pin = `t/eval-caller-frames.t` (**verified 10/10 under rakudo**).

**Measurement-methodology lesson**: the old finding "non-EVAL non-dynamic returns a quiet Any"
was wrong — it was **measurement contamination** where `try` inserts one thunk frame and
shifts the CALLER chain by one (mutsu's existing throw was correct). When probing CALLER
behavior under raku, do not use try; let the bare expression throw uncaught or observe with a
mainline CATCH.

## 2026-07-12: shadow slots default ON — lexical-scope slot campaign S1–S17 completed (ANALYSIS §1.4)

After finishing the slot burn-in of the writeback IR (S1–S17,
[docs/lexical-scope-slot-campaign.md](../docs/lexical-scope-slot-campaign.md)), a full
toggle-ON survey (1379 files) confirmed 0 genuine regressions, and `shadow_slots_active()`
flipped to default true (escape hatch = `MUTSU_NO_SHADOW_SLOTS`; make test 16351 PASS). The
remaining finishing work (removing `BlockScope`'s full clone/restore of locals = the real
perf prize) stays in PLAN §6.

## 2026-07-11: layer 3b-1 step A — `Value` newtype seal (byte-identical, ADR-0005 §2.3)

`pub enum Value` → **`pub struct Value(ValueRepr)`** (`ValueRepr` is the current 48B enum,
private to `crate::value`). Upgraded the 3b-0 API wall (zero external variant references) to
**compiler-enforced**: variant construction/matching outside `src/value/` is a compile error
(E0624/type invisibility; demonstrated with probes).

- Conversion scale: ~1800 sites inside `src/value/`. Patterns and struct-like variant
  expressions became `Value(ValueRepr::..)` via a **compile-error-span-driven mechanical
  conversion** (a fixer loop rewriting the primary spans from rustc JSON), converging in 3
  passes.
- Tuple/unit variant **expression** sites unchanged: variant-name constructor shims
  (`fn Int(..)`, `const Nil` etc., visible only to `crate::value`) were provided on `Value`.
  **In step B (the NaN-box representation swap), these shim bodies become the tag-packing
  constructors** — construction is already funneled through a single point.
- The 3 variant comparisons via `mem::discriminant` (vm_comparison_ops/vm_env_helpers) moved
  to the new wall API `Value::same_variant()` (discriminant-on-struct was flushed out by the
  `enum_intrinsics_non_enums` error).
- Behavior and size fully unchanged: `Debug` forwards to the repr (byte-identical output),
  size guard 48B, make test / wall ratchet green. Step B awaits ADR-0005 Accepted.

## 2026-07-11: GC — candidate buffer now holds `Weak` (grammar-parse acyclic churn resolved, PLAN §2)

Resolved the churn where zef's dist-identity grammar parse buffered every acyclic
`Match`/capture tree as a cycle candidate while the collector reclaimed `reclaimed_cycles=0`
every time ([docs/grammar-parse-gc-churn.md](../docs/grammar-parse-gc-churn.md)) by
**changing candidate-buffer retention from strong `Arc` clones to `Weak` handles
(`ErasedWeakGc`)**.

- A ceiling measurement (direction (3) in the doc) identified the real harm: wall-clock is
  nearly identical GC on/off (~1%), but **RSS was 153MB vs 43MB (3.6x)** — the buffer's
  strong retention kept refcount-dead garbage (whole capture trees) alive until the next
  collect. The ~54% `drop_in_place<HashMap<String,Value>>` in the CPU profile was a
  *concentration* of deallocation cost paid even with GC off, not an excess.
- Soundness of weak retention: true cycle garbage by definition keeps itself alive (internal
  edges are strong), so `Weak::upgrade` at drain time always succeeds and trial deletion is
  unchanged. Refcount-dead entries are skipped on upgrade failure (the header is gone with
  the node, so it is never touched).
- Effect (zbench, debug, interleaved measurement): RSS **3.6x → +6%**, pause_max 41ms →
  3.6ms, pause_total 210ms → 99ms, dead reaching the collector **244,598 → 0** (moved to
  inline deallocation). `MUTSU_GC=on` wall-clock is noise-level vs off (~2%).
- Derived simplification: deleted the off-thread deallocation helper for 16k-node dead
  batches inside collect (dead can no longer accumulate in the buffer). The dead partition
  remains in a degenerate form covering only the narrow race window of "the last handle
  dropped after drain".
- Remaining: suppressing the candidate pushes themselves (~510k per 200 parses) is a separate
  axis (the practical harm — memory retention — is resolved).

## 2026-07-11: Track B T6 — fixed escaped closures missing element incdec/`:delete` captures (gc-post-3a-roadmap §2)

The T6 probe (checking non-state escaped-aggregate capture against raku) showed
plain/compound element assign, push/pop, whole reassign, and cross-thread push all match on
every path. The single gap = **closure free-variable analysis (`compute_free_vars`) did not
recognize element increment/decrement (`%h{$k}++`, `@a[$i]--`) and element `:delete` as
"uses of the aggregate"**: the 4 `PostIncrementIndex`-family opcodes and `DeleteIndexNamed`
were missing from `op_container_mutate_const_idx`. A closure whose only use was one of these
**did not capture** the aggregate, and mutations after the frame escaped silently vanished
(the factory pattern returning `sub ($k) { %h{$k}++ }`; hard to notice since it works within
the same frame).

- Fix = registering the 5 opcodes, nothing more (once captured, the existing
  captured-env merge → in-place mutation via env correctly reaches the shared Gc node).
- pin = `t/escaped-closure-elem-incdec-delete.t` (12 tests, 16/16 match with raku,
  including a 4-thread push).
- ★Diagnostic lesson: for "writes vanish in an escaped closure" cases, first suspect
  **whether the variable is in the closure's captured env (`data.env`)**. If it is absent
  from the VM env (`env_has=false`) yet reads/writes on other paths work, the working path is
  a different already-capturing closure, or a coverage difference in the compile-time capture
  analysis's op list.
## 2026-07-11: GC — relaxed the make_mut underflow assert for a legal race + fixed the unguarded CollectGuard window

#4414's debug assert fired in gc-stress CI (t/lock.t test 10). Investigation produced 2 fixes:

- **`Gc::make_mut`'s `debug_assert!(prev > 1)` was wrong**: between the `!= 1` load and the
  `fetch_sub`, another thread can legally drop **its own** handle, making `prev == 1`
  (cloning is impossible but dropping is concurrent — it only makes the COW unnecessary, not
  unsound). Relaxed to `prev >= 1` and corrected the false premise in make_mut's ordering
  comment ("other threads cannot even drop"). Local repro = t/lock.t ×30 under GC-on stress
  (3/30 panics before the fix → 0/40 after).
- **A window where empty suspects still fell into `reclaim(vec![], vec![])` and raised a
  `CollectGuard`**: the process-global `collecting()` went up without STW, and concurrent
  workers' `Gc::drop` skipped bookkeeping (lost decrements/buffering). Closed the window by
  early-returning when suspects are empty after the dead sweep.

## 2026-07-11: Track B T5 — typed-constraint transparency on the cas path (gc-post-3a-roadmap §2)

The T5 probe (checked against raku) showed element constraints of typed aggregates are
already enforced on normal assignment, cross-thread assignment, scalar cas, and hash 3-arg
cas (via the assignment desugar); **only the path where the cas-specific builtin writes
directly into the cell passed unchecked**:

- `my Int @b; cas(@b[0], 5, "s")` → mutsu swapped (raku throws X::TypeCheck::Assignment).
  raku **type-checks the swap value even when the compare fails**.
- `my Int %h; cas(%h<k>, -> $v { "bad" })` (the general loop of the hash code form) also
  passed unchecked.
- `cas(@a[i], &code)` (the array code form) was simply unimplemented
  (`Unknown function: cas`).

Fixes:

- `check_atomic_elem_type` (`builtins_atomic_cas_code.rs`): resolve the declared constraint
  (`var_type_constraint`) first, then the store-node-embedded `value_type`, and check with
  `type_matches_value` (native int element types accept Int/Num, matching the assignment
  path's coercion). Array 3-arg / multidim cas check before the lock (before the compare);
  the code forms check each closure return value.
- Newly implemented the array code-form cas: both 1-dim and multidim use a celled retry loop
  (the closure runs outside the lock — the Track B re-entrancy rule). Unified with the hash
  code form into a shared `cas_cell_code_loop`, which also fixed the hash side's missing type
  check at the same time.
- pin = `t/cas-typed-constraint.t` (16 tests, confirmed 16/16 match under raku).

★A separate-axis gap found by the probe (unfixed, non-cell path): for an un-shaped
`my Int @m`, `@m[1;1]` autovivifies an intermediate Array which raku rejects with
X::TypeCheck::Assignment but mutsu lets through (missing intermediate type check in multidim
autoviv).

## 2026-07-11: Track B T4 — element cells for multidim cas (gc-post-3a-roadmap §2)

Unified multidimensional `cas(@arr[d1;d2], $expected, $new)` onto the same Track B
element-cell template as 1-dim `cas` (previously: COW the whole array and republish to the
shared store):

- `builtin_cas_array_multidim`: `init_celled_atomic_store` + fetch the top-level slot's cell,
  then do the nested compare+set inside that cell's lock (COW only the structure inside the
  cell). Concurrent CAS on the same top-level slot is serialized by the cell mutex; every
  snapshot holder shares the cell.
- Fixed an inconsistency bug of the old path: mixing 1-dim cas (which cell-ifies the store)
  and multidim cas on the same array made the multidim side pass over celled elements and
  return 0/Nil.
- Made the multidim read/assign paths cell-transparent: `multi_dim_index_read` /
  `index_array_multidim` (deref only cells holding a container), `multi_dim_assign_scalar` /
  `multi_dim_assign_slice` (terminals write through via `assign_element_slot`; intermediate
  cells snapshot→store back), `assign_array_multidim` (write through at the cell level inside
  the lock).
- ★Lesson: **a cell holding a scalar must not be dereffed on the read path** — the
  scalar-as-single-element-list wrap structurally relies on "index 0 returns the cell itself"
  for raw `\target` / `is rw` leaf aliasing (guarded by `t/multislice-lvalue.t` tests 10-11;
  an unconditional deref turned into "Cannot modify an immutable value"). Deref only when the
  inner value is an Array/Hash (= descent genuinely needed).
- The assign path's `assign_through_cell` does not hold the lock while `f` runs (user code
  inside a dimension block reading the same cell would self-deadlock on the non-reentrant
  mutex; visibility is guaranteed by cell sharing, and the atomicity contract belongs to
  `cas` only).

pin = `t/cas-multidim-cells.t` (mixed-cas consistency, shaped, threaded ×4 increments).
Verification = cas-int.t / cas.t / cas-loop.t / atomic.t / thread.t / lock.t / all
S09-multidim / multislice-6e ×2 / all typed-arrays PASS; GC stress
(EVERY_CANDIDATE=100-200 + VERIFY) clean.

## 2026-07-11: GC layer-3a auditability sweep (ANALYSIS rev8 §2.1)

Completed the remaining PLAN §2 task "layer-3a auditability sweep" in one PR:

- **Stale header corrections**: updated statements in `gc/mod.rs` / `gc_ptr.rs` /
  `collect.rs` / `safepoint.rs` / `root_visitor.rs` / `builtins_system.rs` /
  `vm_run_loop.rs` — "default off / collector not landed / cross-thread STW unimplemented /
  Sub and Instance remain Arc" etc. — to the reality of default-on (ADR-0003 §5), STW
  implemented, layer 3a complete.
- **Removed module-wide `#![allow(dead_code)]`** (`gc_ptr.rs` / `collect.rs`). Cleaned up
  the genuinely dead surface it hid: deleted `gc_debug_collect_now` / `collect_if_enabled` /
  `mutator_workers_active` / `GcBox::gc_buffered` / `gc_set_buffered`; made the test-only
  APIs (`collect_cycles` / `WeakGc::new` / `Gc::{get_mut,weak_count,color,trace_children}` /
  the `drain_candidates` re-export) `#[cfg(test)]`. Also removed every
  `#[allow(unused_imports)]` in `mod.rs` and deleted the dead re-exports (9 items:
  `CollectStats`/`Color`/`stw_requested` etc.).
- **Turned invariants into debug asserts**: strong-count underflow in `Gc::drop` /
  `Gc::make_mut` (the origin of VERIFY's survivor invariant violation), underflow of the
  trial-deletion scratch decrement (the earliest detection point for the #4135 over-trace bug
  class), re-entry prohibition of `CollectGuard`, and prohibition of `get_mut`/`make_mut`
  during the reclaim window.
- **Reviewed `make_mut`'s `Relaxed` ordering ((4))**: verdict = sound. The `== 1` uniqueness
  test only acts when the current thread holds the sole live handle, and the collector (the
  only cross-thread reader) has the SeqCst STW handshake ordering every mutator's relaxed
  writes before the scan. The theoretical remaining gap = the check-then-act window between a
  cross-thread `WeakGc::upgrade` and the `== 1` fast path (`Arc::get_mut` closes it with the
  weak lock), but the sole current `WeakGc` user, `WeakSub`, only upgrades on the env-owning
  thread, so it is unreachable — noted in a comment.

## 2026-07-10: first-class containers / container identity complete — BLOCKERS §3 closed

Deleted BLOCKERS.md §3 "first-class containers / container identity" as a whole section
(together with §2.3 hash multislice). Every sub-campaign is complete, and the canaries
(`roast/S32-array/splice.t`, `roast/S02-names-vars/variables-and-packages.t` 39/39,
`t/container-identity-mutation.t` 60) are all green.

- **In-place element mutation (#4362) + full inventory of `make_mut` sites (#4366)**:
  switched all element-level mutations (push/pop/shift/unshift/splice/append/prepend,
  element/slice assignment, hash key write/:delete/++/--, multidim, nested, typed/defaulted,
  the ContainerRef cell path) and the 34 sites of container mutation on the variable itself
  from `Gc::make_mut` (COW detach) to in-place writes into the shared node
  (`gc_data_mut`/`gc_contents_mut`). By-value holders, `:=` binds, and captures all track
  every mutation. Copy independence is guaranteed by detach-at-copy
  (`detach_shared_container`). The KEEP decisions (fresh/derived construction,
  declaration-time tagging, the shared_vars lock+sync protocol) are recorded in #4366's
  commit message. Also fixed the 3 declared-QuantHash bugs
  (`quanthash_store_preserving_identity`).
  Pins: `t/container-identity-mutation.t` (60), `t/quanthash-declared-init-identity.t` (16).
- **Abolished the post-call writeback mechanism (single-level #4370, nested #4372,
  value-path #4377)**: replaced the result-writeback of `compile_expr_method_on_index` /
  `compile_expr_nested_method_writeback` with a chain of **IndexElemAutoviv opcodes**
  (temp-binding the shared node per intermediate subscript), and made the by-value slow path
  (`array_mutate_copy`) in-place in every branch. Fixed in one sweep the raku divergences the
  writeback had created (array growth on missing pop, spurious stores into defaulted hashes,
  lost mutations of `f().pop`/`f().splice`, slot clobbering by pop/shift, etc.). Implemented
  raku's autoviv rule ("any type object without its own push autovivifies"), proven by probes.
  Pins: `t/index-method-no-writeback.t` (30), `t/nested-index-method-no-writeback.t` (27),
  `t/array-value-path-mutation.t` (27, all verified with raku).
- **Slot-ified attribute accessors (#4360)**: `$my_ref := $obj.attr` (a `:=` bind to an rw
  accessor shares the attribute slot's `ContainerRef` cell, tracks bidirectionally, and
  registers the type-constraint cell) and `$obj.attr.VAR does Role` + `.VAR.name = v`. Closed
  into 1 dispatch via the `MarkAccessorRefContext` opcode. Pin: `t/attr-accessor-slot.t` (18).
- **Hash multislice lvalues (#4354/#4355)**: `S32-hash/multislice-6e.t` 549/549 whitelisted.
  The true root cause of "sigilless map param varref stale-env" = leftover stale
  `pending_call_arg_sources`, fixed with set→clear at opcode boundaries. Also implemented
  `%h{|| @list}` dimension splat and unbounded-end range dimension clamping.
  Pins: `t/sigilless-map-args.t`, `t/multidim-splat-lazy.t`.
- **Scalar itemization (#4382)**: ItemArray/ItemList-ification when storing `$x = [...]`
  (`.raku` shows `$[...]`), zen-slice `$a[]` deitemization, hyper decont + itemization
  inheritance of results, decont at method-dispatch entry. Remaining = only Hash's `${...}`
  display (to be done together with the Value representation rework 3b).
- **All write paths to escaped our-sub lexicals (#4385)**: plain assigns / compounds /
  inc・dec to captured lexicals of an `our sub` that outlives its bare block now all resolve
  the persisted cell first, symmetric with the read side. Also fixed the latent bug where the
  old guard shadowed the live capture of a same-named plain `my sub` (both read and write
  sides) via an `escaped_our_sub_names` gate (innermost named-routine-frame test).
  Pin: `t/escaped-our-sub-lexical-write.t` (21, verified with raku).

The same-thread visibility gap on threads (`start { %h<k> = []; say %h }` → `{}`) was also
fixed in #4378 (the read side was missing the `%`→`__mutsu_atomic_hash::` check; made it
symmetric with arrays). Pin: `t/thread-hash-elem-visibility.t` (12).

With this, the large roast-derived blockers are exhausted; selecting the next structural
project moves to PLAN.md (lexical-scope slot campaign / GC post-3a / Batteries).

## 2026-07-09: concurrency/async (S17) blockers complete — BLOCKERS §4 closed

Deleted BLOCKERS.md §4 "concurrency/async (S17)" as a whole section. The inventory showed
**all 99 test files** under S17 (all of S17-supply, S17-lowlevel, S17-promise,
S17-procasync, S17-scheduler, etc.) and the socket family (`S32-io/IO-Socket-Async.t`,
`IO-Socket-Async-UDP.t`, `IO-Socket-INET.t`, `socket-recv-vs-read.t`,
`socket-accept-and-working-threads.t`, etc.) were already whitelisted.

- **Former §4.1 cross-thread plain-scalar lexical writeback**: resolved by the
  escape-analysis → ContainerRef shared-cell promotion campaign (escaping `.then`/`.tap`/
  `.act` arguments #4336/#4340, the free-var surface of whenever bodies #4345, the same fix
  for lazy gather #4348). The re-verification probes (plain-scalar propagation in both
  worker→main and main→worker directions on each of the `start`/`Thread.start`/`.then`/
  `.tap`/`.act`/whenever/gather/`$*SCHEDULER.cue`/`Lock.protect` paths) match raku on every
  path.
- **Former §4.2 async IO / Proc::Async / socket supplies**: the named files — `procasync/*`,
  `IO-Socket-Async*`, `socket-recv-vs-read.t` (test 11) — are all whitelisted.

As raku-compat gaps that do not appear in roast, the inventory found 3 issues with on-demand
supplies (`supply {}` blocks) driven outside react (`.tap`/`.list`/`.wait`): `emit` in a
whenever body over a cold source (`Supply.from-list` etc. with no supplier_id) not reaching
the outer tap; `Supply.do` callbacks not firing on live emits; and `on-close` callbacks not
firing on `Tap.close` (all roast files are green, so these are separate fix targets rather
than BLOCKERS).

## 2026-07-08: `S17-supply/syntax.t` complete (90/90, added to whitelist)

Killed the remaining 2 items of `S17-supply/syntax.t`, which had advanced to 88/90 at the
start of July; now whitelisted.

- **test 90 (#4326)**: passes deterministically via live `Supply.grep`/`.map` forwarding taps
  + supplier pre-drain in the react loop (ordering guarantee of emit-before-done) + global
  emit-order merging (interleaving sibling `whenever $s.grep(...)` in emit order via
  `emit_seqs`). pin=`t/supply-live-grep-map-react-order.t`.
- **test 75 (#4327)**: fixed nested `whenever` (an on-demand supply inside
  `whenever Supply.interval {}`) so `closing => { $closed++ }` fires on the react thread.
  Previously it went down the non-react tap path and `on_close_callbacks` never fired.

This clears the last open S17 file; the concurrency/async foundation now only has
cross-thread lexical writeback and async IO / Proc::Async / socket supplies left.

## 2026-07-08: properly fixed the parallel-load races in `S17-supply/syntax.t` (test 53 / test 57)

Right after whitelisting, `S17-supply/syntax.t` was hitting 2 races under full-file parallel
load (passes standalone but fails deterministically under `make roast`'s high parallelism =
a CI blocker). Instead of "flaky, just re-run", both were fixed properly.

- **test 53 "Can only be in one whenever block at a time" (whenever mutual exclusion within a
  supply block)**: Raku guarantees that per `supply {}` / `react {}` instance, "only one
  `whenever` block can execute at a time". mutsu ran live `whenever` callbacks synchronously
  on the thread that emitted to the source Supplier (the worker of
  `start { $trigger.emit(...) }`), so two sibling `whenever`s ran concurrently on different
  threads from different triggers, interleaving emits. Fix = introduced a per-supply-block
  serialization lock. All `whenever`s registered in one on-demand supply-block body share the
  block's emitter `supplier_id` as their "serialization group", with a secondary map of each
  trigger `supplier_id` → group. On emit, this group lock is held for the whole callback
  execution (including while the `whenever` blocks in `await`), so a sibling `whenever` on
  another thread waits until the lock is released. The lock is per-thread reentrant (so
  re-triggering the same group on the same thread does not self-deadlock) and blocks across
  threads via a condvar (`acquire_supply_serialize` / `set_supplier_serialize_group` in
  `state_supplier.rs`). pin=`t/supply-whenever-mutual-exclusion.t`.
- **test 57 "multiple channels in whenever blocks work" (Channel close-before-drain race)**:
  the react loop's Channel branch did `poll_result()` (queue empty → `Ok(None)`) and
  `can_send()` (closed test) under **separate lock acquisitions**. In that TOCTOU window, if
  the worker ran `$c.send(3); $c.close()`, poll observed "empty" while the close test
  observed "closed", so the queued value 3 was never delivered and the subscription was
  marked done (the order degraded from `'123'` to `'12'`). Fix = added
  `is_drained_closed()` to `SharedChannel`, reading a `drained_closed` flag set atomically
  inside the mutex only when `send_closed` and the queue has become empty, and replaced the
  react loop's done test (`!can_send()`) with it. While values remain queued,
  `drained_closed` stays false, so polling continues and delivers. pin = the existing
  `t/react-whenever-shared-lexical.t` (the same `'123'` ordering scenario) now passes
  deterministically.

Both are in different subsystems (Supplier emit / react-Channel) and only manifested under
`make roast` parallel load (roughly 8/40 and 3-ish per 20-parallel respectively). After the
fixes: 0 failures across multiple rounds at 60-parallel.

## 2026-07-05: container identity for whole-container assignment (`S32-array/splice.t` whitelisted)

The 35 self-splice failures of `S32-array/splice.t` had 1 cause = whole-container assignment
(`@a = ...`) **rebinding** the container. raku treats `@a = ...` as an **in-place update of
the existing container**, so an `@a` reference captured by value into a list, as in
`(5,0,@a)`, observes later reassignments. mutsu replaced the backing `Gc` of `Value::Array`,
so the capturing side kept seeing the old `Gc` and self-splice inserted extra elements. Fixes
(both the SetLocal and SetGlobal paths in `vm_var_assign_set_local.rs` /
`vm_exec_dispatch.rs`):
1. Plain whole-container assignment (`=`, other than `my`/`:=`) updates the contents of the
   existing `Gc` in place via `gc_contents_mut`, preserving pointer identity
   (`array_inplace_reassign`/`hash_inplace_reassign`; self-references are re-pointed
   `new_gc`→`old_gc`).
2. To preserve raku's `=` copy semantics, on fresh-declaration/non-in-place paths like
   `my @b = @a`, if the backing `Gc` is shared (strong_count>1), detach to a shallow copy in
   a fresh `Gc` (`detach_shared_container`).
3. Anonymous containers (`%__ANON_HASH__` reuse) are excluded from in-place. Alias slots are
   also cleared on pointy-block exit.
Regression test = `t/container-identity-whole-assign.t`. The remaining deep sub-case (a
separate slice) = an `@a` captured by a closure switches to the shared-cell (`ContainerRef`)
representation, and a reference captured by value into a list cannot track cell updates (the
return-writeback family, `ident4`).

## 2026-07-04: narrow fix for WhateverCode over-currying (`S02-types/whatever.t` 131/131, #4067)

The remaining 8 failures of `S02-types/whatever.t` (tests 111, 119-124, 126) were chiefly
WhateverCode over-currying (the CallOn-arg branch grabbing `*` too eagerly). A narrow fix
that breaks no other cases brought it to 131/131. Related findings = smartmatch containers
(arg_sources alias), fatal list-Failure, regex assertions inheriting TAP state into the
scratch interp, and the `$_` topic must not be turned into an element on map/routine-reset
(the `!params.contains("_")` guard is required). See the notes from the time for details.

## 2026-07-03: object-hash-ification of non-Str classification keys etc. (`S12-subset/subtypes.t` 92/92 whitelisted)

`S12-subset/subtypes.t` is whitelisted at 92/92 (the only `not ok 92` is the expected failure
`# TODO custom type checking on hashes NYI`). Cleaned up subset/where type constraints and
the handling of object-hash classification keys.

## 2026-07-08: implemented `Attribute.container.VAR does Role` (`S14-traits/attributes.t` whitelisted, #4314)

Implemented the pattern where a user `trait_mod:<is>` mixes a parameterized role into an
attribute's **container**, whitelisting `roast/S14-traits/attributes.t` at 4/8 → **8/8**.

```raku
role doc { has $.doc is rw }
multi trait_mod:<is>(Attribute $a, doc, $arg) {
    $a.container.VAR does doc($arg);
}
class T3 {
    has $.dog  is doc('barks');
    has @.birds is doc('tweet');
    has %.cows  is doc('moooo');
}
T3.new.dog.VAR.doc;  # 'barks' (scalars go via .VAR)
T3.new.birds.doc;    # 'tweet' (@/% are the value = directly on the container)
```

- **Root cause**: `Attribute.container` returned an empty array/hash/Nil, so the `does`
  inside the trait mixed into a throwaway value that never reached the per-instance
  container, stalling at 4/8.
- **Implementation** (#4314):
  - `Attribute.container` returns a container descriptor (a tagged `Scalar` instance)
    carrying the owning class + attribute name + sigil. `.VAR` returns itself, and the
    subsequent `does` reads the tag. Non-`VAR` methods delegate to a sigil-appropriate empty
    container to preserve introspection (`.shape` etc.) (not breaking the `#?rakudo todo` of
    `S12-introspection/attributes.t`).
  - `exec_does_op` catches a `does` against a descriptor and records the composed role
    override per (class, attr) in the new registry `class_attribute_container_mixins`.
  - `apply_attribute_does_role_mixins` overlays the override (role marker + `$arg`'s
    attribute value) onto each instance's attribute value at construction time (both the
    native and full constructor paths).
- **Remaining (other axes)**: `$my_ref := $obj.attr` (`:=` binding to a scalar accessor) and
  `$obj.attr.VAR.role = v` (rw writes to a mixin accessor) await full cell-ification of
  attribute slots.

## 2026-07-08: collate noncharacters in code-point order (`S32-str/CollationTest_NON_IGNORABLE-3.t` whitelisted, #4282)

ICU4X special-cases Unicode noncharacters in UCA weighting (treating U+FFFE as a nearly
ignorable merge separator and U+FFFF as a high sentinel). Raku's reference collation (MoarVM),
on the other hand, gives implicit-weight code points without DUCET entries (noncharacters,
unassigned, private-use) a plain implicit primary weight derived from the code point,
ordering them among themselves by code point. ICU mis-orders these **relative to each other**
only when the first differing code-point pair on both sides is **both implicit-weighted**, so
a branch was added to `coll_ordering` that falls back to code-point order only under that
condition, whitelisting at 1367/1369 → **1369/1369** (details in #4282's commit message).
`S32-str/format.t`, which waits on RakuAST, stays in §1.1.

Completed **3b-0 = the API wall**, the first stage of layer 3b (NaN-boxing,
[ADR-0001](../docs/adr/0001-gc-strategy-and-phasing.md)). Direct `Value::<Variant>`
construction/pattern-matching outside `src/value/` reached **0**; everything is unified
through `Value::view()`/`ValueView`/the `Value::int()`-family constructors/`with_*_mut`/
`as_*`. This makes 3b-1 (the representation switch of `Value` from 48→8 bytes) possible
without touching call sites.

- **slice d (#4315)**: `src/runtime/` 8728→0. Ported 268 files with same-checkout, 4-parallel
  agent batches × 4 waves. Added `version`/`comp_unit_dep_spec`/`promise`/`channel`
  constructors to view.rs. Midway, a subagent hit the account's session cap and wave 4
  stalled half-ported, but exploiting "direct variants still compile (the wall is
  ratchet-only)", a green checkpoint was cut and the remaining 37 files were re-batched to
  completion after the reset.
- **slice e (#4316)**: `src/compiler/` + `src/parser/` + stragglers (env/opcode/gc/precomp/
  repl) 549→0. In the parser it was mostly `Expr::Literal(Value::Int(n))` AST literal
  construction and `matches!` in `#[test]`s. Added the `Value::regex_with_adverbs`
  constructor. Also fixed the `check-value-wall.sh` crash bug at 0 hits under
  `set -euo pipefail` (`grep -v` exits 1 on no match).
- Cumulative: slices a (#4287) → b (#4301 src/vm/) → c (#4308 src/builtins/) → d → e. The
  ratchet baseline went 17759 → 0. Details and the next stage (variant-privacy seal → the
  3b-1 encoding decision) are in [docs/nanbox-3b0-api-wall.md](../docs/nanbox-3b0-api-wall.md) §7.
- Flakes/traps encountered: a rebase racing a background `make test` → phantom FAILs from an
  old binary × new tests; a GC+load SEGV in S17 `semaphore.t` (green on re-run); CI rustc
  1.96's `unused_variables` being stricter than local 1.92 and catching view bindings
  (pre-verifiable on nightly 1.95).

## 2026-07-07: hoist nested-block `BEGIN` to compile-time equivalence (`S02-names-vars/variables-and-packages.t` whitelisted)

A `BEGIN` **inside** a bare block also runs at compile time in Raku, so a read that comes
**before** the BEGIN in source order within the block (e.g. via a forward-declared sub)
observes its side effects. Example:

```raku
{
    is baz(), 3, ...;   # expects 3,4,5 (raku)
    is baz(), 4, ...;
    is baz(), 5, ...;
    my $a; BEGIN { $a = 3 };
    sub baz { $a++ }
}
```

mutsu executed nested BEGINs inline at their source position, so `baz()` was called before
`$a` was initialized, yielding 0,1,2. The fix (`src/runtime/phasers.rs::reorder_at_level`)
threads `is_top` through the phaser-reordering pass and hoists BEGIN ahead of the block's
reads only in nested blocks — strictly gated so it never crosses a declaration/assignment/
initialization:

- Top-level BEGINs are still delegated to `run_toplevel_begin_phasers` and not hoisted by
  this pass (`is_top`).
- A nested BEGIN is hoisted only when "a read (`Say`/`Call`/…) precedes the BEGIN and there
  is no barrier (declaration, assignment, variable initializer)"
  (`first_read_idx < idx < first_barrier_idx`). This preserves the in-source ordering of
  `class C; BEGIN {…C…}` (`S04-phasers/begin.t` test 7), `has $.a; BEGIN {…set_build…}`
  (`S12-attributes/defaults.t`), and `my $x=42; BEGIN {…}; constant c=$x`
  (`S04-declarations/constant.t` test 27), while read-after BEGIN
  (`t/begin-compile-time.t` test 11) keeps working in place as before.
- Blocks containing CHECK/INIT keep the legacy behavior (hoist all BEGINs to the leading
  bucket).

This brings `variables-and-packages.t` to 39/39, now whitelisted (resolving the remaining
tests 29-31; 32-34/37-38/39 had already been resolved by #4235/#4305 etc.). Regression test =
`t/begin-nested-block.t`.

## 2026-07-06: run top-level `BEGIN` at compile time (`S26-documentation/12-non-breaking-space.t` whitelisted)

Raku's `BEGIN { ... }` runs at compile time, so a read that precedes the BEGIN in source
order observes its side effects (`my @a; my $c = @a.elems; BEGIN { @a = 1,2,3 }; say $c` is
3 under raku). mutsu inline-compiled BEGIN and ran it at its source position, so the
read-before case yielded `$c=0`. `S26-documentation/12-non-breaking-space.t` (which sets
`@nbchars` in a trailing BEGIN and reads `.elems` at the top) failed because of this.

**Fix (`src/runtime/run_prelude.rs::run_toplevel_begin_phasers`, `src/runtime/run.rs`)** —
the approach BLOCKERS recommended: "not AST reordering but **eval the BEGIN body at compile
time in a sub-interpreter → reflect into the initial state**":

1. In `run()`, **before** `reorder_phasers` (after `preregister_top_level_subs`), execute
   hoistable top-level `Stmt::Phaser{Begin}`s via `eval_block_value` and remove them from
   `body_main`. `eval_block_value` leaves plain lexical writes in the shared `env` (only
   `&`-callable keys are isolated), so the seed persists. The key point is removing them
   **before** reorder, which avoids the breakdown of the old revert (`hoist_begin_phasers`)
   where "the init of `my $c = @a.elems` gets bucketed before the BEGIN".
2. After hoisting, drop the no-init `my` declarations of seeded variables
   (`Literal(empty Array/Hash)`, `Literal(Nil)`) from `body_main` via
   `drop_seeded_noinit_decls` (descending one level into the SyntheticBlock of
   `my (@a,@b)`). This reproduces raku's "the container is created exactly once at compile
   time; the runtime declaration does not re-initialize". Declarations with a real init
   (`my @a = 9,9,9`) are not dropped, so the runtime init overwrites the BEGIN value
   (raku-conformant).
3. **The hoistable gate is conservative** (`begin_body_is_hoistable`): only pure
   value-assignment BEGINs whose body contains no declaration, BareWord, or Call are hoisted.
   `eval_block_value` diverges from the mainline VM in name resolution and sink-context error
   forcing, so BEGINs that could do symbol resolution or throw (class references / forward
   sub calls / `BEGIN { 1/0 .Int }`) are not hoisted and are left to the in-place path
   (including the `X::Comp::BeginTime` wrap).

Regression test = `t/begin-compile-time.t` (11 tests, green on both mutsu and raku).
`t/begin-phaser-begintime.t` (the BeginTime wrap of BEGIN errors) also has no regression.
BEGIN **inside** nested blocks (`variables-and-packages.t` tests 29-31) is outside this
top-level-only mechanism and needs a deeper layer of the declaration model separately (see
BLOCKERS §3.1).

## 2026-07-05: Track B slice 2 — repaired cell sharing of `state @`/`%` (2 bugs: total loss of accumulation, push death)

`state` aggregates migrate to a shared `ContainerRef` cell once a thread context becomes
active (Track C, `StateVarInit`), but **there was no write-back into the cell, so
accumulation was completely lost** (deterministic: once any thread is spawned,
`state %h; %h<k>++` yields 1,1,1..., and `state @a; @a.push` yields 1,1,...). Moreover,
**when a cell is seeded non-empty, push dies with "No such method 'push'"** (an existing bug
also reproducible on main via the state-migration path at thread spawn). 3 fixes:

1. **Write-through in `set_state_var`**: if the existing entry is a cell and the incoming
   value is plain, write into the cell (so the write-back from block-exit
   `sync_state_locals` reaches the cell).
2. **A cell path in the shared-ctx `ArrayPush` fast op**: push on `state @a` COW-appends to
   the inner node under the cell lock (previously it fell into non-mut dispatch with the bare
   cell as invocant and died).
3. **A cell funnel in `call_method_mut_with_values`**: dispatch on the cell contents → fold
   the mutated result back into the cell → re-point the env at the cell.

Result: cross-call accumulation, pre-seed migration, and **sequential cross-thread sharing**
(running `f($x)` on 3 threads in sequence → the parent observes 0,1,2,3) all match raku.
pin = `t/state-aggregate-shared-cell.t` (10 tests, all passing under real raku too =
semantics verified). High-contention concurrent structural inserts tolerate lost updates
(**real rakudo crashes with a MoarVM oops on the same shape** = outside the language
guarantee; mutsu never breaks). Remaining: non-threaded cross-closure sharing (a separate
bug not involving cells, PLAN §6).

## 2026-07-05: Track B slice 1 — element cells for the atomic stores (removing cas's whole-container COW)

Started Track B (element cell-ification) of ADR-0001 layer 3a. As the first slice,
**cell-ified the element values** of the thread-shared atomic stores
(`__mutsu_atomic_hash::` / `__mutsu_atomic_arr::`) as `ContainerRef`s.

- **Design core**: the map/array **structure** stays a COW snapshot as before (snapshots held
  lock-free by readers on other threads remain immutable); only the element **values** are
  cells (`Gc<Mutex<Value>>`) mutated in place. All snapshots share the same cells, so
  cross-thread reads always see the latest value. The reader-side `ContainerRef` deref is the
  existing mechanism dating from `%h<k> := $x` binding, pre-verified by probes to be
  universal across element reads, arithmetic, comparison, grep, sort, and stringify.
- **Effect**: `cas %h{$_}` went from "clone the whole map per op" to "O(1) RMW under the cell
  Mutex". S17-lowlevel/thread.t test 28 (10k-entry hash × 30k cas) is **12.2s → 1.6s
  (GC-off) / 21.2s → 1.7s (GC-on)** (debug). The GC-on penalty also converged +74% → +9%
  (with the giant clones gone, the GC's accompanying traffic vanished too).
- Implementation: on a container's first atomic touch, cell-ify all elements in one pass.
  `cas` (closure form, `.succ`/`.pred` fast path, expected/new form) and element assignment
  (`shared_*_elem_set`) go through cells. Closures execute **outside the cell lock**
  (Track B's re-entrancy rule = the same boundary problem as the GC safepoint, ADR-0001
  §3-6) and retry with compare-and-store. Multidim cas etc. stay on the legacy COW (TODO).
- pin = `t/atomic-elem-cells.t` (churn correctness, hot-key contention, write-through
  consistency between cells and normal assignment). `make test` 1529 files PASS; the S17
  lock/cas/semaphore/thread family green.

## 2026-07-12: archived the layer-3a completion summary from PLAN §2 (PLAN cleanup; this is now the canonical record)

The list of completed items of GC layer 3a
([docs/gc-level1-detailed-design.md](../docs/gc-level1-detailed-design.md) §11) (see the
individual entries in this file for details on each):

§11 steps 1–4 (root/child visitors, `MUTSU_VM_STATS` GC counters, `Gc<T>`/node header/
candidate buffer) / step 5 first wave (`Arc→Gc` for `Hash`/`Array`/`ContainerRef`;
`Set`/`Bag`/`Mix` #4117; deleting the dead `arc_contents_mut`) / steps 6-7 (Gc-ifying
`Promise`/`Channel` + the supply-registry root visitor #4127) / step 8 the trial-deletion
collector proper + safepoint wiring (GC-on output byte-identical to GC-off,
`tests/gc_stress.rs`) / step 9 second wave (`Sub`/`Instance` attributes #4123,
`WeakSub`=`WeakGc`) / step 10 (`LazyList` third wave #4125) / `gc_trace` wrapper completeness
(#4124/#4134/#4135) / `WeakGc<T>` finishing / cross-thread safety (minimal STW #4130) /
basing `make_mut`/`get_mut` uniqueness on `header.strong` / debugging operations
(`MUTSU_GC_LOG`/`MUTSU_GC_VERIFY`) / the CI `gc-stress` job / `DESTROY`-on-reclaim (the
`Trace::finalize` hook = fires on both refcount death and cycle reclaim, separated from Rust
`Drop`) + the dead sweep (strong=0 candidates are freed without trial deletion even while
workers are running = resolving unbounded accumulation and multi-second pauses in threaded
mutation loops, pause_max 2.8s→16ms) + inner-dispatch (`run_range`) backedge safepoints /
cross-thread cooperative STW (next entry) / the default `MUTSU_GC=on` flip (ADR-0003 §5;
bench-class ~+8% accepted by user decision, the root reduction being layer 3b).

## 2026-07-05: Phase B (GC) — cross-thread cooperative STW complete (design memo §6.1; archived here from PLAN §2)

The collector raises `STOP_REQUESTED` and runs the cycle scan only after every other mutator
is quiescent (parked at a safepoint, or in a blocking wait — promise/channel/lock/semaphore
condvars, join, and sleep count as safe regions). If quiescence is not reached in time (an
unwrapped blocking site), it times out → requeues the suspects + cooldown, falling back to
the old behavior (soundness unchanged, only liveness degrades). All raw `std::thread::spawn`
sites handling `Value` were unified onto `spawn_user_thread` (mutator registration) — signal
watcher / proc-async pump / zip / supply throttle. Also fixed the stranding where
collect_white's buffered-skip left cycle members re-buffered between drain and STW
achievement as White (VERIFY caught it: 55-80 occurrences per run → 0).
= **cycle reclamation is now possible while server-style non-joining workers stay alive**
(churn measurement: 412 cycles reclaimed over 98 collects, pause_max ~50ms).

## 2026-07-05: Phase B (GC) — **`MUTSU_GC` flipped to default on** (GC campaign layer 3a complete)

After the acceptance measurements and 3 finishing optimizations, **mutsu became an
implementation that reclaims cycles by default** (achieving ADR-0001's "GC is table stakes").
`MUTSU_GC=off` is the explicit opt-out.

A 4th item surfaced in the first CI run after the flip (fixed forward):

- **Offloaded the dead sweep's large-batch deallocation off the collector thread**. In
  thread-churn workloads (S17-lowlevel/thread.t), the dead become "env-scale hashes from
  thread clones × 16k per round", and their serial deallocation alone took 3.5–5.5 seconds
  per collect (a serialization of the deallocation cost that, with GC off, each worker paid
  in parallel on its own thread). The glibc arena-contention theory was rejected — swapping
  in mimalloc changed nothing — and it was concluded to be real work, O(garbage). After
  DESTROY queueing (finalize) finishes on the interpreter thread, batches of 1024+ nodes are
  dropped on a registered reclamation thread (`spawn_user_thread`) — being registered, later
  collects' STW correctly waits for it (and if it can't keep up, the existing
  requeue+cooldown deferral applies). Effect: pause 3.5–5.5s → **1.2–5.3ms**; thread.t
  recovered to +6% vs GC-off.

The 3 pre-flip optimizations (all reducing the standing cost of GC=on):

1. **A buffered-bit fast path in `Gc::drop`** (the same pre-test as Bacon-Rajan's Decrement):
   instead of passing an Arc clone to `buffer_candidate` and deduping via swap on every
   survivor drop, already-cancelled nodes take a single relaxed load. This dominated
   workloads that clone/drop the same handle in a hot loop (the Sub node of recursive subs
   etc.) — **fib25 +28.9% → +0.5%, bench-fib +29.8% → +3.0%**.
2. **Skipping the program-end collect**: the only observable effect of the exit-time collect
   is "DESTROY delivery to leaked cycles" (memory is freed by exit). When no user class/role
   defines DESTROY (`registry_has_destroy_methods` — the same predicate as the drain side, a
   single O(number of types) scan at exit; builtin native DESTROYs are out of scope) and
   log/verify/stats are disabled, skip it entirely. Stopped paying the ineffective
   "re-enumerate 4000 live suspects × huge captured envs" that took 160-220ms in bench-class.
3. **A tri-state AtomicU8 cache for `gc_enabled()`** (OnceLock's 2 loads → 1 relaxed load;
   called on every survivor drop path).

Acceptance measurements (release, best of 9 iterations, quiet machine; details = ADR-0003
§5): scalar/function hot paths are fib25 +0.5% / bench-fib +3.0% / int-arith +2.0% (nearly
free, as the type filter intended), churn ≤ +10%. **Only bench-class is +7.5–8.3%** (over
the <5% gate) — the residue is one per-drop branch on the pre-existing traffic of 16.5M
`Value` clone/drops over 28k instances; reducing the traffic is layer 3b (NaN-boxing)
territory. **Flipped with user approval** (recorded in ADR-0003 §5).

The only exception is the crate's own unit-test build (`cfg!(test)`), default off — parallel
`cargo test` shares process-global collector state (GC-on unit testing is covered by the CI
gc-stress job single-threaded). `tests/gc_stress.rs`'s baseline changed to explicit
`MUTSU_GC=off`. Update note added to design doc §9.1.

## 2026-07-05: Phase B (GC) — implemented the ADR-0003 production trigger (size threshold + adaptive backoff)

Implemented the production trigger of the now-Accepted
[ADR-0003](../docs/adr/0003-default-on-gc-trigger.md). With this, `MUTSU_GC=on`
**automatically reclaims cycles during execution** without stress variables (previously only
the program-end collect). The last mechanism before the default-on flip is in place.

- **Trigger**: `buffer_candidate` passes the post-push buffer length to `note_buffer_len`;
  on reaching the effective threshold it arms the existing `PENDING` → a synchronous collect
  at the next safepoint (using the existing cooperative STW path as-is).
- **Adaptive backoff**: per collect, `threshold = clamp(BASE, 2 × survivors, MAX=1M)`
  (`note_collect_survivors`). survivors = the number of live nodes revived by scan_black. The
  deferral path that requeues suspects on STW timeout also reports the requeue count as
  survivors (unscanned live suspects stay in the buffer, so without backoff every push would
  spin drain/requeue).
- **Tunable**: `MUTSU_GC_THRESHOLD` (BASE; default 16384, `0` disables). The effective
  threshold is printed as `gc_threshold=` on the gc line of `MUTSU_VM_STATS` (the observation
  point for adaptation).
- **Measured**: garbage churn (400 self-cycles, BASE=256) = 11 collects, 2758 nodes
  reclaimed, output identical to GC-off. An 800-node live chain (cas-loop shape, BASE=64) =
  **only 4 collects**, with the threshold adapting 64→1604 (a fixed period would be ~13
  collects, repeatedly full-scanning the growing live graph). Same structure as PHP 7.3's
  Zend GC ("root buffer threshold + raise on fruitless collects").
- Guarantees: 2 additions to `tests/gc_stress.rs` (mid-run reclamation without stress
  variables + output identity / threshold growth on live suspects + a logarithmic bound on
  collect count). `make test` 1519 files PASS; S17 concurrency smoke under plain GC=on
  (semaphore/lock/cas-loop) VERIFY FAIL=0.

What remains is only the acceptance measurements (fib push==0, method-call/bench-class <5%,
churn ≤1.2x) → flipping the default to on.

## 2026-07-05: Phase B (GC) — promoted the CI gc-stress roast step to blocking

The advisory roast with `MUTSU_GC=on` confirmed **Result: PASS** on the PR run of #4215
(knob 1024). With all 3 failure classes of the advisory era resolved (STW spawn-churn
starvation #4205, shared-env phantom edges #4213, `EVERY_CANDIDATE=64` quadratic #4215),
removed `continue-on-error` and made **every step of the gc-stress job a blocking gate**
(cargo test / prove t/ / roast whitelist / VERIFY FAIL detection). From now on, a suite
failure under GC=on stops CI as a real regression, not a known gap. The roast step changed
from `set +e` to `set -o pipefail` (passing prove's exit through tee).

## 2026-07-05: Phase B (GC) — closed §11 step 11 (sound-by-refcount) + gc-stress knob to 1024

- **Closed §11 step 11 (tracking `Value` edges of the OS-resource async registries) as
  "no code change needed"**. The exhaustive survey concluded: this implementation's collector
  does trial deletion rooted at the candidate buffer, with liveness judged solely by GC
  strong refcounts. The `RootVisitor` family is unconsumed in production (all
  `#[allow(dead_code)]`, test-only), and if a registry holds a
  `Value`/`SharedPromise`/`SharedChannel`, that holding itself acts as an external strong ref
  that the scan revives. Hence the 9 registries not covered by visitors
  (AsyncSocketConn/Listener, SupplyChannelMap, PipeProc/ProcByPid, SignalRegistry,
  uncaught_handler_store, FakeSchedulerMap, LockStateMap.async_waiters) produce no
  unsoundness, no leaks, and no VERIFY false positives. The list is recorded in design doc
  §11 step 11 as hardening for a future root-consuming mode (full-root VERIFY / tracing).
  By-product: **ADR-0001 §4.3 (the scope of A') is narrowly resolved as "not needed as a
  precondition for level-1a"**.
- **Changed CI gc-stress's `MUTSU_GC_EVERY_CANDIDATE` from 64→1024**. The push-period trigger
  fires a full scan (O(live suspect graph)) periodically, which is quadratic on workloads
  whose live structure keeps growing: `cas-loop.t` (building a linked list via 4×1000 cas)
  takes >180 seconds at N=64 (8500+ collects × ~4300 traced each), 3.3 seconds at N=1024.
  Even at 1024, thousands of collects run across the suite, so soundness/VERIFY coverage is
  maintained. This measurement is the rationale for the default-on trigger design (ADR-0003
  planned: buffer-size threshold + adaptive backoff).

This resolves all 3 failure classes of the GC=on advisory roast (semaphore=STW starvation
#4205 / require=phantom edge #4213 / cas-*=stress-knob quadratic). Once the post-knob-change
main run is confirmed green, the roast step gets promoted to blocking.

## 2026-07-05: Phase B (GC) — fixed false reclamation (live-data corruption) from phantom edges of shared captured envs

The GC=on advisory roast's `S11-modules/require.t` was deterministically red on 1 subtest
(`$*PROGRAM.parent(2)` degrading to `/packages/...` and the chdir failing). Node-watch
instrumentation traced it to **the attrs node of `$*PROGRAM`'s IO::Path Instance being
falsely reclaimed at the first collect (cycle=0)**, with `drop_gc_edges` emptying the live
attribute map (`.Str` becoming ""). VERIFY was silent (the counts were consistently wrong, so
it could not detect it).

The cause was a **violation of Trace's 1:1 rule**: a closure's captured-env overlay map
(`Arc<SymMap>`) is Arc-shared among all closures from the same frame, but `SubData::trace`
inline-traced the map's contents from each holder. The map owns each `Gc` handle **exactly
once**, yet N sharers each claiming the edges causes trial deletion to over-decrement by N-1
phantom edges, turning live nodes with external references white and reclaiming them. This
was a missed application of the rule already established as `value_gc::uniquely_owned`
(trace shared Arc wrappers only when solely held). `LazyList::trace` was equally guilty.
Additionally, `drop_gc_edges` ran `values_mut()` (= a COW clone) on the shared map, running
`Gc::clone` during reclaim and polluting the counts — a co-resident secondary bug.

Fix: added `Env::gc_overlay_uniquely_owned()` (overlay Arc strong_count==1) and restricted
the env walks of `SubData`/`LazyList`'s `trace`/`drop_gc_edges` to sole-holder cases. Shared
maps lean conservative as external root holders (cycles reachable only through them are
deferred — never corrupted). Normal closure captures flattened via `clone_env` at capture
time are fresh maps = solely held, so the main closure-cycle reclamation capability is
unchanged (confirmed by all gc_stress reclamation tests passing).

pin = `value_gc::tests::shared_captured_env_edges_are_not_claimed_per_sharer` (a real 4-node
cycle of a shared map + 2 closures: the live hash is intact in both strong count and
contents, the real cycle is reclaimed — confirmed to fail reliably with the gate disabled).
`make test` 1513 files PASS; require.t GC=on 58/58.

## 2026-07-05: Phase B (GC) — fixed 2 liveness holes where spawn churn starves the cooperative STW

`roast/S17-lowlevel/semaphore.t` (RT #128628: rapidly spawning 4000 `Promise.start`s) hit a
deterministic CI timeout under `MUTSU_GC=on` (Wstat 124 in the gc-stress advisory roast).
From gdb thread dumps and `MUTSU_GC_LOG` pause analysis (pause_ns≈52ms dominating each
collect), identified 2 holes whereby **`try_stop_the_world` wastes 50ms on nearly every
attempt during short-lived-worker churn**:

1. **The birth window**: `spawn_user_thread` does `enter_mutator_worker` on the parent side
   (quiescence target +1), but the worker cannot contribute to QUIESCENT until it starts and
   reaches its first safepoint/safe region. During a spawn burst there is almost always a
   thread inside the birth window, so STW repeats 50ms timeout → 100ms cooldown. → **The
   parent pre-counts the not-yet-started worker as quiescent right before spawn**
   (`preregister_worker_quiescent`), and the worker's first action (`worker_started`) does a
   checked exit. The worker touches no Gc state until it passes the checked exit, so this is
   sound (if a scan is in progress it parks before starting).
2. **Missing exit notification**: a worker's exit (`exit_mutator_worker`) satisfies the
   quiescence equation by "lowering the target" but does not notify the rendezvous condvar,
   so a waiting collector sleeps out the full remaining timeout and only notices satisfaction
   at the deadline recheck (this is the identity of the 52ms pause). With short-lived-worker
   churn, someone is almost always mid-exit at stop-request time, so nearly every collect
   paid the full amount. → `exit_mutator_worker` wakes the collector via `notify_worker_exit`.

Effect: the N=4000 repro went **from a 60-second timeout → 7.6 seconds** (debug). pin =
`tests/gc_stress.rs::spawn_churn_does_not_starve_stop_the_world` (`pause_ns_total < 2s` over
a 300-spawn churn; measured 3.51s before the fix / tens of ms after — catches a recurrence of
either hole). `make test` 1504 files PASS; S17 concurrency GC=on smoke
(stress/start/nonblocking-await/lock) VERIFY FAIL=0.

## 2026-07-05: Phase B (GC) — emitting every safepoint kind + `MUTSU_GC_AT`/`MUTSU_GC_COLLECT_NOW`/random stress (§9.2/§9.2a concluded)

Of the safepoint-kind enum in design memo §9.2a, only `Backedge` was ever emitted. Wired the
remaining kinds at their respective re-entry boundaries (§1.2: points holding no container
borrows or locks) and implemented the 3 deferred triggers of §9.2, completing the full
deterministic / random stress surface:

- **Safepoint emission** (all gated on `armed` = GC-off pays 1 cached load):
  - `call` — the entrances of `push_call_frame`/`push_light_call_frame` plus the 3 fast paths
    that build no frame (`call_compiled_function_fast`/`_light`/`_positional_light`). The
    fast paths bypass push_call_frame, so they need individual emission (discovered by test).
  - `return` — `pop_call_frame` (the return-merge boundary; the return value is an owned
    stack root).
  - `await` — the entrances of `SharedPromise::wait` / `SharedChannel::receive_result`
    (before acquiring the state lock; a collect's finalizer can touch other
    promises/channels, so not while holding the mutex).
  - `react_poll` — one poll unit of the react/supply drive loop (the existing unconditional
    `gc_park_point` remains for STW cooperation when not armed).
  - `lazy_force` — the force/pull entrances of `force_lazy_list_vm` /
    `force_lazy_list_vm_n`.
  - `nested_run` — the nested-VM entrance of `with_nested_registers`.
  - `thread_join` — the merge boundary after joining all hyper/race workers.
- **`MUTSU_GC_AT=call,return,...`** — collect only at safepoints of the listed kinds (a kind
  bitmask). Unknown names warn and are skipped.
- **`MUTSU_GC_COLLECT_NOW=1`** — a single collect at the start of `Interpreter::run` (a
  `Once` guard). An empty-heap collect is a silent no-op, so with `MUTSU_GC_LOG` enabled the
  trigger itself is logged.
- **random stress** — `MUTSU_GC_RANDOM_RATE=0.0..1.0` + `MUTSU_GC_RANDOM_SEED=<u64>` (unset
  derives from startup time). A global-atomic splitmix64 fires probabilistically at every
  safepoint. **The seed is always logged to stderr** (§9.2's reproducibility requirement).
  Deterministic with a pinned seed.

Guarantees: 3 additions to `tests/gc_stress.rs` (the AT filter fires only on listed kinds +
demonstrated reclamation; seeded-random output identity + seed logging; collect-now firing),
all passing along with the existing 13. Smoked the concurrency t/ files in both
`EVERY_SAFEPOINT` and `AT=await,react_poll,thread_join,call,return` modes (no deadlocks).
`make test` 1502 files all passing.

What remains in PLAN.md §2 narrowed to 3 items: "promoting the roast advisory to blocking
(under observation)", "§11 step 11 (as needed)", and "the trigger policy for default GC=on".

## 2026-07-04: whitelisted `S09-subscript/slice.t` at 56/56 — nested slice adverbs

Resolved the last remaining test 35 (the `nested slices` subtest, 62 assertions) and
whitelisted `S09-subscript/slice.t`. Implemented slice adverbs
(`:p`/`:k`/`:v`/`:kv`/`:exists`/`:delete`, their negations, and combinations) on nested index
lists (`("a".."z")[(3, (30, (5,)))]`) so they recurse over the index tree and preserve its
shape in the result. Previously only the top level was handled, and a sublist was treated as
a single scalar index (out of range).

The adverbs live on 4 independent paths, so **additive** recursion was added to each
(flat/non-nested behavior stays byte-identical, zero regressions):

- **`:k`/`:v`/`:p`/`:kv`** — added the positional recursion
  `format_positional_slice_level` to `builtin_subscript_adverb`
  (`builtins_multidim_subscript.rs`). A nested sublist becomes one nested list element (the
  recursion result); scalars expand in place. Additionally, Range/Seq/List **targets** are
  coerced to an array view (`("a".."z")[3,5]:k` used to return `Nil`). Missing elements of a
  coerced list are `Nil` (not the `Any` of a real `@`-array).
- **`:exists`/`:!exists` (+`:kv`/`:p`)** — the single-dimension `Index` `ExistsIndexAdv`
  opcode (`vm_var_exists_ops.rs`) detects nested slices and recurses via
  `nested_exists_slice`. Distinguished from the coordinate walk of multidim `@a[1;2]`.
- **Bare `:delete`** — the `DeleteIndexNamed` opcode (`vm_var_delete_ops.rs`) recurses via
  `exec_nested_slice_delete`. Returns the deleted values in tree shape and deletes every leaf
  slot.
- **`:delete:kv`/`:delete:k`/…** — the compiler (`expr_method.rs`) routes
  `DELETE-KEY`(Index + adverb pair) to the `__mutsu_subscript_adverb` slice path with a
  delete flag. Also added the fix syncing delete mutations to the local slot
  (`writeback_multidim_var_to_local`).

No new tests needed (roast slice.t covers it). No regressions in `make test` / the adverbs
roast family.

## 2026-07-04: Phase B (GC) — finishing weak references (`WeakGc<T>`): cycle-breaking demonstrated

As a finishing piece of ADR-0001 layer 3a, completed and verified cycle breaking via weak
references. The primitive `WeakGc<T>` itself was already added in #4123 for `Value::WeakSub`
(recursive closures' self-references) (`Gc::downgrade` / `upgrade` / `ptr_eq` /
`strong_count` / `as_ptr` / Clone / Debug / Send+Sync) and is also used by the
consumed-LazyList registry. This round finished the API and demonstrated the cycle-breaking
semantics in unit tests.

- **API completion**: `WeakGc::new()` (a perpetually dangling equivalent of `Weak::new()` —
  `upgrade()` is always None; for empty initialization of weak slots) + `Gc::weak_count()`
  (the number of weak observers; a node drops its value at strong=0, and weaks only extend
  the allocation, so they never resurrect a reclaimed node).
- **Cycle-breaking demonstration** (`gc::gc_ptr` unit tests):
  - `weak_back_edge_breaks_a_cycle_without_the_collector`: making the back-edge of
    `A --strong--> B --weak--> A` weak means the weak does not count toward strong counts
    (untraced by `gc_trace`/root visitors), so **both A and B are freed by refcounting
    alone = the cycle dissolves without the collector** (verified with real drop counters).
  - `weak_observer_of_a_collected_strong_cycle_sees_none`: after the collector reclaims a
    strong cycle, its weak observer gets `upgrade()→None` (weaks do not resurrect reclaimed
    nodes).
  - `upgrade_is_some_while_alive_none_after_drop` / `dangling_weak_upgrades_to_none`.

With this, cycles are handled by both wheels: **(1) cut in advance with weak edges
(proactive; closure self-refs etc.)** and **(2) reclaimed by the trial-deletion collector
(reactive)**. `make test` = Result: PASS (33 gc unit tests green, no regressions). The
primitive is additive only; production execution paths are unchanged.

## 2026-07-03: Phase B (GC) kickoff — foundation slices of the cycle collector on Arc (ADR-0002)

With ADR-0002 confirming Phase A (the roast goal) achieved, started developing GC (Phase B;
ADR-0001's level 1 = a cycle collector on Arc). Following the implementation order of
[docs/gc-level1-detailed-design.md](../docs/gc-level1-detailed-design.md) §11, these slices
stack up only the foundation types and counters "compiled in, default off" (§9.1) — no
reclamation running yet (no `Value` variant replaced either):

1. **root visitor** (#4094): `Interpreter::visit_roots()` / `Env::visit_values()`. Enumerates
   every `Value` reachable from execution state in one place.
2. **child visitor** (#4096): `Value::visit_gc_children()` + `ArrayData`/`HashData`/
   `SubData`/`InstanceAttrs`/`SharedPromise`/`SharedChannel`. The "edges a node points to"
   primitive for walking candidate subgraphs.
3. **`MUTSU_VM_STATS` GC counter slots** (#4099): the 8 counters
   collections/candidate_pushes/dedup_hits/....
4. **Minimal implementation of `Gc<T>` / node header / candidate buffer** (this slice):
   `src/gc/gc_ptr.rs`.
   - `Gc<T>` = an `Arc<GcBox<T>>` wrapper. Inherits allocation, `Send + Sync`, and the
     unsizing coercion to the type-erased `ErasedGc` (`Arc<GcBox<dyn Trace>>`) from Arc,
     while `GcHeader` holds the Bacon-Rajan node state (a GC-visible strong count separate
     from the Arc's own, `Color` (Black/Purple/Gray/White), and the buffered flag).
   - The `Trace` trait (object-safe, `Send + Sync`) lets a node hand its direct `Gc` children
     to the collector.
   - A process-global candidate buffer (`Mutex<Vec<ErasedGc>>`): when a `Gc` drop leaves a
     survivor, that node is Purple-ed and pushed as a possible cycle root (with dedup).
     `drain_candidates()` is the seam to the synchronous collector of step 8.
   - With `MUTSU_GC` unset (the default), drops skip candidate registration entirely = fully
     inert. Enabled with `on`/`1`. Wired step 3's
     `record_gc_candidate_push`/`record_gc_candidate_dedup_hit`.
   - No `Value` variant is replaced `Arc→Gc`, so normal execution is unaffected (the next
     slice, step 5 = the first-wave replacement of `Array`/`Hash`/`ContainerRef`). Covered by
     unit tests (color transitions, dedup, inert, `Send + Sync`).

## 2026-07-03: Phase B (GC) — CI `gc-stress` job + cross-thread safety + container-identity fix

Added the CI job continuously validating the full suite under GC=on (design memo §9.3). Its
de-risking **found and fixed 2 real bugs**.

- **Cross-thread data race → fixed with a single-threaded gate**: a safepoint collect running
  concurrently with `start`/`Promise`/`hyper`/`race` worker threads touching shared
  containers caused data races/panics (VERIFY FAIL in `t/lock.t`, a real panic in
  `start-panic-boundary.t`). Worker interpreters `Arc::clone` the `shared_vars`, so
  implemented a minimal cooperative STW that collects **only in single-threaded intervals**
  via `Interpreter::gc_single_threaded()` (`Arc::strong_count(shared_vars) == 1`). Both the
  main and worker loops are gated, so no concurrent collect can occur. Full STW (correctness
  of concurrent collects) comes later.
- **Container-identity corruption → based `make_mut`/`get_mut` on `header.strong`**: with
  GC=on, the candidate buffer holds `Arc` clones of nodes, inflating the `Arc` count, so the
  `Arc::get_mut`-based uniqueness test spuriously COWed and broke container identity
  (aliasing) of slice-assignment lvalues etc. Fixed to judge by the live handle count
  (`header.strong`, excluding buffer refs) (GC-off has Arc count==strong, so unchanged).
- **The CI `gc-stress` job** (`.github/workflows/ci.yml`): runs `cargo test` + `prove t/` +
  the roast whitelist with `MUTSU_GC=on MUTSU_GC_EVERY_CANDIDATE=64 MUTSU_GC_VERIFY=1`.
  **Blocking gates = cargo test + "job fails on VERIFY FAIL detection"** (reliably catching
  heap corruption). `prove t/` and roast are `continue-on-error` (advisory): some tests fail
  on GC semantics the collector doesn't implement yet (`DESTROY`-on-reclaim etc.), but that
  is a known gap, not corruption, so non-blocking (promoted after implementation). The
  gc_stress integration test was fixed to clear the env so it does not depend on ambient
  `MUTSU_GC`.

Verification: GC-off `make test` = Result: PASS (no regressions). Under GC-on, cargo test
all passing; **VERIFY FAIL=0** on prove t/ and a whitelisted roast sample (collector sound);
concurrency tests (lock/start/hyper-race) passing.

## 2026-07-03: Phase B (GC) — debug/verification tooling `MUTSU_GC_LOG` / `MUTSU_GC_VERIFY` (§9.4/§9.5)

Implemented the collector's debugging-operations foundation. GC becomes untraceable when it
breaks without logging and verification (design memo §9.4 intro), so this is hardened before
the collector goes into full operation.

- **`MUTSU_GC_LOG=summary|detail|trace`** (`gc/collect.rs`): tiered logging of
  `off`(default)/`summary`/`detail`/`trace` (`1`/`on` = summary alias). Fixed grep-friendly
  `key=value` format with a `[mutsu gc]` prefix to stderr.
  - `summary`: per non-empty collect, `start cycle=N reason=R candidates=C` /
    `end cycle=N traced=T revived=V reclaimed=X roots=C pause_ns=P`. `reason` is the
    safepoint kind (`backedge`/`program-end`/`manual`…).
  - `detail`: adds `cycles=` (number of reclaimed cycles) to the `end` line.
  - `trace`: a `reclaim cycle=N id=…` line per reclaimed node.
  - Safepoint-driven collects were also unified onto `collect_cycles_at(reason)`, passing
    `SafepointKind::name()` as the reason.
- **`MUTSU_GC_VERIFY=1`** (§9.5): verifies **soundness invariants** before and after each
  collect.
  - Takes a pristine snapshot (reachable nodes + strong) before the collect,
  - **before reclaim**: every white (to-be-reclaimed) node has strong=0 (= only internal
    cycle edges = true garbage; reclaiming with strong>0 = freeing a live node =
    use-after-free is detected here),
  - **after reclaim**: survivors are black with non-increased strong (detecting
    scan_black over-restore/underflow-wrap).
  - During implementation, verify initially FAILed twice with "a survivor's strong was not
    restored to pristine" — but that was **a bug in the verify logic itself** (a survivor
    pointed to by reclaimed garbage legitimately loses that edge and does not return to
    pristine = Bacon-Rajan restores only survivor edges). Corrected to the right invariant
    and confirmed FAIL=0. Verify detecting → fixing its own bug ended up demonstrating the
    collector's soundness.

Verification: integration tests
`tests/gc_stress.rs::{verify_mode_reports_no_violations, log_modes_emit_expected_lines}`.
VERIFY FAIL=0 on real programs (cycle churn / compute). `make test` (GC off) = Result: PASS,
no regressions.

## 2026-07-03: Phase B (GC) — `gc_trace` completeness: also walk Gc nodes nested in wrappers (second-wave continuation)

Following the second wave (`Sub`/`Instance` → `Gc`, #4123), a slice that **plugs the
collector's edge-visibility holes**. Until now `gc_trace` yielded as children only the
migrated `Gc` node variants (Array/Hash/Set/Bag/Mix/ContainerRef/Sub/Instance), so **`Gc`
containers wrapped in non-node wrappers were invisible edges** (e.g. with a hash value of
`Pair("k", @a)`, that `@a`'s strong count was never decremented, missing any cycle through
`@a` = a leak).

- Added recursion arms for non-node wrappers to `gc_trace`: `Pair`/`ValuePair`/`Scalar`
  (itemized)/`Capture`/`Seq`/`Slip`/`HyperSeq`/`RaceSeq`/`Mixin`/`Junction`/`GenericRange`/
  `Proxy`. These walk the `Value` inside the wrapper to reach nested `Gc` nodes. `Box` is
  uniquely owned and `Arc<Vec>` sequences are immutable, so no cycle can exist except through
  a `Gc` node and the recursion always terminates. **`WeakSub` (`WeakGc`) is a weak edge and
  is not traced** (preventing over-collection).
- Verification: with `MUTSU_GC=on MUTSU_GC_EVERY_SAFEPOINT=1`, confirmed the cycles of
  `%h<p> = ("k" => %h)` (self-reference via Pair) and `@a.push($[@a])` (via an itemized
  Scalar) are reclaimed (reclaimed_nodes > 0). And the output of a Pair/wrapper-heavy compute
  program is **byte-identical** to GC-off (the added edges never falsely reclaim live data).
  Integration test
  `tests/gc_stress.rs::wrapper_nested_cycles_are_reclaimed_and_preserve_data`.
  `make test` = PASS.

Remaining Gc candidates: `LazyList` (third wave), `Promise`/`Channel` (async, §11 steps 6-7).

## 2026-07-03: Phase B (GC) — safepoint wiring: GC automatically reclaims cycles during execution (§11 step 8, second half)

The collector proper (previous entry) had only the manual `gc_debug_collect_now` call;
implemented the **safepoint wiring that automatically launches collects at the VM's re-entry
boundaries** (`src/gc/safepoint.rs`). This reaches the state where "GC runs on its own and
reclaims cycle leaks".

- **Safepoint**: wired `gc_safepoint(Backedge)` at the backward edge of the bytecode dispatch
  loop (between instructions = a safe re-entry boundary holding no container borrows, design
  memo §1.2). Gated by `gc_safepoints_armed()` (a cached bool), so **the default (`MUTSU_GC`
  unset) early-returns on 1 load** = normal execution is near-zero-cost with no collects at
  all.
- **Triggers** (design memo §9.2 first cut): `MUTSU_GC=on` + `MUTSU_GC_EVERY_SAFEPOINT=1`
  (collect at every safepoint = deterministic stress) or `MUTSU_GC_EVERY_CANDIDATE=N` (arm
  pending every N candidate pushes and collect at the next safepoint; `Gc::drop` may hold
  borrows so no inline collect, §1.2).
- **End-to-end verification**: with `MUTSU_GC=on MUTSU_GC_EVERY_SAFEPOINT=1` (collect every
  instruction), real Raku self-reference (`%h<self> := %h` / `@a[0] := @a`) and
  mutual-reference (`%x<y> := %y; %y<x> := %x`) cycles are reclaimed (`reclaimed_nodes` /
  `reclaimed_cycles` > 0), and **a compute-heavy program's output is byte-identical to
  GC-off** = no live data is ever corrupted (the strong counts correctly count every live Gc
  handle, so trial deletion reclaims pure garbage cycles only). Integration test
  `tests/gc_stress.rs` (run in CI via cargo test). `make test` (GC off) = Result: PASS, no
  regressions.

Remaining: additional safepoint kinds (call/return/await/thread_join etc.), cross-thread
cooperative STW (concurrent collects), `MUTSU_GC_AT`/random stress, Gc-ification of
`Promise`/`Channel`/the supply registry (async cycles), `Sub`/`Instance` (second wave) →
`LazyList` (third wave).

## 2026-07-03: Phase B (GC) — the trial-deletion collector proper (§11 step 8): actually reclaiming cycles

With the first wave (migrating all container families `Arc → Gc`) complete, implemented the
**Bacon-Rajan trial-deletion collector that actually reclaims cycles**
(`src/gc/collect.rs`). Steps 4-5 so far were "compiled in, default off" groundwork with zero
runtime behavior; this is the first slice where GC actually "runs".

- **Algorithm** (Bacon & Rajan, synchronous variant): drain the candidate buffer and run
  trial deletion on the reachable subgraph. `mark_gray` (tentatively delete internal edges =
  decrement children's GC strong counts) → `scan` (nodes with `strong>0` have external
  references and are restored via `scan_black`; `strong==0` is white = pure cycle garbage) →
  `collect_white` → `reclaim`. The GC strong count is trial-deletion scratch, and survivors
  are fully restored by `scan_black`, so the heap's refcounts are intact after the collect.
- **Reclaiming on Arc**: `Trace::drop_gc_edges(&mut self)` (a container clears its own
  Value-holding collections) cuts white nodes' outgoing edges, dropping internal Arc
  references and letting Arc free them. The `&mut` is obtained by the collector via the
  backing Arc's pointer (`gc::gc_drop_edges`, avoiding `invalid_reference_casting`). During
  reclaim, `CollectGuard` makes `Gc::drop` inert (skipping strong/buffer bookkeeping, Arc
  drop only) → preventing double decrements/underflow.
- **Safety**: **manual/opt-in only** (`gc_debug_collect_now`). There are no production
  callers yet (safepoint wiring is the next slice), and with `MUTSU_GC` unset the candidate
  buffer is empty so a collect is a no-op. Hence normal execution and CI are unchanged.
- **Verification**: unit tests (`gc::collect::tests`) with real drop counters confirm
  reclamation of 2-node / self cycles, preservation of cycles with external references,
  non-reclamation of acyclic candidates, and empty-buffer no-op.
  `make test` = Result: PASS (14076 tests, no regressions).

Next is the rest of §11: safepoint wiring (auto-launching collects at re-entry boundaries) +
Gc-ification of `Promise`/`Channel`/the supply registry (async cycle support) +
`Sub`/`Instance` (second wave) → `LazyList` (third wave).

## 2026-07-03: Phase B (GC) first wave complete — migrated `Value::Array` (5c) + `Value::ContainerRef` (5d) to `Gc`

Following Hash (5b), migrated the remaining 2 containers of the first wave, completing the
`Arc → Gc<T>` replacement for Array/Hash/ContainerRef (the first wave of ADR-0001 layer 3a).

- **5c. `Value::Array(Arc<ArrayData>)` → `Gc<ArrayData>`** (#4116): `impl Trace for
  ArrayData` + the `gc_trace` Array arm. Resolved ~50 files / 353→0 compile errors
  type-driven. **Deleted the now-dead `value::aliased_mut::arc_contents_mut`** (its only user
  disappeared with both the Array and Hash migrations) → the containers' "raw-pointer writes
  into shared Arc contents" (the documented-unsound path violating Stacked/Tree Borrows) is
  gone, consolidated into the single `gc_contents_mut` chokepoint.
- **5d. `Value::ContainerRef(Arc<Mutex<Value>>)` → `Gc<Mutex<Value>>`**:
  `impl Trace for Mutex<Value>` (lock → the inner value's `gc_trace`) = bind cells become
  nodes of the Gc graph, and self-bind cycles (`%h<k> := %h` / `@a[0] := @a`) become
  traversable by the collector. Minimal migration (breaking sites are construction + type
  annotations only; most of the 215 `ContainerRef(` sites stay unchanged via
  `.lock()`/match on the `Gc` deref). The 4 spots where sed swept up non-Value
  `Mutex<String>`/`Mutex<Option<ChildStdin>>`/`Mutex<HashMap>` were individually reverted.
  §3.3's typed-constraint `CellValue`-ification is deferred to future Track B (currently
  ContainerRef carries no type constraint, so `Gc<Mutex<Value>>` is a faithful 1:1).

Verification: both slices `make test` = Result: PASS (1459 files/14076 tests). 5c: all
whitelisted array roast; 5d: all S03-binding roast (scalars/arrays/hashes/nested/ro/closure)
green. Binding semantics preserved (`:=`, element bind, `=:=` identity, rw params,
cross-thread shared push). `Set`/`Bag`/`Mix` were also migrated in the parallel PR #4117.
Remaining Arc containers: `Sub`/`Instance` (a later wave), `LazyList` (third wave). Next is
§11 step 6+ (`Promise`/`Channel` → supply registry → the trial-deletion proper with
safepoint collect).

## 2026-07-03: Phase B (GC) first wave started — migrated `Value::Hash` to `Gc<HashData>` (§11 step 5)

Following step 4 (the `Gc<T>` foundation), started the first wave of ADR-0001 layer 3a (the
container-family `Arc → Gc<T>` sweep) as per-type slices.

- **5a. Making `Gc<T>` a drop-in for `Arc<T>`**: `make_mut`/`get_mut`/`ptr_eq`/
  `strong_count_of`/`as_ptr`/`erased`/`From`/`Debug`/`AsRef`/`PartialEq` (value comparison
  like Arc; identity is `ptr_eq`) + a free `gc_contents_mut` (the Gc version of
  `aliased_mut::arc_contents_mut`, same unsoundness contract) + the `ContainerMakeMut` bridge
  trait (COW `make_mut` covering both `Arc<T>`/`Gc<T>`, so shared macros can straddle
  Array(Arc)/Hash(Gc) mid-migration). Uniqueness is judged by `header.strong` (live GC
  handles, excluding the candidate buffer's Arcs) = identical behavior to the current
  `Arc::strong_count` with or without the buffer → the migration is behavior-preserving.
- **5b. `Value::Hash(Arc<HashData>)` → `Value::Hash(Gc<HashData>)`**: added
  `impl Trace for HashData` (walking the map's Values via `Value::gc_trace`) + the Hash arm
  of `Value::gc_trace`; mutation goes through `gc_contents_mut`, COW through `Gc::make_mut`,
  identity through `Gc::ptr_eq`. Resolved ~180 compile errors across 49 files type-driven
  (mostly mechanical `Arc::*`→`Gc::*`; the rest were helper signatures
  `&Arc<HashData>`→`&Gc<HashData>` and `Value::Hash(Arc::new)`→`Gc::new`).

Verification: `value_stays_small` is green = `Gc<HashData>` is the same 1 word as Arc and
`Value` does not bloat. All whitelisted S32-hash roast
(delete/exists/kv/pairs/push/slice/iterator/invert/map …) + S02-types/hash.t green.
Container identity preserved too (`my %g := %h; %g<d>=4` reflected in `%h<d>` / bound shares
`.WHICH`, assignment copies differ in `.WHICH`). The pre-existing partial failures of the
non-whitelisted multislice-6e (multi-dim slice assignability) / perl.t (scalar-deconted
perlify) are unrelated to this migration (unimplemented features). With `MUTSU_GC` unset
there is no candidate registration = just 1 atomic instruction per hash clone/drop. Next is
5c (`Value::Array` → `Gc<ArrayData>`, same shape, ~1400 sites).

## 2026-07-03: `S03-metaops/hyper.t` complete (420/420, added to whitelist)

Whitelisted the dispatch cluster's last file, `S03-metaops/hyper.t`, resolving the remaining
2 items.

1. **test 407 — builtin-MRO all-candidates dispatch (`».+&` / `».*&`)**: made the VM side
   able to call all candidates across multi-level class hierarchies like List/Any/Cool for
   `.+`/`.*` (`src/vm/vm_call_helpers.rs` / `src/vm/vm_hyper_method_ops.rs` /
   `src/runtime/methods_call_helpers.rs` / `src/runtime/methods_signature_shaped.rs`).
2. **test 408 — atomicint reset write-back inside closures**: a `$r = 0` (plain assignment
   to an `atomicint`) inside a nested closure frame like `subtest {...}` did not reset the
   shared atomic cell, so the next `⚛++` kept reading the old value (`$runs` accumulating
   2→4→6→8). Two root causes:
   - `reset_atomic_var_key` looked up the `name→value_key` mapping only in the current
     frame's `self.env`, silently no-oping from nested frames where the mapping exists only
     in `shared_vars` (the cross-frame authority). → Added the `shared_vars` fallback.
   - Closure free-variable writes go through the **SetGlobal** path, not SetLocal, and there
     was no reset call there. → Added the same atomic reset as SetLocal to the SetGlobal
     store (`src/vm/vm_exec_dispatch.rs` / `src/runtime/runtime_shared_vars.rs`).
   The same-shaped plain-int case worked only because it went through the normal
   captured-writeback machinery. Regression test =
   `t/atomicint-closure-reset-writeback.t`. No regressions in the existing atomic/cas roast
   or local tests.

This brings the remaining items of BLOCKERS.md §5 dispatch cluster to zero.

## 2026-07-02: `S06-operator-overloading/infix.t` — comma-operator overloading brings 50/50 (whitelisted, #4072)

Resolved the remaining 2 items of BLOCKERS.md §2.4 (tests 31-32): overloading the comma
operator itself via `sub infix:<,>($a,$b){...}`
(`EVAL('sub infix:<,>($a,$b){42}; 5,5')` → 42) and de-chaining of custom operators defined
with `is equiv`; `roast/S06-operator-overloading/infix.t` reached 50/50 and is whitelisted
(PR #4072). Tests 11 and 33 are known `# TODO`s that rakudo itself marks divergent, so they
are not a problem at the TAP level.

(This entry was recorded retroactively on 2026-07-07 because BLOCKERS.md §2.4 had been left
un-updated after #4072. For implementation details see PR #4072's diff.)

## 2026-07-02: true lazy-array campaign — resolved the 6 root causes in `S09-subscript/slice.t`

`S09-subscript/slice.t`, the last file of BLOCKERS.md §4 "true lazy arrays / infinite
sequences", used to abort at test 39/56 with a panic on an unimplemented feature; fixed it to
run 56/56 to completion (not yet whitelisted — the remaining 6 items are separate features
unrelated to laziness, details in `TODO_roast/S09.md` — since retired; open items now live in
BLOCKERS.md). Behind one test file were 6
bugs/unimplemented features with distinct root causes:

1. **`:=` bind force-materialized genuinely-lazy `LazyList`s**: only the `coroutine` family
   was kept lazy; `sequence_spec`/`closure_seq`/`lazy_pipe` (infinite sequences like
   `(0...*).cache`) were converted at bind time to an Array with a 100k cap. BIND, unlike
   `=` assignment, binds the container as-is, so mutation support is not a concern and lazy
   retention is always fine (`vm_var_assign_set_local.rs`).
2. **`LazyList::is_genuinely_lazy()` missed "`.lazy` on a finite list"**: `lazy 3,4,5` has
   only a cache with no coroutine/body, so the old logic
   `(coroutine.is_some() || !body.is_empty() || ...) && is_lazy_marked()` came out false and
   `.is-lazy` returned `False`. `is_lazy_marked()` is a marker exclusive to the `lazy`
   prefix/`.lazy` calls, so it turned out to be sufficient on its own (`value_lazy.rs`).
3. **The parser for the `lazy`/`eager`/`hyper` statement prefixes did not wrap the whole
   following comma list**: `expression_no_sequence` reads only a single term, so
   `lazy 3,4,5` parsed as `(3.lazy, 4, 5)` and `lazy 1...*` as `(1.lazy)...*`. Fixed by
   adding `parse_prefix_listop_operand`, which consumes the whole comma-separated list like
   `return`'s `parse_comma_or_expr` (`parser/expr/postfix/loop_.rs`). Unglamorous, but a
   broad general bug affecting any code using `lazy`/`eager`/`hyper`.
4. **Nested `LazyList` sub-indexes were not recursed on the read side**: the
   `(lazy 3,4,5)` part of `@a[1,(lazy 3,4,5)]` was matched only against
   `Value::Array`/`Seq`/`Slip`; `Value::LazyList` was unsupported (`vm_var_index_ops.rs`).
5. **A dual-store bug where flat slice assignment used in expression context loses the
   write**: `@a[1,3] = "x","y"` works correctly as a bare statement, but used as an
   expression, as in `say (@a[1,3] = "x","y")`, it wrote back only to the `env`-side copy,
   desyncing from the `locals` slot so stale values were read. The cause: only the
   slice-assignment branch early-returned, skipping the shared locals-resync tail
   (`vm_var_assign_index_named.rs`). A general correctness bug unrelated to laziness.
6. **Assignment/bind into nested lazy sublists was unimplemented**:
   `@a[1,(lazy 3,4,5)] = "a"...*` and top-level `@a[lazy 1,2,(4,5)] = ...` panicked with
   "Index out of bounds". Implemented via the new `SliceKeyTree`, separating the write pass
   (`assign_slice_key_tree`) from `read_slice_key_tree`, which re-reads the container after
   assignment completes to assemble the nested return value (with duplicate indices
   overwritten by later writes, Raku's return value reflects the "final state" rather than
   the "value at write time", so it cannot be built in a single pass). Used `boundary_len`
   (the original length before the assignment starts, snapshotted before autoviv-resize) to
   reproduce raku's behavior: lazy-derived nesting is cut off at the boundary while plain
   nesting extends as usual.

Regression test: `t/nested-lazy-slice.t` (new). As a side effect nearly broke
`t/gather-lazy.t` as a regression of an existing bug (applying the #2 fix verbatim to the
BIND side made plain gather's `:=` bind forced — plain gather is `.is-lazy` False but must
not be forced on bind); resolved by correcting to the OR condition
`coroutine.is_some() || is_genuinely_lazy()`. `make test` fully passing; the
regression-watch roast files listed in `docs/lazy-arrays.md` all verified too.

## 2026-07-02: `S17-lowlevel/lock.t` — resolved the `Lock.protect` race condition + 2 derived bugs (added to whitelist)

Resolved the "genuine race condition" tracked in BLOCKERS.md §6.1; now whitelisted at 48/48
(both `Lock` and `Lock::Soft` subtests 24/24). Behind one symptom were 3 independent bugs.

1. **The race condition proper**: `Lock.protect`/`.lock`/`.unlock`, unlike
   `Semaphore.acquire`/`.release` (introduced for semaphore.t in #3992), never called
   `enter_critical_section`/`leave_critical_section`, so the `shared_critical_dirty`
   resync/writeback chokepoint was not functioning for `Lock`. The call path was duplicated
   in 3 places (the `native_lock` slow path and 2 VM-side fast paths), all fixed with the
   same pattern. Regression test: `t/lock-protect-shared-scalar.t`.
2. **Array-element bind corruption (an independent compiler bug)**: discovered a bug —
   reproducible standalone (no threads) — where running `my $x := @a[$i]` in a loop corrupts
   the element bound in the *previous* iteration. The cause: the `:=` VarDecl/rebind
   compilation shared `compile_call_arg`, which for `Expr::Index` arguments unconditionally
   emitted the temporary global variables meant solely for `is rw` argument write-back of
   `f(@a[$i])` (`__mutsu_index_rw_arg_N`/`__mutsu_index_rw_orig_N`, names fixed at compile
   time). Inside a loop, the same bind statement reuses the same fixed global name every
   iteration, so `SetGlobal`'s "write-through to an existing `ContainerRef`" (meant for
   rebinding `our` variables) kicked in and the new iteration's cell reference was written
   into the previous iteration's cell. Added a new compiler flag `bind_target_direct`, set
   only at the 3 bind/rebind call sites and read-and-immediately-cleared at the top of
   `compile_call_arg_with_escape`, preserving `is rw` write-back for genuine function calls
   nested in a bind RHS (`my $x := f(@a[$i])`) while completely skipping the mechanism for
   the bind target itself. Regression test: `t/bind-array-elem-in-loop.t`.
3. **A TAP output-ordering bug**: `Thread.start` ignored `clone_for_thread`'s
   `parent_in_subtest` and unconditionally set `immediate_stdout = true`, so the `pass` of a
   fire-and-forget thread started inside `subtest { ... }` leaked directly to top-level
   stdout ("ok 5" printed before the subtest header). Made `dispatch_thread_start` respect
   `parent_in_subtest`, and added `drain_shared_thread_output()` to both
   `dispatch_thread_finish` and `finish_subtest` (for the fire-and-forget pattern that never
   calls an explicit `.finish`).

As a side finding, `Lock::Soft` (a type that should behave like `Lock` outside the JVM) was
not recognized as a type at all. Solved by simply registering it as an MRO subclass of
`Lock`, following the existing `Lock::Async` alias pattern (`native_lock` dispatches on
attributes, so it does not depend on the class name).

## 2026-07-02: PLAN.md §H "★the main prize" CompUnit::Repository — closed the install→use bridge

The north-star task of making modules installed by zef etc. `use`-able. Investigation showed
`CompUnit::Repository::FileSystem`/`::Installation`, `install`/`need`/`resolve`, and
`use lib "inst#..."` chaining were already implemented — not a build-from-scratch, but
filling the 3 missing spots of the "bridge":

1. **Missing attributes on the default `$*REPO` instance**: the default `$*REPO` created at
   startup was constructed directly via `Value::make_instance`, lacking the `short-id`
   attribute and the `IO::Path`-ification of `prefix` that explicit `.new()` construction
   grants (`$*REPO.short-id` died with `No such method` — a real bug). Fixed to set the same
   attributes as `.new()`.
2. **Well-known-name support in `repository-for-name`**:
   `CompUnit::RepositoryRegistry.repository-for-name("site")`, which zef's installer actually
   calls (`Zef::Install.rakumod`), handled only the `"file#..."` prefix and always returned
   `Nil` for well-known names. Added a `default_repo_dir(kind)` helper following the same XDG
   pattern as `precomp.rs::cache_dir()`, returning a `CompUnit::Repository::Installation` for
   each of `site`/`home`/`vendor`/`perl`.
3. **The bridge proper: auto-registering the default site repository into `use`
   resolution**: with (1)(2) alone, `.install()` succeeds but a plain `use ModuleName`
   (without `-I`) still fails because `resolve_module_path` looks only at `self.lib_paths`.
   Solved by calling `add_default_site_repo()` (adding `inst#<default-site-dir>` to
   lib_paths) at the end of `Interpreter::new()`. **Key discovery**: putting this only in
   `main.rs` was insufficient — `Test::Util`'s `is_run` has a fast path that in many cases
   does not launch a real subprocess but runs an in-process nested interpreter via
   `Interpreter::new()` (`src/runtime/test_functions/subprocess.rs`), bypassing `main.rs`.
   Building it into `Interpreter::new()` itself makes it apply uniformly to every
   Interpreter instance — CLI, REPL, EVAL, and is_run's in-process path.

Guarantee = `t/compunit-repository-for-name.t` (the type of `repository-for-name("site")`,
the default `$*REPO.short-id`, the `Nil` fallback for unknown names, and an end-to-end
verification of install → `use` succeeding in a fresh process).

**Items explicitly left out of scope** (organized into PLAN.md §H item 2): 2 separate bugs
found during the investigation that block end-to-end execution of an actually vendored zef
binary — (1) a gap in the coercion catch-all where binding a non-Hash object that duck-types
Associative (`has %.hash handles <AT-KEY EXISTS-KEY ...>`) to a `%`-sigil attribute of
`Zef::Client` passes through unchecked; (2) a parser error stopping compilation of the real
`Zef::CLI.rakumod`. Both are independent of the correctness of CompUnit::Repository itself.

## 2026-07-02: §F typed exceptions — element bind into a non-container throws X::Bind (#4053)

`(List)[0] := 1` / `(Int)[0] := 1` (positional element bind into a type object)
**silently succeeded**, because the generic index-assign op (`IndexAssignGeneric`) swallowed
targets other than Array / Hash / HashEntryRef / Stash in a no-op `_` arm. A type object has
no slot to bind into, so it should throw X::Bind (asserted by roast/S32-exceptions/misc.t).
The bind marker (`__mutsu_bind_index_value`) is now tracked at value extraction, and if a
bind reaches the non-container fall-through, X::Bind is thrown. Defined-Int / Str LHS
(`10[0] := 1` etc.) is already rejected at parse time as an invalid bind LHS (likewise
X::Bind). Normal assignment and Array/Hash element bind are unchanged.

Result: the 2 `#?rakudo todo 'wrong exception'` cases in `S32-exceptions/misc.t` (already
whitelisted) turned green (**180/182 → 182/182**). mutsu throws exactly the X::Bind the test
expects (rakudo itself throws a BIND-POS type-object error, hence the TODO in roast).
pin: `t/bind-into-non-container.t`.

## 2026-07-02: §2 D — OTF-ification of multi candidates with `state` / caching-proto bodies (#4047)

Part of the pre-GC groundwork (PLAN §2 D, removing multi-dispatch tree-walk fallbacks).
Multi candidates declaring `state` (and caching-proto bodies
`proto cached($a){ state %cache; %cache{$a} //= {*} }`) were forced onto the tree-walk
interpreter fallback on the assumption "OTF-compiling loses the `state` cell's identity". In
fact it is not lost: `otf_compile_cache` (fingerprint+package keyed) returns a stable
compiled body per candidate, so the `state` key (`__state_{pkg}::{$var}@{ip}`, embedding the
compiled opcode position) is stable across calls, and the candidates' `/n`-suffixed packages
separate distinct candidates' `state` cells.

Removed the `function_body_declares_state` exclusion from the 4 multi-candidate OTF forks
and the non-trivial-proto-body fork. Beyond removing the fallback, this **also fixes a
correctness bug**: the tree-walk fallback shared the `state` cell between distinct same-named
candidates (`multi f(Int){state $c} multi f(Str){state $c}`), returning `1,2,3,4,5` (wrong)
via the proto (correct = `1,2,1,2,3`).

The only boundary kept on the interpreter is signature **alternates**
(`multi f(A) | (B) { state $c }`): a single routine sharing `state` across all alternates
(compile-time state_group). Alternates are registered as separate candidates with identical
bodies, so OTF keyed by per-alternate body fingerprint would split the sharing. The resolved
per-candidate `FunctionDef` carries no alternate marker, so registration records them into
`multi_alternate_signature_names` (`FxHashSet<Symbol>`), referenced by the new
`multi_candidate_state_forces_interpreter` gate. pin: `t/multi-candidate-state-otf.t` (the
OTF win path) + the existing `t/multi-signature-alternates.t` (alternate sharing).

## 2026-07-01: dispatch cluster — `wrap.t` / `dispatching.t` complete (added to whitelist)

- **`S06-advanced/wrap.t`** (90/90; 7 blockers resolved in #3973, finished in #3981):
  candidate selection did not look at the multi-method's full-MRO candidate list, so
  `candidates[N].wrap` rewrote the wrong candidate. Aligning candidate selection to indexes
  over the whole MRO made `wrap`/`unwrap` act correctly on the intended candidate.
- **`S06-advanced/dispatching.t`** (8/8, added to whitelist): with the candidate-resolution
  problem shared with wrap.t resolved, all subtests passed together.
- Remaining dispatch cluster: `S03-metaops/hyper.t` (400/408 run, 8 failures).
  `S03-operators/assign.t` later reached 302/302 (see below). Details in BLOCKERS.md §5.

## 2026-07-01: across-the-board progress on the S17 concurrency foundation (semaphore / then / stable / migrate / syntax)

- **`S17-lowlevel/semaphore.t`** (2/2, added to whitelist, #3992): moved Semaphore's critical
  section onto the `call_protect_block`-style reconcile/writeback chokepoint, stabilizing
  read-modify-write on shared scalars/arrays even in bare critical sections.
- **`S17-promise/then.t`** (19/19, added to whitelist, #3989): drain worker-thread output at
  the `.result` synchronization point, fixing missing output in `Promise.then` chains.
- **`S17-supply/stable.t`** (7/7, added to whitelist, #3990): implemented the
  `Supply.stable` debounce combinator.
- **`S17-supply/migrate.t`** (13/13, added to whitelist, #3999): implemented the
  `Supply.migrate` combinator.
- **`S17-supply/syntax.t`** (kept tap-callback writeback across quit handlers, #3994;
  additionally fixed captured-outer lexicals to be shared between sibling whenever
  callbacks, #4018): advanced to 88 of 90. The remaining test 75 (a nested `whenever`'s
  on-demand supply goes down the non-react tap path instead of the react loop, so
  `on_close_callbacks` never fires) and test 90 (cross-whenever `last` ordering, a hard
  case) have root-cause analyses done (see `TODO_roast/S17.md` — since retired; open items now
  live in BLOCKERS.md); whitelisting deferred.
- Remaining S17 issue: `S17-lowlevel/lock.t` (increments lost on a shared counter under
  `Lock.protect` — a genuine race condition). Details in BLOCKERS.md §6.

## 2026-07-01: `S17-promise/start.t` complete (64/65→65/65 equivalent, added to whitelist)

Made fire-and-forget threads' stdout flush in execution order (#4010, `flush thread-clone
stdout in execution order`). Previously the arrival order of TAP `pass`/`ok` output from
other threads was not guaranteed, producing "out of sequence" warnings; resolved.
`roast/S17-promise/start.t` whitelisted.

## 2026-07-01: `S07-hyperrace/basics.t` complete (90/90, added to whitelist, #4001)

Enforced the single-iterator contract on `HyperSeq`/`RaceSeq` (making `.iterator` uncallable
more than once). Advanced 87→90 and whitelisted.

## 2026-07-01: `S32-io/io-cathandle.t` complete (31/31, added to whitelist, #4005)

The `IO::CatHandle` implementation itself was already done, but the remaining 2 subtests
(`on-switch`, `perl`) uncovered and fixed 3 general bugs:

1. `call_sub_value(..., merge_all: true)` (the path where native Rust calls a Raku
   callable — not just on-switch callbacks but also Promise/Supply taps, `reduce`, and
   lvalue Proxy FETCH/STORE), when recording writes to captured outer variables as targets to
   reflect into the caller's local slots, restricted the value types to
   `Bool`/`Int`/`Num`/`Str`/`Rat` only, making assignments of List or Nil invisible
   (`resolution_call_sub.rs`). Regression test:
   `t/native-callback-captured-write-nonscalar.t`.
2. `IO::Path.raku` derived the class name from the `$!SPEC` attribute rather than the actual
   `class_name` (`IO::Spec::Unix` → `IO::Path::Unix`), so `.raku.EVAL` of a plain `IO::Path`
   instance produced an instance of a different class and `eqv` failed
   (`native_io_path.rs`). Regression test: `t/io-path-raku-class-roundtrip.t`.
3. `IO::Handle`'s `is-deeply`/`eqv` compared even `handle` (an internal fd-registry id that
   differs per `.open()`) and `mode`, so two independent opens of the same file were not
   judged equal. After confirming the same scenario yields `True` on real Rakudo 2022.12,
   added a dedicated `PartialEq` branch comparing only
   `path`/`chomp`/`nl-in`/`nl-out`/`encoding`/`bin` (`value_eq.rs`). Regression test:
   `t/io-handle-eqv-ignores-open-state.t`.

With this, the IO trio `S32-io/io-handle.t`, `io-cathandle.t`, and `lock.t` are all
whitelisted.

## 2026-07-01: `S03-operators/assign.t` complete (302/302, added to whitelist)

A series of fixes around itemization (item-containerizing rvalues) advanced 270→302, now
whitelisted. Highlights:

- Computed single-index / single-hash-key assignment, 1-element array slices, and `$(EXPR)`
  item-assignment itemize the rvalue (#1819e933, #6b0b033d, #52cc5b66, #450ec80b).
- A list-destructure rvalue belongs to the LHS target side, not the RHS (#84aae448).
- A computed slice assignment's rvalue is the assigned value itself (#1e7682e5).
- Package-qualified scalar stores sync with the `our` lexical alias (#5585ff65).
- Reverse meta-operator assignment `$x R op= $y` targets arg1 (#09d96f6b).
- `,=` on a hash/array container reads the LHS as that container (#424b48a5).
- Detail fixes: list precedence of word-form compound ops, `,=`'s list assumption,
  `~>`/`~<`, `%=` on `:U`, etc. (#8aa5f1b9 as the finisher).

## 2026-07-05: PLAN.md fully restructured around the goal "a batteries-included Raku implementation"

By user policy, the project goal was codified as "**a Raku language interpreter where just
installing mutsu gives you a well-documented standard bundled library and you can write
practical code immediately**" (the Raku version of the positioning bun took for JavaScript),
and PLAN.md was restructured.

- Created the new §1 "🔋 Batteries" as the centerpiece: fixing the bundle set, the vendoring
  mechanism, promoting the home-grown `t/lib` libraries (Tubu / DBDishLite), building out the
  `docs/batteries/` documentation, a working-modules regression CI, mzef (absorbing the
  existing north-star), and binary distribution.
- Completed descriptions from the old structure (Tubu / Humming-Bird / flip-flop & feed /
  Match captures / the NativeCall MVP series / the DBIish investigation conclusions /
  HTTP::Status) moved to the archive section at the end of [news/2026-06.md](2026-06.md).
  Completed checklist items of substrate (§2), perf (§G), and roast (§F) were also compressed
  to one-line references into news.
- Added a batteries-included axis to the metrics table (bundled library count = currently
  0 / target 10+, mzef, binary distribution, GC) and updated the whitelist to 1350/1463.

## 2026-07-12: symbol intern round-trips eliminated — `Symbol::as_str()` (leak scheme) makes the "large refactor" unnecessary

Resolved PLAN §5's "remainder of symbol intern round-trips (large refactor)" not by
Symbol-keying the registry/dispatch APIs but by **leaking the interned strings**. The symbol
table is append-only and valid for the process lifetime, so `Box::leak` at intern time plus
an `id_to_str: Vec<&'static str>` lets `Symbol::as_str() -> &'static str` return **without
holding a lock and without allocating**. The constraint the old PLAN assumed — "long-lived
`with_str` is impossible due to read-lock deadlock" — itself vanished, and the downstream
`&str` APIs (`call_compiled_method`/`push_method_dispatch_frame`/
`check_method_wrap_chain`/registry lookups) work as-is without signature changes.

- **Dispatch hot-path conversion**: the main dispatch preludes (interpret/mut) reuse the
  receiver's class `Symbol` directly + borrow via `as_str` (removing both the String
  allocation and the re-intern). Also converted `owner_class.resolve()` on
  fast_method_cache hits, the private `!m` and metamethod `^m` preludes, the native-new
  purity check, the IO instance prelude, and comparisons like `== "Failure"`.
- **Per-thread resolve cache**: with as_str now hot, the RwLock read surfaced at 4.1% self,
  so added a thread-local `Vec` memo of `id -> &'static str` mirroring `INTERN_CACHE`.
- **Measured** (release, P-core pinned, `perf stat -r 5`): method-call **-6.1% cycles**
  (952M→894M), bench-class flat (the dominant term is Value clone/attrs = Lever 2
  territory).
- Remaining: `Symbol::intern(method)` shrank to a flat ~0.8% (including the #4402 TLS cache
  effect). Pre-interning CompiledCode constants (a serde-skipped lazy side table would not
  ripple into precomp serialization) and the SipHash of registry String keys remain as
  separate small levers.

## 2026-07-12: the 2 remaining symbol levers done too — per-call method-name intern removal + registry FxHash-ification

Collected the 2 levers "left over" from the entry above within the same day (PLAN §5's
symbol item is hereby complete and deleted):

- **Pre-interning method names**: added a lazy Symbol side table co-located with the
  constant pool on `CompiledCode`
  (`const_syms: OnceLock<Box<[OnceLock<Symbol>]>>` + `const_sym(idx)`).
  The `CallMethod`/`CallMethodMut` opcodes settle the method Symbol from the constant slot
  once (interning only names rewritten by the `^`/`!` modifiers) and pass it through to the
  prelude via the new `try_compiled_method_or_interpret_sym`/
  `try_compiled_method_mut_or_interpret_sym`. The per-call `Symbol::intern` (a TLS hash
  lookup) is replaced by a Vec index.
  **There was no ripple into precomp serialization**: `CompiledCode` is not serde-enabled
  (the precomp cache stores the AST), so the old PLAN's concern was wrong at the premise.
- **FxHash-ifying the registry**: switched every map/set of `Registry` (~40 fields) to
  `FxHashMap`/`FxHashSet` (declarations unchanged via in-file aliases; ~10 external
  signatures adjusted). Registry keys are program identifiers, not attacker-controlled, so
  HashDoS hardening is unnecessary.
- **Measured** (release, P-core pinned, `perf stat -r 5`, vs the #4441 binary):
  method-call **-2.7% cycles / -5.5% instructions** (880M→856M / 2403M→2271M),
  bench-class **-2.2% cycles** (2995M→2928M).

## 2026-07-12: bench-fib <2x goal achieved — Int/Num fast paths for `<=`/`>`/`>=` + removing GetLocal's String clone

A follow-up slice right after the fib regression fix (below). `perf record` (bench-fib)
revealed "`to_big_rat_parts` at 2.3% despite using only Ints" — `exec_num_lt_op` has an
Int/Num fast path but **`<=`/`>`/`>=` did not**, so the hot loop's `$n <= 1` fell into the
generic compare (BigRat conversion + alloc) every time. Added the same fast path as NumLt to
`exec_num_le_op`/`exec_num_gt_op`/`exec_num_ge_op` (Junctions/type objects/BigInt stay on the
generic path = semantics unchanged). Also changed `exec_get_local_op`'s per-GetLocal
`code.locals[idx].cloned()` (606k String allocs/run in fib) to a `&str` borrow, and made the
3 frame-less fast paths reuse their per-call locals `Vec` via `locals_pool`.

**Results** (release, quiet machine, perf stat -r5): bench-fib 0.548→**0.398s** (vs raku
**1.92x** — the `<2x` goal ✅), fib 0.155→**0.150s** (**0.91x**), int-arith 0.12→**0.089s**
(**0.60x** — it also helped the comparison inside range loops).
bench-class/method-call/bench-hash/bench-array unchanged.

## 2026-07-12: resolved fib's absolute 2.3x regression — ?LINE from env to a field + reusing empty overlay tiers (fib beats raku at 0.94x)

Took the absolute fib/bench-fib regression discovered that morning (~2.3x slower vs
2026-05-24) from measurement to root fix.

- **GC was innocent**: fib/bench-fib are within noise between `MUTSU_GC=off` and on (#4420's
  buffered-flag fast path had already reduced the per-drop cost to 1 relaxed load; fib(25)
  is collections=1, total pause 86µs). The prime suspect "GC default on" was dismissed.
- **The real culprit = `Env::flattened`'s recursive deep clone**: `perf record` showed >40%
  of fib's execution time in `Env::flattened`'s full HashMap clone (+ ~28% in its drop).
  Each recursive call stacked a scoped-overlay tier, and every time `MAX_OVERLAY_DEPTH` (16)
  was reached the whole chain was recursively flattened. The only thing making overlays
  non-empty was the per-statement `SetSourceLine` opcode inserting `?LINE` into the env
  (728k times in fib(25) = 21% of all opcodes, also inducing CoW map forks).
- **2 fixes**: (1) moved `?LINE` from an env entry to the `Interpreter::cur_source_line`
  field (save/restore at frame boundaries via `VmCallFrame`/`CallFrameEntry`; ~10 readers
  switched to field reads); (2) `Env::scoped_child` skips parent tiers that are "empty
  overlay + no tombstones" and reuses the parent chain — pure recursion now has constant
  chain depth and flattening disappears in principle.
- **Results** (release, quiet machine, `perf stat -r5`, taskset 0-3, raku re-measured under
  identical conditions): fib **0.85s→0.155s** (raku 0.164s — **0.94x, beating raku**; also
  2.4x better than May 2026's best of 0.37s), bench-fib **2.51s→0.548s** (10.5x→**2.6x**),
  bench-class 0.23s→**0.181s** (**0.93x**), method-call 0.22s→**0.182s** (**1.14x**).
  fib(25)'s container-`Gc` drop churn 17.5M→**14**.
- **pins**: `t/source-line-deep-recursion.t` (depth-40 recursion, callframe line after
  return, non-empty-overlay recursion, the `$?LINE` constant) +
  `env::tests::empty_tiers_are_reused_not_stacked`.
- **Measurement trap (recurred; recorded)**: the second contamination incident that day — a
  concurrently running session's build/tests inflated the raku baseline 2.4x (0.164s→0.393s),
  nearly misreading it as "mutsu 2.2x". Always re-measure **both** mutsu and raku in the same
  quiet window.

## 2026-07-12: zef `info` fully works with hyper enabled — the true cause of "%-hash attribute push loss" was shared-array `append` bypassing the atomic store (#4466)

Root-fixed the biggest blocker of PLAN §1 B2, "~80% loss of instance %-hash attribute pushes
on hyper workers". **Both %-hashes and instance attributes were innocent** — the true cause
was plain lexical `@array`s on worker threads:

- `push @a` on a worker has funneled into the `__mutsu_atomic_arr::` store since #4167, and
  subsequent reads prefer the atomic entry. But **only push/unshift** funneled;
  `append`/`prepend`/`pop`/`shift`/`splice` wrote to a stale base/env copy and **silently
  lost everything** (pop/shift "return the value but the removal never lands"). Minimal
  repro: `hyper.map(-> $x { my @a; push @a,"n"; append @a,("p1","p2"); @a.elems })` → 1
  (expected 3).
- In zef, populate's `append @short-names-to-index, $dist.meta<provides>.keys` was wiped
  out, collapsing the keys of `%!short-name-lookup` to 1570, "the number of unique dist
  names" (expected 9256 — the difference being exactly all the provides keys). Pushes into
  the hash itself were fine.
- **fix**: generalized `shared_array_extend` into a generic `shared_array_mutate` (seed the
  atomic entry and apply a closure to the `ArrayData` under the write lock) and extended the
  CallMethodMut shared fast path to all mutators. append/prepend always funnel like push;
  pop/shift/splice are routed "only when an atomic entry shadows the base", preserving the
  legacy slow path's error handling (arity/lazy/immutable) when not shadowed. The splice
  body was extracted from `array_mutate_copy` as the shared `splice_array_data`.
- **Result**: unmodified upstream zef's `zef info Zef` **works end-to-end through the
  Identity/Provides/Depends output with hyper enabled, GC default on, on the real fez index
  (7648 dists)** (stable over 2 release runs). The previously observed "with GC on, the 2nd
  Ecosystems reads an empty `$!name`" also no longer reproduces after the fix (to be
  monitored).
- pin: `t/hyper-array-mutators.t` (12 cases, green under rakudo too).
- **Debugging lessons**: (1) even when it "looks like the hash is corrupted", suspect
  corruption on **the side that generates the keys** (the lexical array) — instrument the
  values at each stage right before they enter the hash (the `idx=[...]` note was the
  clincher). (2) The synthetic repro kept passing because it made the provides key names
  identical to the dist names, rendering the loss invisible — flow **mutually
  distinguishable values** through each data path of a repro. (3) When it looks
  real-data-dependent, trimming the data to 1/150 (jq'ing fez.json 500→50 entries) and
  driving the real module directly turned bisection into minutes.

## 2026-07-15: ANALYSIS.md rev9 — resolved-item archive

ANALYSIS.md was rewritten in English and slimmed down for rev9: the document now keeps
only *open* issues, and everything already resolved was moved here. This section is the
archive of what was removed from the document body.

### Resolved before rev8 (was the doc's own "resolved" preamble)

- **VM was a bytecode shim** → CP-3 collapse merged the bytecode VM into the single
  `Interpreter` struct.
- **Lazy-list collapse / infinite-Range eager-expansion crashes** → unified pull model
  (`materialize_capped`).
- **Concurrent scalar/`state` sharing corruption** → live shared cells (Track C + Track B
  slices 2/3).
- **Regex re-parsed on every match; validator/matcher dual implementation** →
  `REGEX_PARSE_CACHE` + single parser.
- **Roast fudge preprocessing applied to all inputs** → gated behind `MUTSU_FUDGE`.
- **`Value` enum bloat** → boxed to 72→48B (then 8B in layer 3b, below).
- **No panic→`X::` conversion boundary for main/worker threads** → `run` /
  `guard_worker_panic` / #3214; user-sized allocations guarded with `try_reserve`.
- **`locals ↔ env` dual store** → single authoritative store (#3455).
- **Tree-walk execution of user method bodies** → removed in the §B campaign (#3680).
- **No GC (cycle leaks)** → cycle collector on Arc (ADR-0001 layer 3a), default-on
  (ADR-0003, 2026-07-05).
- **`RuntimeError` `result_large_err` at 23 sites** → cold fields boxed
  (`RuntimeErrorCold`), attribute count 23→0.
- **`OpCode` 192B stride + dead opcodes** → `ForLoop(Box<ForLoopSpec>)`, 48B, dead
  variants removed, per-opcode histogram (#4279).

### Resolved between rev8 (2026-07-06) and rev9 (2026-07-15)

- **NaN-boxing (ADR-0001 layer 3b) complete** — `Value` is one NaN-boxed 8-byte word
  (48→8B; `value_size_guard` now pins ≤8B). Step B core #4442, wall-in
  #4445/#4446/#4448/#4450, guards #4467, representation flip #4469. Every release bench
  got 5-9% faster at the flip; GC counters stayed invariant vs main.
- **JIT (ADR-0004 layer 4) J1–J5 complete, `MUTSU_JIT` default ON** (2026-07-13, #4482)
  — Cranelift, Tier A opcode-to-helper translation + Tier B inline Int/Num arithmetic
  (#4471/#4474/#4476/#4478/#4479/#4480), new blocking `jit-stress` CI job. Remaining:
  J4d (Tier B variable-op inlining).
- **Baseline optimizer passes now exist (ADR-0006)** — rev8's "no constant folding/DCE"
  is obsolete: literal constant folding #4485, constant-pool dedup #4486, `constant`
  inlining + constant-condition DCE #4487, declaration-marker fusion #4488,
  `SetSourceLine` opcode replaced by a static ip→line table #4489.
- **The rev8 performance gap table closed entirely** — bench CI (2026-07-14, main
  `827fdb0e`, vs Rakudo v2022.12 on the same runner): fib 0.59x (JIT 0.48x), method-call
  0.89x, bench-class 0.84x, bench-fib 1.15x (JIT 0.92x), bench-tak+jit 1.02x, most other
  benches ≤0.5x. rev8 still listed bench-fib 3.2x / method-call 2.7x / bench-class 2.3x
  as open gaps. Driven by the declaration-path/call-path perf slices
  (#4492–#4495, #4506–#4508) on top of NaN-boxing + JIT.
- **GC auditability sweep (#4414)** — closed rev8 §2.1 concerns ①②④: stale
  "default off" module headers corrected, module-wide `#![allow(dead_code)]` removed
  (plus the genuinely dead surface it hid), `Gc::make_mut`'s all-Relaxed strong-count
  re-link documented sound (uniqueness only acts when this thread holds the sole handle;
  the collector reads counts only under the STW SeqCst handshake) and guarded with debug
  asserts.
- **Two cooperative-STW soundness races fixed (#4410)** — the gc-stress SEGVs were real:
  Dekker-style handshake needed SeqCst (StoreLoad), and worker TLS destructors
  (`PENDING_INSTANCE_DESTROYS` etc.) dropped Gc values after `exit_mutator_worker`;
  `WorkerGuard::drop` now drains them while still registered.
- **Track B tail T4/T5/T6 complete** (#4416/#4417/#4418) — multidim `cas` cells,
  typed-constraint checks on every cas write, escaped-closure element `++`/`--`/`:delete`
  cells. Cut the largest `gc_contents_mut` cluster (`vm_var_assign_index_named.rs`)
  from 18 to 6 call sites. GC candidate buffer went `Weak` (#4420), fixing the
  grammar-parse RSS blow-up (3.6x → +6%).
- **Shadow slots default ON** (2026-07-12) — the §1.4 lexical-scope slot campaign's
  S1–S17 slot bakes landed and a full toggle-ON whitelist survey (1379 files) found zero
  genuine regressions; `MUTSU_NO_SHADOW_SLOTS` is the temporary opt-out
  (`compiler/mod.rs:27`).
- **Module-sub OTF gate relaxation campaign closed** (#4427→#4429→#4431→#4437) —
  CATCH/CONTROL/phasers, nested class/role/grammar declarations, coercion return types,
  and EVAL/EVALFILE (after the CALLER-frame semantics fix #4435) are all OTF-compiled
  now. Residual exclusions are mechanism-level: `state`, sigilless `\x` params,
  `is encoded(...)`, `start`.
- **Method/function resolution caching added** — sound multi-method resolution cache +
  `fast_method_cache` (`vm/vm_call_method_compiled_cache.rs`), invalidated on new
  declarations; parallel function-resolution cache on the call path.
- **Roast whitelist 1364 → 1391 / 1463 (95.1%)**; the frontier moved to `integration/`
  (real programs — see the 2026-07-14 PLAN/BLOCKERS re-audit #4498). Releases v0.3.0 /
  v0.3.1 shipped; bench CI now records history on every main push (#4456).

## References

- The whitelist stands at **1338** as of this writing (2026-07-01, at `4c311c71`).
- For the large volume of items completed during June (the full conquest of
  `S32-exceptions/misc.t`, most of the IO trio, the many DONE items of the first-class
  container campaign, etc.), see [news/2026-06.md](2026-06.md).
