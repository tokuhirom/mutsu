# A module written with the `unit` form of a slang-provided class declarator
# (`unit widget Name;`), the shape Test::Async's `unit test-hub Test::Async::Hub;`
# uses.
use SlangDeclarator;

unit widget SlangUnitWidget;

method widget-name { 'SlangUnitWidget' }
