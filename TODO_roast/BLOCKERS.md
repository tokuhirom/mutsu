# roast blocker index

An index for tracking roast failures by **root cause** rather than per test file.
Used to decide where a fix now would unblock the most tests at once.

**Last updated 2026-07-14** (★re-measured all 79 non-whitelisted files: **the previous
version's remaining-work table listed only 24 S\* files and entirely omitted the
41 `integration/` files, 7 `6.c/` files, and 4 APPENDICES files**. Nearly all of those files
are **raku full-pass = ★achievable**, so the previous version's conclusion that "no ★ items
remain" was wrong. Added the new section "integration — real-program compatibility").

**2026-07-12**: consolidated all sections into a single remaining-work table. Progress notes and
completion reports were moved to `news/`; this file holds only the "currently open remaining items".
The same day, the local raku was updated from **Rakudo v2022.12 → v2026.06** and the raku column
was re-measured for every row.

## How to read this document

- **Items that reach the whitelist are removed from this table**; details move to `news/`.
  For the history of completed campaigns, see `news/2026-06.md` / `news/2026-07.md`.
- Per-file detail logs are in `TODO_roast/S*.md`. The full roast × raku result listing is
  [`raku-baseline.tsv`](raku-baseline.tsv) (generated by `scripts/roast-raku-baseline.sh`,
  explained in [`raku-baseline.md`](raku-baseline.md)).
- **The numbers are a snapshot as of writing.** This file is updated concurrently from multiple
  sessions, so before starting work always re-measure the actual pass/fail with
  `MUTSU_FUDGE=1 prove -e target/debug/mutsu roast/<path>.t`.
- **How to read the raku column**: the local reference implementation is
  **Rakudo v2026.06 (default language 6.d)**. raku is measured by running the **unfudged raw
  files** directly (fudge cannot be applied because it requires rewriting roast/). `SORRY` means
  the local raku cannot compile the file — removed constructs, rakudo-unimplemented syntax,
  fudge-dependent lines, etc. — and does not mean "the test is invalid". Only the cases where
  raku passes fully but mutsu fails are achievable real bugs (★).

## Current assumptions

- The whitelist stands at **1407 / 1463** (2026-07-15, `wc -l roast-whitelist.txt`) = **56** files not whitelisted.
- **The S\* files (per-synopsis feature tests) are exhausted.** All of the former large campaigns
  (true lazy arrays / desugaring of dispatch and operator sugar / S17 concurrency & async /
  first-class-container container identity / cross-thread lexical writeback) are complete, and
  what remains in S\* is only the [per-file table](#s-per-file-remaining-items) below
  (**all of them non-goal / no-oracle / awaiting-infrastructure**).
- **The ★ frontier has moved to `integration/`** (next section). The breakdown of the 79
  non-whitelisted files is not 24 S\* files but mainly **41 `integration/` + 7 `6.c/` +
  4 APPENDICES + 2 of roast's own tooling tests + 1 MISC**, and nearly every `integration/`
  file is a raku full pass.

## integration — real-program compatibility (the bulk of ★achievable; fully measured 2026-07-14)

`integration/` is a group of tests that run **real Raku programs** (Advent Calendar posts,
99 problems), and is the compatibility indicator closest to the project goal of "practical code
just works". In `raku-baseline.tsv`, **nearly every** non-whitelisted `integration/` file has
`raku_status=PASS` (raku full marks) — **by definition, all ★achievable**. It is not one bug per
file: **the root causes collapse into a handful of clusters**.

Measurement: `MUTSU_FUDGE=1 timeout 25 prove -e target/debug/mutsu <file>` (2026-07-14, debug build).

| Root-cause cluster | Files | Affected files (excerpt) | Symptom |
|---|---|---|---|
| **① abort via stack overflow** (★same symptom, but four separate causes — see the subsection below) | **done** | `99problems-41-to-50.t` (#4510), `99problems-51-to-60.t` (#4516), and `man-or-boy.t` (`&`-sigil free-var scan + vouch — see subsection) were infinite recursion, now fixed; `deep-recursion-initing-native-array.t` was a debug-build measurement misdiagnosis (it passes in release from the start) = added to the whitelist | `fatal runtime error: stack overflow, aborting` (process abort). **3 of the 4 were infinite recursion, 1 was a measurement error. Not a single one was "genuinely deep recursion running out of stack"** |
| **② unparseable (`===SORRY!===`)** | **0 (done)** | all 10 resolved (#4511 / #4514 / #4515 / #4517 / #4518 / #4522) | **This cluster is exhausted.** 7 files reached the whitelist; 3 now pass parsing and moved on to feature gaps — see "Breakdown of ②" below |
| **③ hangs / timeouts** | **done** | all 5 reached the whitelist (2026-07-15): `gather-with-loops.t` (lost state writes for iterations ended by `next`/`redo` + state-key collisions between sibling gathers + state not reset in nested loops), `advent2013-day14.t` (unbound placeholder parameters in whenever blocks + in-thread redeclaration clobbering the parent lexical), `APPENDICES/A01-limits/misc.t` (the `x` operator's O(n) push loop replaced with doubling memcpy so a 4GiB string takes seconds + multi-method composition across distinct roles + preserving dynamic-variable side effects in where clauses), `overflow.t` (huge-exponent `**` turned into X::Numeric::Overflow/Underflow Failures + `exit` from a worker thread terminating the process + unhandled-Failure explosion in `say`), `advent2012-day21.t` (pure speed stress — 102s in debug / comfortable in release. Set a per-file timeout of 120s) | — |
| **④ error-message quality** | 2 | `error-reporting.t` (**26/33**, originally 4/33), `weird-errors.t` (**30/36**, originally 26/36) | Tests that inspect **exception wording, line numbers, and backtraces**, e.g. "Parse error contains line number". #4539 implemented backtraces on all runtime errors, is_run = stderr identical to the CLI, Backtrace.new/full, etc. error-reporting remaining 7 = compile-time detection of undeclared routines (tests 5/23 — the biggest target), backtrace frames for module files (15), Failure dual backtrace (20), return inside map (21), `%::{''}` (25), type-capture inheritance (30). weird-errors remaining = `say(;:[])` (11), `::a`→X::NoSuchSymbol needs `::` info on BareWord (20), our method invocant type (29), `new Foo:` (32), `hash a=>1` (33) |
| **⑤ individual feature gaps** | 2 | `advent2011-day07.t` (`Metamodel::GrammarHOW` inheritance — `Advent::GrammarProfiler` requires `class ProfiledGrammarHOW is Metamodel::GrammarHOW` plus the equivalent of `^set_how` = **major metamodel work**), `precompiled.t` (precomp infrastructure) | **8 files reached the whitelist (2026-07-15, #4533 / #4535 / #4536 / #4538 / #4541 / #4542)** — day22/day20/day18 (introspection), day14 (`Say`-family opcodes missing from `has_calls`), 2013-day10 (operator adverbs), day04 (wrap recursion re-entry), 2011-day10 (`--doc`/`pod2text`), day24 (merging inherited proto candidates in derived grammars). Details in [news/2026-07.md](../news/2026-07.md) |

**Shortcut (files one subtest away)**: `6.c/S04-declarations/my-6c.t` is at **111/112** (the only
failure = test 57, the `OUTER::<$x>` pseudo-package).

### Breakdown of ① (all 4 root-caused on 2026-07-14)

The initial diagnosis "deep recursion overflows the stack" was **wrong**. The aborts look
identical but the causes are distinct:

| File | Actual cause | Status |
|---|---|---|
| `99problems-41-to-50.t` | **Infinite recursion.** A closure's frame env was a scoped child of the *caller's* env, and the captured env was merged with "don't insert if already present", so a caller lexical of the same name shadowed the callee's own capture (i.e. lexical scoping degenerated into dynamic scoping). The P46 actions build closures that each hold their own `my @args` per clause, so an inner clause read the outer `@args` (which contained itself) and self-recursed | **Fixed in #4510.** P41 and P46 pass. Next blocker is P47's parameterized `multi rule expr($p)` (moves to ⑤) |
| `99problems-51-to-60.t` | **Infinite recursion** (P57). `CallMethodMut` unconditionally executed `locals[slot] = env[name]` on every method call on a named receiver, but the callee's env is "flattened caller + its own writes" and **parameters exist only in slots**. So when the receiver had not been rebound, the caller's same-named variable overwrote the callee's parameter, and in the self-recursive `add-to-tree($tree[1], …)` the `$tree` rolled back to the caller's node and never reached a leaf | **Fixed in #4516** (write back only when actually rebound). All 37 tests run to completion. Remaining fails = test 8 (balanced-binary-tree branch dropping) and test 21 |
| `man-or-boy.t` | **Infinite recursion.** The remainder of the same bug as #4510: "a same-named lexical in the calling frame shadows the closure's own capture". This time the shadowed variables are **`&x1..&x5` (A's `&`-sigil parameters)**, which are not in `authoritative_free_vars` and therefore never overwrite-installed. Details below | **Fixed and whitelisted** (10/10). (a) Added `GetCodeVar`/`CallOnCodeVar` to the free-var scan (the constants are sigil-less `"x1"` while the lexical is `&x1`, so record them with the `&` prefixed), (b) exempted `&` names from the rw-arg sink exclusion (`own_call_arg_sources`) (raku rejects `is rw` on `&` parameters, so no rebind path exists) |
| `deep-recursion-initing-native-array.t` | **Not a bug.** It really does need ~20,000 levels of nesting, but **it passes in release from the start** (7 seconds, 3/3 stable). It only looked like an overflow because this table's initial measurement used a **debug build** (debug uses several times the native stack per frame; release's limit is 20k–40k levels, and the required 20k just barely fits) | **Added to the whitelist.** Neither stack growth nor a depth guard was needed |

Lesson: **the shape of the abort (`stack overflow`) says nothing about the cause.** Count the
depth and compare against raku first, to separate infinite recursion from finite deep recursion
(you can tell instantly when `raku` finishes in 8 calls while mutsu emits a 1900-line trace).
3 of the 4 were infinite recursion, and the remaining one was a **measurement error**.

A second lesson: **the ① measurements were taken with a debug build.** Debug consumes several
times the native stack per Raku frame compared with release, and
`deep-recursion-initing-native-array.t` (~20,000 levels) overflowed only in debug. CI uses
release, so **any failure where stack depth is suspect must be confirmed with a release build**
(the same point as the "Delegate the full roast run to CI" section of CLAUDE.md).

The decisive probe when suspecting a dual-store (locals ↔ env) bug: **read the same variable
twice within a single expression**.

```raku
sub f($tree, $d) {
    say $tree.WHAT.^name ~ ' | ' ~ $tree.WHAT.^name;
    return if $d >= 1;
    f($tree[1], $d + 1);
}
f([3, Any, Any], 0);
# raku:  Array | Array  /  Any | Any
# mutsu before the fix:    Any | Array   ← first read correct; second rolls back to the caller's value
```

### man-or-boy diagnosis (2026-07-14, measured after #4516)

Tracking the identity of each `&x` with `.WHICH` (`Sub|ID`) pinpoints the divergence point.
**The probe must not *call* `&x`** (`x4('TAG')` would fire the B closure and change the execution
itself). Even if you give it a name like `sub bee`, mutsu returns an empty `&x.name` (a separate
small gap), so use `.WHICH` for identification.

```
A#1 k=4 d=0 B=Sub|33  x=[27 28 29 30 31]    <- the original 5 literals
A#2 k=3 d=1 B=Sub|41  x=[33 27 28 29 30]    <- A#1's B fired from inside A#1 = correct
...
A#5 k=0 d=4 B=Sub|65  x=[57 49 41 33 27]    <- k<=0, so it calls x4 (=Sub|33 = A#1's B)
A#6 k=2 d=1 B=Sub|73  x=[33 57 49 41 33]    <- expected [33 27 28 29 30]
```

A#6 is "A#1's B fired **from inside A#5**". B's body is
`A(--$k, $B, &x1, &x2, &x3, &x4, ...)`, so `&x1..&x4` must be read from **A#1's captured env**
(= 27,28,29,30), but mutsu reads `&x1..&x4` from **the calling frame A#5** (= 57,49,41,33).
Invocations fired from inside A#1 (A#2) happen to have a coinciding chain and thus look correct;
**it breaks the moment B fires from a different frame**.

This is the same bug as the shadowing fixed in #4510. #4510 overwrite-installs only "plain
lexicals the creator does not modify after capture" (`CompiledCode::authoritative_free_vars`);
`&x1..&x5` are not in that set, fall into the don't-overwrite path, and lose to the caller's
same-named parameters.

**Resolution (2026-07-15)**: the cause was two layers deep, and both had to be fixed (which is
why experiments fixing only one of them "did not work"):

1. **Free-var scan omission**: reads of `&x1` go through `GetCodeVar`/`CallOnCodeVar`, whose
   constants are the sigil-less `"x1"`. They are not included in `op_name_const_idx` (the
   GetGlobal family), so they never entered `free_var_syms` at all, and the capture filter
   dropped them (`&x1` is a plain user lexical, so the system-name rules would not keep it
   either) — **they were not even captured** = resolved purely via dynamic scope.
   → The scan now records them under the `&`-prefixed key `&x1`.
2. **rw-arg sink exclusion**: A forwards `&x1..&x5` as arguments in `x4($k, &x1, …)`, so
   `own_call_arg_sources` rejected the vouch. But **raku rejects `is rw` on `&`-sigil
   parameters** ("Can only use 'is rw' on a scalar ('$' sigil) parameter"), so per spec there
   is no path for a `&` argument to be rebound by the callee → exempt `&` names.
   (Rebinding via `is raw` theoretically remains, accepted as a known gap. A direct `&f = …`
   rebind is a name-write and is captured as before.)

**Remaining gaps also resolved (follow-up commits in the same PR)**:

- **Bare calls `x()`**: the `CallFunc`/`ExecCall` family record the read only as a callee name,
  so it was outside the free-var scan. Registering every callee name would bloat
  `free_var_syms` with `&say` etc. (paying a `free_at_entry` lookup per closure call), so only
  callee names that match **the set of enclosing `&`-sigil lexicals visible at the definition
  point** (`CompiledCode::outer_code_var_names`, propagated transitively from the parent's
  `local_map` by `compile_closure_body`/`compile_sub_body`) are recorded as `&name` free vars.
  Zero bloat, lexically precise.
- **Authority loss across multiple nesting levels** (a pre-existing bug that hit scalars too):
  the vouch was burned only onto "direct children of the creator", so the inner closure in
  `sub mk($x) { my $mid = sub { sub { $x } }; $mid.() }` could not be vouched by the
  intermediate frame (`$x` is not the intermediate's local) and was shadowed again by the
  caller's same-named lexical. Since the vouch already guarantees "the entire subtree performs
  no writes" (nested free-var writes fold up to the creator), this was resolved by
  **propagating authority down to descendants as long as no intermediate closure redeclares the
  same name** (`propagate_authoritative_down`).

### Breakdown of ② (cluster complete — #4511 / #4514 / #4515 / #4517 / #4518 / #4522)

**Reached the whitelist (7 files)**: `advent2010-day11.t`, `advent2013-day04.t`,
`advent2014-day16.t` (#4511: `qto`/`qqto` fused adverbs, whitespace/newlines between the quote
word and the delimiter, the starting line of the heredoc body, selective adverbs such as
`q:to:c`, multi-argument `for` over a lazy gather Seq) / `advent2009-day16.t`,
`advent2009-day23.t` (#4514: `when` as a statement modifier, smartmatch with a Match on the RHS,
`(with …)`/`(given …)` as expressions, `my@i` declarations without whitespace) /
`advent2012-day04.t` (#4515's feed continuation lines and lazy `for` over an infinite closure
sequence, #4517's looser-than-comma operator precedence, #4518's destructuring signature + grep
binder) / `advent2012-day19.t` (#4522) / `6.c/APPENDICES/A04-experimental/01-misc.t` (#4523:
made `^find_method`/`^lookup` resolvable on concrete values, coercion types' implicit source now
`Any`, `Date`/`DateTime` `.IO` throws on type objects).

The last one, `advent2012-day19.t`, passed all 10 subtests in #4522. It implemented: the problem
that **user-defined operators with precedence traits** (`sub postfix:<k> is tighter(&infix:<*>)`)
were never applied to terms (the postfix loop skipped them all as "picked up at the prefix
layer", but that pick-up existed only inside the user-defined-prefix branch); chains of
alphabetic postfixes (`4kVW`); the bug where `is looser(&postfix:<k>)` used `PREC_PREFIX` as the
baseline instead of k's registered level; `Mu.ACCEPTS`; and `Range.new`.

**Now parse but still have failing subtests (2 files)** — moved out of ② into feature gaps:

| File | Remaining | Blocker |
|---|---|---|
| `integration/advent2012-day15.t` | 2/11 | statement-form phasers create their own scope (`$best` in `NEXT (state $best) max= $_;` is not visible from `LAST`) / `INIT` runs after the mainline |
| `6.c/APPENDICES/A04-experimental/01-misc.t` | 3/19 | `:D`/`:U` DefiniteHow coercion (`Target:D(Source:U)`). #4514: 0/19 → 16/19 |

**The single file that remained unparseable in ②**: `integration/advent2012-day19.t`.
**User-defined postfixes with precedence traits**, e.g. `sub postfix:<k> ($a) is
tighter(&infix:<*>)`, are never applied to terms (`4.7k` is a SORRY). The postfix loop in
`postfix/loop_.rs` skips postfixes whose registered precedence is below `PREC_PREFIX` as "picked
up at the prefix layer", but that pick-up loop exists **only inside the branch for user-defined
prefix operators**, so on a bare term like `4.7` nothing consumes them. Furthermore this file
requires `4kΩ` (a chain of postfixes at different levels) and `4.7kΩ ± 5%` (an infix looser than
both), so it needs **precedence climbing that applies user-defined operators according to the
precedence table** — not a one-shot fix. In addition, the bug in `registry.rs` where
`resolve_op_precedence` resolves `is looser(&postfix:<k>)` as "`PREC_PREFIX` - 5" instead of
"registered level - 5" also needs to be fixed.

## S* per-file remaining items

Definitions of the classifications:

- **★achievable** — the local raku passes fully but mutsu fails = a real bug. The whitelist is
  reachable. However, every remaining item needs multiple features rather than a one-shot fix
  (the cheap single-file wins are exhausted).
- **Awaiting infrastructure** — requires implementing a large underlying subsystem such as
  RakuAST or 6.e generics. Low priority, shelved.
- **No oracle** — the local raku SORRYs or aborts, so reference verification is impossible
  (6.e-only syntax, 6.d restrictions). Implementation work remains on the mutsu side, but the
  priority is low because the correct answer cannot be confirmed locally.
- **Non-goal** — rakudo itself also fails / a roast-side problem / a removed spec. Advancing
  these as a side effect of general mutsu improvements is fine, but **do not target
  whitelisting that file**.
- **Unpassable** — the test itself contains deliberate flunks etc.; confirmed impossible to pass.

| Classification | File | mutsu | raku (v2026.06/6.d) | Blocker (one line) |
|---|---|---|---|---|
| Awaiting infrastructure | `S32-str/format.t` | aborts at 26/49 | **49/49 full pass** (SORRY on v2022.12) | Requires `Formatter::Syntax.parse`→Match and `Formatter.AST`→`RakuAST::Node` = **no RakuAST subsystem**. The raku update provided an oracle, but stubbing is forbidden, so it stays shelved |
| Awaiting infrastructure | `S02-types/generics.t` | 0/1 | **1/1 full pass** (SORRY on v2022.12) | Needs 6.e coercion type terms + `Array[T]` subclassing. The raku update made reference verification possible, but the size of the required infrastructure is unchanged |
| No oracle | `S02-names/pseudo-6d.t` | aborts at 116/159 | SORRY (`::=` NYI) | `::("CALLER")::<$*bar>` CALLER pseudo-package deref unsupported. Even on v2026.06 rakudo SORRYs because `::=` binding is unimplemented |
| No oracle | `S02-names/pseudo-6e.t` | aborts at 79/202 | SORRY (`$?` constant twigil NYI) | Same as above (the 6.e version). Still SORRY on v2026.06 |
| No oracle | `S02-names-vars/names.t` | 144/156, notok 3 | SORRY (unfudged line) | test 142 "Null PMC access when printing a var typed as ::foo" edge. raku SORRYs on a fudge-dependent line (a bare `$`) |
| No oracle | `S02-types/array-shapes.t` | aborts at 35/43 | 38/43 (raku also aborts; same on v2026.06) | shaped array `.pairs`' `.value` does not return a writable container |
| No oracle | `S05-metasyntax/longest-alternative.t` | 57/62, notok 5 | SORRY (`::` LTM stopper NYI) | LTM (longest-token-match) tie-break edge. Still SORRY on v2026.06 because rakudo has not implemented `::` |
| No oracle | `S10-packages/basic.t` | 59/83, notok 9 | 6/83 (mutsu ahead; same on v2026.06) | Error-detection edges for the semicolon form of package declarations |
| No oracle | `S12-attributes/trusts.t` | 9/15, notok 6 | SORRY (forward reference `trusts B`) | Cross-class private access via `trusts` unsupported. Still SORRY on v2026.06 |
| No oracle | `S19-command-line-options/01-dash-uppercase-i.t` | 0/8 | 0/8 (`$*OS` unsupported; same on v2026.06) | `-I` + `@*INC` + `$*OS` introspection (is_run subprocess) |
| No oracle | `S32-basics/xxPOS.t` | aborts at 11/64 | 53/64 (raku also aborts; same on v2026.06) | Aborts from test 12 onward due to deep features |
| Non-goal | `S05-capture/hash.t` | rejected (matches raku) | SORRY (removed) | Hash captures in regexes `%<name>=(...)` are removed/reserved in the current spec. mutsu also gives a compile error, matching raku = **no implementation needed** |
| Non-goal | `S05-mass/rx.t` | aborts at test 20 | SORRY (`::` NYI) | rakudo itself cannot compile it: `::` backtracking control NYI. mutsu remaining: `<commit>`, `::`/`:::` cuts, the family of compile-time exceptions for removed metacharacters |
| Non-goal | `S05-metasyntax/angle-brackets.t` | aborts at 51/95 | SORRY (v2026.06 rejects `<*xyz>` at compile time) | All 51 reachable subtests pass. test 52's code-string subrule (turning a string containing `{...}` into a regex) also kills old raku = requires the equivalent of MONKEY-SEE-NO-EVAL. New raku does not even run due to the unfudged `<*xyz>` at line 267 |
| Non-goal | `S05-nonstrings/basic.t` | 0/5 (ran 0) | SORRY (removed) | Regex matching against non-strings (streams/arrays) = early Perl 6 spec. Removed from the language while Rakudo never implemented it |
| Non-goal | `S06-advanced/caller.t` | 19 ran / plan 22 | SORRY | Stale test, over-stated plan (22 planned vs effectively 19 assertions). Confirmed unpassable (#3975) |
| Non-goal | `S06-advanced/return_function.t` | aborts at 1/4 | SORRY | Expects the old Pair-flattening behavior. rakudo also rejects it at compile time. Outside spectest.data |
| Non-goal | `S10-packages/require-and-use--dead-file.t` | 4/18 | raku also FAILs | Return values of `require`/`use`/`no` and `%*INC` updates unimplemented. But raku itself also fails this file |
| Non-goal | `S12-class/open_closed.t` | 8/9, notok 1 | SORRY (no `oo` module) | test 7 is a roast-side typo (the body of `method h` says `'called Qux.i'`). Awaiting an upstream fix |
| Non-goal | `S12-meta/exporthow.t` | aborts at 1/12 | ABORT at 2/12 | rakudo itself dies at line 22 (`ClassHOW` has no `tryit`) |
| Non-goal | `S12-traits/basic.t` | 0 at parse | SORRY (removed) | Removed `trait_auxiliary` syntax. raku also rejects it |
| Non-goal | `S12-traits/parameterized.t` | aborts at 6/8 | SORRY (removed) | Same as above (the `trait_auxiliary:<is>` category has been removed from the language) |
| Non-goal | `S32-str/sprintf.t` | 170/174 (fails 101-104) | SORRY (`zprintf` undefined; same on v2026.06) | The remaining 101-104 are a raku quirk of zero-padded `%g` sci notation (`34.1e+30`). The reference raku lacks `zprintf`, so the correct algorithm cannot be verified |
| Unpassable | `S32-temporal/time.t` | 8/10, notok 2 | SORRY | The test contains 2 deliberate `flunk("FIXME ...")` failures, plus raku also has `gmtime`/`localtime`/`times` undefined |

## Recommended order of attack right now

1. ~~**The last remaining ① file = `man-or-boy.t`**~~ **Done** (2026-07-15, added to the
   whitelist. See the resolution record in the "man-or-boy diagnosis" section). Cluster ① is
   fully cleared. The ② unparseable cluster was also fully cleared by #4522 (see the table above).
2. ~~**③ hangs / timeouts (5 files)**~~ **Done** (2026-07-15, all 5 added to the whitelist —
   see the ③ row of the table).
3. **Shortcuts**: `6.c/S04-declarations/my-6c.t` needs only the single `OUTER::<$x>` subtest
   (111/112). `advent2011-day04.t` is at 1/2.
4. **④ error-message quality** (`error-reporting.t` 4/33) is the same target as the identically
   named task in PLAN §6, so it can be driven by roast pass/fail while working on that.
- For the S\* table (non-goal / no-oracle / awaiting-infrastructure), advancing entries as a side
  effect of general mutsu improvements is fine, but do not make whitelisting those individual
  files the goal. `S32-str/format.t` and `S02-types/generics.t` gained an oracle from the raku
  update, but the size of the required infrastructure (RakuAST / 6.e generics) is unchanged.
