# S11 - compilation-units, modules

- [ ] roast/S11-compunit/compunit-dependencyspecification.t
  - 2/6 pass (1-2). Tests 3-6 regressed: CompUnit::DependencySpecification.new(:short-name<Foo>) now fails (returns Nil instead of instance). Removed from whitelist.
- [ ] roast/S11-compunit/compunit-repository.t
- [ ] roast/S11-compunit/rt126904.t
- [ ] roast/S11-modules/export.t
  - 38/59 pass (1-38). Remaining failures: RT83354 needs cross-module operator dispatch for `has $.b` accessor; RT84280/RT125715/RT129215 need complex cross-module features; GH#2955 needs parametric role export/import.
- [x] roast/S11-modules/gh2979.t
- [x] roast/S11-modules/importing.t
- [x] roast/S11-modules/import-multi.t
  - 17/17 pass. Fixed: exported `proto sub` no longer redeclares an existing
    same-named mainline proto (merged at import); custom `is` traits that mix a
    role into an exported routine now survive export/import (exported_sub_values).
- [x] roast/S11-modules/import.t
- [x] roast/S11-modules/import-tag.t
- [x] roast/S11-modules/lexical.t
- [x] roast/S11-modules/module-file.t
- [x] roast/S11-modules/module.t
- [x] roast/S11-modules/need.t
- [x] roast/S11-modules/nested.t
- [x] roast/S11-modules/rakulib.t
  - 1/1 pass.
- [x] roast/S11-modules/re-export.t
- [x] roast/S11-modules/require.t
- [x] roast/S11-modules/runtime.t
  - 1/1 pass.
- [x] roast/S11-modules/versioning.t
  - 9/9 pass. `CORE-SETTING-REV` is folded to the compunit's language-revision
    letter at parse time; `BEGIN $*RAKU.version`/`$*PERL.version` fold to the
    compunit's Version. EVAL now sets parser lib paths so `use Foo; bar`
    resolves Foo's parenless exported subs.
- [x] roast/S11-repository/cur-candidates.t
- [x] roast/S11-repository/cur-current-distribution.t
- [x] roast/S11-repository/curli-install.t
  - 19/19 pass. `use lib "inst#PATH"` now installs a
    `CompUnit::Repository::Installation` as `$*REPO` (chained in front of the
    previous repo). The instance does `CompUnit::Repository::Installable`/`::Locally`
    (via its MRO), exposes `.id`/`.short-id`/`.loaded`, guards against
    reinstalling an identical distribution (`already installed`), and `.need`
    returns a real `CompUnit` (`.short-name`/`.version`/`.handle.globalish-package`).
    Symbols loaded by `.need` stay hidden from `::('Foo')` until
    `GLOBALish.WHO.merge-symbols(...)` publishes them into GLOBAL. Also fixed a
    general for-loop bug where a single named loop param (`for ... -> $x`) leaked
    across function calls that reused the same variable name.
