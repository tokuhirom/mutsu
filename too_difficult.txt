roast/S02-literals/allomorphic.t (multiple unrelated blockers: (1) `my class` lexical scoping bug — `my class IntFoo {...}` registered globally by name, so two `my class IntFoo` in disjoint blocks share the same class table entry and instances created from the first end up dispatching methods defined by the second, breaking the .ACCEPTS subtest where @true and @false gather blocks each declare their own IntFoo/RatFoo/NumFoo/ComFoo classes; fixing this requires rearchitecting class registration to be lexically scoped (per-scope class table or unique mangled names) which is a cross-cutting change touching register_class_decl, method dispatch, and instance type identity; (2) `val(<1 2 3>.Slip)` must preserve Slip-ness — val candidate currently returns List unconditionally; (3) `warns-like` plus `:U.Numeric` / `:U.Real` must emit "uninitialized numeric" warnings on type objects of Int/Num/Rat/FatRat/Complex/IntStr/NumStr/RatStr/ComplexStr/CustomNumeric/CustomReal — mutsu does not emit CONTROL warnings for type-object numeric coercion and the warns-like Test::Util infra to capture them is not wired up either. Each blocker is independent and substantial; total scope exceeds a focused fix.)
roast/S02-magicals/dollar-underscore.t (17/23 pass; needs container-based `$_` semantics — `:=` vs `=` aliasing, fresh `$_` per sub call, empty pointy signature arity check, BlockScope must propagate plain assignment to `$_` while keeping `:=` block-local)
roast/S02-magicals/sub.t
roast/S02-types/generics.t
roast/S02-types/multi_dimensional_array.t
roast/S03-binding/attributes.t
roast/S03-operators/context.t (17 failures across many unrelated features: @(multi,arg)/$(multi,arg)/item(multi,arg) multi-arg context coercions, %(...) hash builder with odd-item X::Hash::Store::OddNumber detection, parse-time X::Obsolete detection for P5 ${..}/@{..}/%{..}/"${..}"/"@{..}" deref forms, anonymous @/% variables typed as Array/Hash instead of Any, %$var coercion from pair-list, item @a/%b non-flattening)
roast/S03-sequence/misc.t
roast/S04-exceptions/exceptions-alternatives.t
roast/S04-phasers/in-eval.t (test 13 "INIT did not run at compile time" is marked `#?rakudo todo` in the source and fails on raku itself when run directly via prove without roast fudging — cannot pass through our test infrastructure regardless of mutsu changes)
roast/S04-statements/for.t (raku itself fails subtest 65 "for loop iteration with label-less 'last' gives ()" — the test cannot pass completely on any implementation in its current form, so per workflow we cannot whitelist it; additionally mutsu fails ~20 other subtests including lazy for loop semantics, for loops not decontainerizing, hole mutability for @a[*], and sink-context method dispatch in final statement)
roast/S04-statements/return.t
roast/S05-capture/alias.t
roast/S05-capture/array-alias.t
roast/S05-capture/caps.t
roast/S05-mass/recursive.t
roast/S06-advanced/lexical-subs.t
roast/S06-advanced/return-prioritization.t
roast/S06-advanced/wrap.t (raku itself fails 3 of 90 subtests — tests 56, 57, 58 about `&foo.wrap` restoring the original sub when the wrap handle goes out of scope; this is a known Rakudo failure, so the test is unachievable for any conforming implementation without also reproducing the Rakudo bug)
roast/S11-modules/versioning.t
roast/S11-repository/cur-current-distribution.t (requires entire CompUnit::Repository::FileSystem and ::Installation subsystems, Distribution::Path, $?DISTRIBUTION variable, -M command-line module loading honoring META6.json provides, and make-temp-dir; spans modules/distribution/repository plumbing far beyond a single PR)
roast/S11-repository/curli-install.t
roast/S12-introspection/walk.t (requires a substantial rewrite of WALK introspection: (1) WALK must return a real WalkList object (not a precomputed Sub) that is itself iterable as a sequence of per-method callables AND directly callable via postfix `()`, with `.invoke(...)`, `.reverse`, `.quiet` methods; (2) eight distinct traversal orders — `:canonical` (default, MRO), `:super` (excluding receiver class), `:breadth` (BFS), `:descendant` (reverse MRO), `:ascendant`/`:preorder` (depth-first preorder), each yielding distinct candidate sequences (E with C is A is B / C is A is B / D is A produces ECDAB/CD/ECDAB/ABCDE/ECABD); (3) `:omit` and `:include` filter callbacks taking a candidate object that supports `.gist` and `.^can`; (4) lazy iteration so methods are invoked only when consumed in the for loop (Lazyness subtest); (5) `quiet` mode wrapping thrown exceptions in Failure (with `.exception`) instead of rethrowing, plus default-mode rethrowing X::TypeCheck::Binding for failed signature binding; (6) Slip return values preserved as Slip in the result list (not flattened); (7) `:roles` traversal interleaving role submethods with class methods in the right order while excluding role methods (only submethods); (8) candidate objects must be individually invocable with `($instance)` and dispatch into the correct class/role's own method body. The current methods_walk.rs precomputes results and returns them as a single Sub — every aspect above is missing. Implementing this requires a new WalkList runtime type, candidate-callable values that capture (owner-class, method-def, invocant), several new MRO traversal helpers, and integration with the existing exception/Failure infrastructure. Too broad to bundle as a focused fix.)
roast/S12-meta/exporthow.t
roast/S12-methods/accessors.t
roast/S14-roles/parameterized-mixin.t
roast/S14-traits/attributes.t
roast/S17-lowlevel/semaphore.t
roast/S17-procasync/encoding.t
roast/S17-supply/categorize.t
roast/S17-supply/migrate.t
roast/S26-documentation/07b-tables.t (requires parse-time validation of empty =begin table/=end table; that validation regresses 12-non-breaking-space.t which depends on mutsu's broken BEGIN-at-end-of-file phaser ordering producing an empty runtime EVAL'd table)
roast/S26-documentation/08-formattingcodes.t (requires full Pod formatting-code parsing -- B<>, C<>, R<>, L<>|meta, V<>, E<...> with decimal/hex/unicode-name/HTML5-entity escapes, nested <<>> delimiters, and construction of Pod::FormattingCode objects; mutsu currently treats Pod paragraph text as a plain Str with no FormattingCode structure. Additionally tests 41-42 rely on the `fudge` preprocessor's `#?rakudo todo` directive which mutsu's test runner does not apply -- even rakudo itself fails those two tests when run directly without fudging.)
roast/S32-array/perl.t
roast/S32-hash/perl.t
roast/S32-io/child-secure.t
roast/S32-io/io-path-cygwin.t
roast/S32-list/categorize.t
roast/S32-io/socket-recv-vs-read.t
roast/S32-temporal/local.t
roast/S32-basics/xxPOS.t (raku itself fails at test 53: BIND-POS on Any:U)
roast/S09-multidim/XX-POS-on-dimensioned.t (requires extensive shaped-array infrastructure: Array.new(:shape(2;2)) with multi-dim AT-POS/ASSIGN-POS/BIND-POS/DELETE-POS/EXISTS-POS, X::NotEnoughDimensions exception class with operation/got-dimensions/needed-dimensions fields, native array[int]/array[num] parameterized with shapes including bounds checking, and .shape returning a semicolon-list matchable against (2;2). Spans parser, compiler, VM, and runtime — not achievable as a single focused fix.)
roast/S17-channel/basic.t (last subtest requires Supply.throttle, Channel.Supply.tap routing sent values through the Supply, and `await Supply` completion semantics — a significant async/concurrency rework. 28 of 29 subtests pass after fixing the qualified-class-name shadowing bug that made `my class X::Roast::Channel` shadow built-in `Channel`.)
roast/S11-modules/import-multi.t (raku itself fails to compile the test file: "A symbol '&Gfoo' has already been exported" at line 96, even though the G1/G2 block is marked `#?rakudo skip`. The skip directive only suppresses runtime, not compile-time export collision detection. Test cannot be made to pass without `fudge` preprocessing, which mutsu's test runner does not apply.)
roast/S14-traits/routines.t (requires multiple deep features: (1) multi trait_mod:<is>(Routine $c, SomeRole) dispatch where a user-defined role is matched as a positional type parameter — mutsu currently throws "No matching candidates for proto sub: trait_mod:<is>" as soon as the first `sub answer() is description(...)` is parsed, because trait_mod dispatch does not recognize user roles as type matchers; (2) closure-scalar write-back from wrapped sub dispatch: `$c.wrap: sub { $recorder ~= 'wrap' }` in a trait body mutates an outer-scope scalar, but mutsu's wrap dispatch calls `call_sub_value(..., merge_all=false)` and the call-frame merge only propagates Array/dynamic vars, so scalar mutations are dropped (test 10 "and the wrapper has been called once"). Fixing the latter is a one-line change (merge_all=true in resolution.rs wrap path) but is blocked on (1), which is a substantial trait-dispatch overhaul across runtime/methods.rs, runtime/dispatch.rs, and the multi-dispatch matcher.)
roast/S05-metasyntax/repeat.t (23 failing subtests spanning many distinct regex features: closure-literal quantifiers `**{N}`/`**{N..M}`/`**{N..*}`, exclusive range quantifiers `**2..^5`/`**1^..4`/`**1^..^5`/`**^5`/`**1^..*`, `%%` trailing-separator operator, named-capture with closure quantifier `$<letter>=(A)**{3}`, parse-time detection of P5-style `{1,3}`/`{1,}` throwing `X::Obsolete`, solitary-quantifier detection with `X::Syntax::Regex::SolitaryQuantifier`, solitary backtrack-control `:` with `X::Syntax::Regex::SolitaryBacktrackControl`, non-quantifiable construct detection (`{}*`, `<?{1}>?`) with `X::Syntax::Regex::NonQuantifiable`, and `% abc` parsing `abc` as separator-atom `a` only. Requires adding several regex-specific exception types plus substantial regex parser/engine work across regex_parse.rs and regex/ modules — too broad to bundle in a single PR.)
roast/S04-declarations/will.t
roast/S32-io/pipe.t (requires major Proc/IO::Pipe infrastructure: Proc identity via `===`, IO::Pipe.close returning Proc::Status that smartmatches Proc, .out.slurp(:close) and (:close, :bin), bidirectional `:in`/`:out` subprocess pipes with chained stdin (`:in($sh1.out)`), `:bin` binary mode encode/decode round-trips, async `start { $proc.in.write }` concurrent with parent reading from $proc.out, `:merge` to a user-supplied :out(Handle:D), and Proc::Status.exitcode propagation — spans runtime process management, IO::Pipe type, async/await with subprocesses, and Proc smartmatch semantics. Not achievable as a single focused fix.)
roast/S32-str/format.t (raku itself fails to compile this test: "Undeclared names: Format, Formatter" — the test targets v6.e.PREVIEW experimental Format/Formatter/Formatter::Syntax/RakuAST::Node classes that do not exist in current Rakudo. Cannot pass on mutsu without implementing an entire experimental Format DSL plus RakuAST introspection; cannot pass on raku either.)
roast/S12-subset/subtypes.t (raku itself fails 2 of 92 subtests: test 74 "Array of subset type as parameter to function" with "Constraint type check failed in binding to parameter '%f'; expected Foo but got Hash[Foo:D]" and test 92 "custom type checking ok". Cannot pass fully on raku, therefore cannot pass on mutsu regardless of interpreter changes.)
roast/S12-methods/qualified.t (2 of 7 subtests pass; remaining 5 require multiple deep MOP features: (1) `callsame` inside a method overriding a punned-role method must dispatch to the role's method via the MRO — mutsu's callsame does not know about role-punning in the dispatch chain; (2) runtime `self does Role` mixin must modify the instance's type so subsequent qualified `self.Role::meth` calls see newly-added roles — requires mutable per-instance role composition and re-dispatch; (3) `$?ROLE` and `$?CLASS` compile-time pseudo-constants bound to the enclosing role/class declaration, including inside parameterized roles where `$?ROLE` must be the parameterized variant (e.g. R1[Int]); (4) parameterized-role qualified method calls `self.Bar::R::of-type` resolved by the role's base name across the MRO with the correct T binding; (5) "relaxed" qualified dispatch ambiguity detection throwing `X::AdHoc` with "Ambiguous concretization lookup for R1" when the type composes multiple parameterizations of the same role. Each is independently substantial and together span role composition, MOP, dispatch, and parameterized-role plumbing.)
roast/S04-statements/gather.t (raku itself fails subtests 29 and 30 "can access bound gather result while gathering" / "bound gather result has up-to-date value while gathering" with "This continuation has already been invoked" — the test relies on being able to re-enter a gather continuation to observe partial results from inside the gather body via a bound variable, a semantics Rakudo no longer supports. Cannot pass on raku, therefore cannot pass on mutsu. Additionally mutsu has independent gaps: laziness of gather (subtest 12), nested-identical-gather take routing (subtest 17), and INIT-phaser-inside-gather throwing X::ControlFlow (subtest 33) — all require continuation-like gather semantics and phaser control-flow machinery not currently present.)
roast/S02-names/is_default.t
roast/S02-literals/string-interpolation.t (21/42 pass; remaining 18 tests all use `throws-like 'code', X::Undeclared` to verify that parsing/EVAL of strings containing interpolation of undeclared variables like `"@$whoopsies[]"`, `"@@whoopsies[]"`, `"$$whoopsies[]"`, etc. dies at compile time with X::Undeclared / X::Undeclared::Symbols. mutsu does not perform compile-time undeclared-variable checking at all — `say $whoopsies` silently returns Nil instead of erroring — so implementing these test cases requires adding a full compile-time symbol-table / strict-mode pass in the parser or compiler that rejects references to undeclared lexicals and throws typed X::Undeclared / X::Undeclared::Symbols exceptions, which is a cross-cutting architectural change touching parser, compiler scope tracking, exception types, and potentially a large amount of existing mutsu code that relies on the current permissive behavior. Also requires parser support for sigil-swap interpolations `@$var[]` / `%$var{}` / `&$var()` / `$@var[]` / `$%var{}` / `$&var()` — mutsu currently parses `@$whoopsies[]` as literal `@` + `$whoopsies[""]` rather than as array-deref of $whoopsies. Out of scope for a focused fix.)
roast/S03-operators/identity.t (raku itself fails 8 of 46 subtests: "binding of slurpy array subparam retains =:=" (1)&(3), "binding of implicit @_ subparam retains =:=" (1)&(3), and all four "misuse of =:= is failure" tests (Mu / literals / != / even when ==). The slurpy/@_ failures stem from current Rakudo semantics where binding a slurpy param copies elements so =:= identity is not preserved across the call boundary; the "misuse of =:= is failure" tests expect =:= between non-containers to return a Failure, but raku returns a plain Bool. Cannot pass fully on raku, therefore cannot pass on mutsu regardless of interpreter changes.)
roast/S29-os/system.t (raku itself (Rakudo 2022.12) fails 2 of 41 subtests: subtest 18 "run() in sink context throws on unsuccessful exit and contains exit code of -1 and OS error" — the test expects the X::Proc::Unsuccessful message to match /"exit code: -1"/ and /"OS error = "/, but raku emits "exit code: 1, signal: 0" with no "OS error = " substring; and a later qx/qqx-related subtest. Cannot pass fully on raku, therefore cannot pass on mutsu regardless of interpreter changes.)
roast/S17-promise/then.t (raku itself dies at line 96 in sub get-results with "No such method 'andthen' for invocant of type 'Promise'" — the test calls `.andthen` on a Promise inside the get-results helper used by the "simple keep" subtest, but current Rakudo Promise has no `andthen` method. The test aborts mid-run on raku before reaching `done-testing`, so it cannot pass fully on any implementation without modifying the roast file. First 15 of 19 top-level subtests pass on raku; the remainder never run.)
roast/S17-lowlevel/lock.t (requires real OS-thread concurrency with true parallelism plus working Lock condition variables and $*SCHEDULER.cue: (1) Thread.start must run the block in a separate OS thread that actually runs concurrently with the main thread — mutsu runs Thread.start synchronously (or at least not in a way that interleaves with other threads), so the "Lock is at least somewhat effective" subtest, which depends on two threads racing to append 'a'/'b' to a shared string under a Lock and expecting an interleaved-but-locked pattern matching /^ [ a+: b+: | b+: a+: ] $/, fails because both threads run to completion one after the other; (2) Lock.condition / Cond.wait / Cond.signal require a real blocking condvar implementation with proper wait-queue semantics and release-the-lock-while-waiting behavior — the "Condition variable worked" subtest uses $c.wait() to block thread 1 until thread 2 calls $c.signal() inside the same Lock, and mutsu's wait never suspends so @log ends up as just "ale" instead of "ale,porter,stout"; (3) the "Thread 1 never ran after it was tried N times" subtests hammer the same wait/signal contract 100 times, needing correct predicate-form $c.wait({ $flag }) as well; (4) the lock/condvar crash subtest uses $*SCHEDULER.cue({...}) to enqueue work and then waits on a condvar until $done == $todo across 100 async tasks, which requires both an actual scheduler that runs queued work on worker threads and the same condvar machinery. Fixing any one subtest requires a real threading runtime (std::thread-backed Thread value, Arc<Mutex<>> / Arc<Condvar> backed Lock, a ThreadPoolScheduler implementation of $*SCHEDULER.cue), plus value sharing across threads — mutsu's Value and Interpreter are not currently Send+Sync and the global interpreter state is single-threaded. This is a cross-cutting concurrency rewrite, far beyond a single focused fix.)
roast/S32-array/adverbs.t (85 failures across many unrelated gaps: (1) callable indices — `@a[*-1]` style WhateverCode works but `@a[&some-sub]` / callable block indexing into array adverbial access returns wrong values (":!v" / ":!kv" / ":!p" on callable indexes yield -1 / (Any) instead of the expected tail element); (2) X::Adverb exception class — mutsu does not throw a typed X::Adverb with `.nogo`, `.unexpected`, `.source`, and `.what` attributes when adverbs conflict (`:k:v`), are unknown (`:foo`), or are mixed (`:kv:p:zip:zop`); needs a full X::Adverb class plus adverb-validation logic in the subscript operators for both normal and zen slices; (3) various adverb-on-missing-element edge cases where `@a[idx]` without adverb must return (Any) rather than Nil; (4) zen slice adverbs on arrays contained in `@a[]` need the same X::Adverb validation. Each piece is independent and substantial; scope exceeds a focused fix.)
roast/S17-promise/nonblocking-await.t (18/28 subtests pass; remaining 10 require several independent major subsystems: (1) Supply reactive programming with `supply { whenever Supply.interval(...) { emit ...; done } }` inside `await` — mutsu's await-on-supply returns 0 from `[+]` sum, meaning the interval-emit-done pipeline is not driven to completion; needs real Supply/Supplier machinery with timer-backed intervals, whenever-done control flow, and await-on-Supply blocking until done; (2) Channel blocking `await($c)` semantics where receiving from a closed empty channel must throw `X::Channel::ReceiveOnClosed` — mutsu returns 0 instead of blocking for a value and does not detect close-while-empty; (3) `X::Await::Died` exception class that wraps a Promise-body `die` with backtrace context showing both the dying sub name and the awaiting sub name in `.gist` — mutsu passes the original message through but does not synthesize an X::Await::Died wrapper nor include stack-frame names from both sides of the await; (4) `IO::Socket::Async.listen/connect` with react/whenever over the returned Supply and `$conn.write`/`.Supply(:bin)` — full async TCP socket server+client with bin/ascii encoding roundtrip; (5) `atomicint` with postfix `⚛++` and `is-deeply` on atomicint values — requires a new Value variant with atomic ops. Each subsystem is independently substantial; total scope spans async I/O, reactive Supplies, Channel blocking semantics, Await exception synthesis, and atomic types — far beyond a single focused fix.)
roast/S02-types/nil.t (raku itself (Rakudo 2022.12) fails to compile the test file: "Calling f2(Nil) will never work with declared signature (Int $x?)" at line 89 — Rakudo's compile-time type check rejects `f2(Nil)` for signature `Int $x?`. The file never runs on our installed raku, so the expected output cannot be obtained and the test cannot be whitelisted. Even ignoring that, several independent mutsu gaps would still need fixing: (1) `for Nil { }` should iterate once with a Nil topic (mutsu currently treats Nil as an empty list in `value_to_list`); (2) `Nil.ords` must return an empty Seq and emit a CX::Warn "Use of Nil in string context", and `Nil.chrs` must return "\0" and emit a similar warning — mutsu's Nil method fallback currently swallows both to Nil and does not raise CONTROL warnings for Nil string coercion; (3) the `is-eqv` / `is-deeply` comparisons in the test and two CONTROL `pass` tests depend on those warnings firing exactly once each; (4) `(my Str $x = Nil) === Str` — the assignment expression result must be the stored Str type object, but mutsu's scalar-assign path returns Nil unchanged from the stack; (5) `my MyInt $x = 5; $x = Nil; $x === MyInt` for `subset MyInt of Int` — mutsu defaults the language version to 6.e which nominalizes subset defaults to the base type, so Nil-assignment resets to Int instead of MyInt; changing the default version is a cross-cutting decision. Deferred until raku is upgraded and the mutsu gaps above are addressed.)
roast/S02-types/sethash.t (2 of 222 ran subtests fail; 62 later subtests never execute: requires parameterized SetHash[T] element-type tracking. The test expects SetHash[Int].new(<a b c>) to throw X::TypeCheck::Binding (now fixed) and also expects $s{42} = 1 on a SetHash[Str] to throw X::TypeCheck::Binding — needs per-Value::Set container_type_metadata registration, Value::Set variant in container_type_metadata/register_container_type_metadata lookup paths, propagation of the key-type constraint through scalar container binding ($s = SetHash[Str].new(...)), and indexed-assign key typecheck against the recovered constraint. Also needs .keyof to consult that metadata instead of hardcoding Mu for Set/Bag/Mix. Additional smaller issues likely still remain in tests 223-284 (parametric role inheritance class RT125611 is SetHash, SetHash.hash key identity, group-of tests, Nil-key exceptions, etc.) which cannot be observed because test 221/222 failures abort before them. Each piece is independently substantial; out of scope for a single focused fix.)
roast/S14-roles/versioning.t (requires a full Raku language-revision-aware role system: (1) per-role language-revision tracking via `use v6.c`/`v6.d`/`v6.e.PREVIEW` pragmas propagated into role metadata (`.^language-revision` on Role, pun, and parameterized pun); (2) multi-module multi-version role candidates — `VerRole` defined separately in Ver6c/Ver6d/Ver6e packages must coexist as candidates on a single shared role group with `.^candidates` returning all three and per-candidate `.^language-revision` returning c/d/e; (3) parameterized role selection by arity choosing the right language-revision candidate (`VerRole.new`/`VerRole[Str].new`/`VerRole[Str,Str].new` picking c/d/e respectively); (4) Enum type objects carrying `.^language-revision` from the `use v6.x` in effect at enum declaration site; (5) submethod inheritance rules that differ by language revision — a 6.c class consuming a 6.e role must NOT receive the role's submethods, a 6.e class consuming a 6.c role must also not receive them, and BUILD/TWEAK submethods from roles must run during `.new` only when role version and class version both allow; (6) `.^submethod_table` MOP introspection returning only class-local submethods; (7) `POPULATE` vs `BUILDALL` method-existence gating. Each of these is a substantial feature spanning parser (version pragma propagation), module loading (candidate merging across `use`), MOP (per-role revision metadata, submethod_table), role composition (version-gated submethod inheritance), and object construction (BUILDALL ordering with role-version filtering). Total scope is a full language-versioning subsystem — far beyond a focused fix.) (2 of 222 ran subtests fail; 62 later subtests never execute: requires parameterized SetHash[T] element-type tracking. The test expects SetHash[Int].new(<a b c>) to throw X::TypeCheck::Binding (now fixed) and also expects $s{42} = 1 on a SetHash[Str] to throw X::TypeCheck::Binding — needs per-Value::Set container_type_metadata registration, Value::Set variant in container_type_metadata/register_container_type_metadata lookup paths, propagation of the key-type constraint through scalar container binding ($s = SetHash[Str].new(...)), and indexed-assign key typecheck against the recovered constraint. Also needs .keyof to consult that metadata instead of hardcoding Mu for Set/Bag/Mix. Additional smaller issues likely still remain in tests 223-284 (parametric role inheritance class RT125611 is SetHash, SetHash.hash key identity, group-of tests, Nil-key exceptions, etc.) which cannot be observed because test 221/222 failures abort before them. Each piece is independently substantial; out of scope for a single focused fix.)
